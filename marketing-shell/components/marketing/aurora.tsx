// Retired decorative layer. The Apple-style surface uses flat colour and
// whitespace; this stays a no-op so the pages that mount it need no edits.
export function Aurora(_props: { className?: string }) {
  return null;
}
