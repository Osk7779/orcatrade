# OrcaTrade — Continued Development Plan

**Author:** Claude (working with Oskar Klepuszewski)
**Last updated:** 2026-05-13 (Sprint D — TARIC live integration complete)
**Status:** Living document — update each sprint completion.
**Companion doc:** Backend / infra / scale / enterprise work tracked in
[backend-grade-plan.md](backend-grade-plan.md). This doc stays focused
on product + marketing + SEO sprints.

---

## North star

OrcaTrade is the **import operations team available 24/7** for European
SMEs sourcing from Asia. Five domains (search, sourcing, intelligence,
logistics, finance), one platform, calculator-grounded recommendations
the user can trust.

The platform should always be **further ahead than the business needs it
to be** — but not by so much that we waste sprints on features no real
customer has asked for.

## Operating principles

1. **Ship per sprint.** Every sprint ends with a `git push` and Vercel
   auto-deploy. No long-lived branches.
2. **Tests are the contract.** New code ships with tests; suite stays green.
   Current baseline: 562/562.
3. **Calculator-grounded.** Never expose to the user a number that didn't
   come out of `lib/intelligence/*-quote.js` or a documented data source.
4. **Pure-JS, zero npm.** Stay deployable on Vercel Hobby. Single
   `api/[...path].js` dispatcher; no build pipeline; no React.
5. **Match scope to evidence.** Don't add features for hypothetical users.
   When in doubt, build the smallest thing that lets a real founder feel
   the difference and stop there.
6. **Picking next moves.** When the user says "continue the development"
   without specifics: check this plan, pick the topmost open sprint,
   confirm in 1–2 sentences, then execute.

## What's done (cumulative state at Sprint 33)

| Sprint | Outcome |
|---|---|
| ≤22 | Pre-context-summary baseline: site, agents, calculators |
| 23–28 | Programmatic SEO sprint — 351 guide pages in EN/PL/DE |
| 29–30 | Polish + German full localisation of routing/customs/warehouse guides |
| 31 | Import Plan Builder at `/start/` — 6-step wizard, 4-calculator orchestration |
| 32 | Shareable plan permalinks (`?p=<base64>`) + homepage hero promotes wizard |
| 33 | Localised wizard: `/pl/start/` and `/de/start/` + locale-aware emails |

**Live surfaces:** homepage, platform overview, 351 SEO guide pages,
3-locale wizard, 5 AI agents, contact form, dashboards, free analysis.

## Open frontier — ranked by leverage

### Horizon 0 — Intelligence depth (current focus)

**Why this is now the priority:** the platform looks smart on the
surface but has thin internals. Customs = duty + VAT + brokerage; an
accountant builds that in Excel. Sourcing = country averages; an
importer needs supplier-level signals. Without depth, paywalling the
product (H3) is charging Pro fees for what a free Excel template
replicates.

H0 sprints are pure code/data — no auth, no KV, no infra. They could
ship next week and double the perceived intelligence of every plan.

#### Sprint A — EU trade defence: anti-dumping + countervailing duties

**Why now:** Currently we compute MFN duty and stop. A user importing
bicycles ex-CN sees 14% MFN — the actual landed duty is 62.5% (14% MFN
+ 48.5% ADD). We're under-quoting by 5-figures on the most common
high-volume importable goods.

**Scope:**
- `lib/intelligence/data/eu-trade-defence.js` — curated dataset of ~50
  active EU AD/CVD measures by HS chapter + origin. Fields: HS prefix,
  origin, type (AD/CVD), rate range, regulation citation, notes.
- Customs calculator looks up applicable measures, adds to duty line.
- Result UI surfaces measure as a separate cost component.
- Email summary includes the warning.
- Tests for known cases: bicycles CN (AD 48.5%), e-bikes CN (AD+CVD),
  aluminum extrusions CN, ceramic tiles CN, fiberglass fabric CN.

**Deliverables:** dataset file, calculator extension, UI, 10+ tests.
Disclaimer surfaced: "Curator data, not legally authoritative — verify
on TARIC before commercial decisions."

**Estimate:** 1 session.

---

#### Sprint B — Preferential origin pathways

**Why now:** Bangladesh apparel ships at *zero duty* with valid EUR.1
under EBA. Vietnam under EU-Vietnam FTA. Türkiye under the Customs
Union with ATR. We currently apply flat MFN. A user with an LDC
supplier sees 12% duty when they should see 0%.

**Scope:**
- Origin-pathway data: which (origin × HS chapter) pairs unlock which
  preferential regime (GSP+, EBA, EU-VN FTA, EU-KR FTA, ATR, REX).
- Wizard already asks `claimPreferential` boolean → extend to ask
  *which regime* if user says yes, OR auto-detect best regime.
- Customs calc applies preferential rate (often 0%) when valid claim.
- Result explains what document the user needs (EUR.1 / Form A / REX
  registration) to actually claim it.

**Estimate:** 1 session.

---

#### Sprint C — Compliance overlay in the plan

**Why now:** CBAM/EUDR/REACH/CE pages exist as standalone UI; the
import-plan generator never surfaces them. A user importing aluminum
windows from VN should see "CBAM applies — quarterly reporting from
your importer-of-record." They don't.

**Scope:**
- HS-chapter → applicable regimes mapping (CBAM: chapters 25, 27, 28, 31,
  72-76; EUDR: 09, 18, 40, 44, 47-49; REACH: substantively all chemicals;
  CE: most electronics, machinery, toys, PPE).
- Result section in wizard: "Compliance check" with applicable regimes,
  next steps, and links to OrcaTrade's deeper guides.
- Email body includes the same.

**Estimate:** 1 session.

---

#### Sprint D — Live TARIC integration (shipped 2026-05-13)

**Status:** Done. See the update log entry for the full ship summary.
The key design decision worth remembering: EU TARIC does NOT have a
clean public REST API. We use the UK Trade Tariff API as a free
sub-chapter sanity-check source (disclosed in every result), with KV
caching at 7-day fresh / 30-day stale, and a hard env-var kill-switch
for tests + CI. The architecture is pluggable — `fetchUpstreamRate` is
the one place to swap in a paid EU-direct provider or a bulk-XML
ingestion pipeline if the UK divergence becomes a real customer
complaint.

---

### Horizon 1 — Conversion-side polish (after H0)

#### Sprint 34 — Localised AI agents

**Why now:** Wizards are now PL/DE but the orchestrator/sourcing/logistics
agents only respond in English. A Polish founder who hits "Pogłęb plan"
gets an English reply — broken UX.

**Scope:**
- Read locale from `?lang=` query string OR Accept-Language fallback
- Inject locale into the agent's system prompt: "Reply in Polish unless
  the user writes to you in another language."
- Localise the agent landing page chrome (`/agent/orchestrator/`,
  `/agent/sourcing/`, `/agent/logistics/`, `/agent/finance/`, `/agent/`)
- Translate the seed-prompt suggestions visible on the agent page
- Frontend localStorage key already includes locale — don't bleed
  Polish history into the English experience

**Deliverables:**
- `/pl/agent/orchestrator/`, `/de/agent/orchestrator/` (and 4 more × 2)
- `lib/agents/i18n.js` parallel to `lib/start-i18n.js`
- 10–15 new tests covering locale routing in agent handlers
- Cross-link wizard CTAs to locale-correct agent pages

**Estimate:** 1 long session.

---

#### Sprint 35 — Plan-revision diff + email follow-ups (deferred)

**Why deferred:** Premature without recurring users (H2). Permalinks
already exist; "what changed since you built this" is a *retention*
feature, but we don't have repeat visitors to retain yet. Park until
post-H2 when we have signed-in users and saved plans with history.

**Original scope preserved for later:**
- Permalinks already let users return to a plan. But pricing
shifts (duty rates change quarterly, freight rates monthly). When a user
revisits, they should see *what changed since they built it* — turning
the permalink into a recurring-engagement mechanism.

**Scope:**
- Encode plan generation date inside the share URL alongside inputs
- On permalink load, recompute and diff the new totals vs. the snapshot
  embedded in the URL → render a "What's changed" callout if delta > 5%
- Resend email at T+30 days: "Your plan from <date> — pricing has
  shifted by €X. Open updated plan."
- Cron-style: not actually a cron (Vercel Hobby has no scheduled fns);
  use Resend's scheduled-send if available, OR ship as a manual founder
  tool the user can trigger weekly

**Deliverables:**
- Plan codec extended to embed `generatedAt` + landed-cost snapshot
- New "What changed" UI block in the result renderer
- New backend handler `/api/plan/refresh` that produces a re-engagement
  email body for a given share URL

**Estimate:** 1 session. Defer the cron piece if Vercel constraints bite.

---

#### Sprint 36 — Conversion analytics dashboard (deferred)

**Why deferred:** Need KV (Sprint 37) to store events durably across
function instances. Build alongside H2 once persistent storage exists.

**Why later:** Every plan submission already logs a structured
`import_plan_generated` JSON event. Vercel captures stdout — it's just
not surfaced. Without visibility, we can't tell which categories
convert, which origin/destination pairs perform, or where users drop off.

**Scope:**
- Append-only file store of structured events (Vercel /tmp is
  per-instance — won't work; better: write to GitHub Gist via API,
  or use Vercel KV when we add it in Sprint 37)
- Simplest viable: ship a `/dashboard/leads/` page that fetches recent
  events and renders summary tiles (count by locale, by category, by
  destination, top routes, mean landed total)
- Auth-gate via a single `?token=<secret>` query param read from env

**Deliverables:**
- `lib/handlers/leads-summary.js` reading from event store
- `dashboard/leads/index.html` with charts (no chart library — pure CSS
  bars and number stats)
- 5 tests covering aggregation correctness

**Estimate:** 1 session, pending Sprint 37 if we want persistent storage.

### Horizon 2 — Foundational retention layer (next 4-6 sprints after H1)

These unlock everything beyond the current stateless conversion model.
Required for: paying customers, returning users, team accounts,
saved plans, billing.

#### Sprint 37 — Vercel KV persistence

**Why:** Everything currently stateless. To save plans, attach them to
users, build a dashboard with history, or send delayed follow-ups, we
need real storage. Vercel KV (Upstash Redis) is the path of least
resistance — pure REST API, free tier sufficient for early traction.

**Scope:**
- Provision Vercel KV (manual, requires user on Vercel dashboard)
- New `lib/intelligence/kv-store.js` wrapping the REST API
- Migrate `runtime-store.js` rate-limit + cache from in-memory to KV
- Persist `import_plan_generated` events to KV with 90-day TTL
- Plan-by-ID storage so share URLs can be short canonical IDs
  (`/start/p/<8-char-id>`) instead of base64 inputs

**Caveat:** Keep base64 share URLs working as a fallback — they're
zero-storage and serve as backup if KV is misconfigured.

**Estimate:** 1–2 sessions.

---

#### Sprint 38 — Magic-link auth (no passwords)

**Why:** Returning users + saved-plan history requires identity. Magic
links via Resend = simplest possible onboarding, no password-reset hell.

**Scope:**
- `/api/auth/request` — accept email, generate signed token, email link
- `/api/auth/verify` — verify token, set httpOnly cookie
- `lib/auth.js` — request-context user resolution from cookie
- Session = signed JWT, no server session store
- `/account/` page showing identity + sign-out
- Existing wizard pre-fills email from cookie if logged in

**Estimate:** 2 sessions.

---

#### Sprint 39 — Saved plans + history

**Why:** With auth + KV in place, plans can persist per-user.

**Scope:**
- On wizard submit, if logged in → save plan to user
- `/account/plans/` lists user's plans with stats summary
- "Open in wizard" button reloads the plan into the wizard for editing
- Sharing a plan with a colleague creates a permission grant (read)
- Comments thread per plan? (Defer until customer asks.)

**Estimate:** 1–2 sessions.

### Horizon 3 — Pricing + payments (after H2)

#### Sprint 40 — Pricing page + tier definition

**Why:** Without a published price, every conversation is "what does
it cost." Lock down the offer.

**Scope:**
- Decide tiers WITH user (not for them) — current best guess:
  - Free: 1 plan/month, public guides, 1 agent query/day
  - Pro (€49/mo): unlimited plans, all agents, history, sharing
  - Team (€199/mo): 5 seats, shared plans, priority support
  - Enterprise: custom — contact us
- `/pricing/` page (already exists in nav — currently a stub)
- Feature flags table in `lib/auth.js` keyed off subscription tier

**Estimate:** 1 session of design conversation + 1 session of build.

---

#### Sprint 41 — Stripe integration

**Why:** Move the conversation from "this is great" to "here's my card."

**Scope:**
- Stripe Checkout for subscriptions (hosted, no PCI)
- Webhook handler in `lib/handlers/stripe-webhook.js`
- Sub → user.tier in KV
- `/account/billing/` linking to Stripe customer portal

**Estimate:** 1–2 sessions, hard part is webhook idempotency.

---

#### Sprint 42 — Tier gating

**Why:** Free → Pro friction must exist or no-one upgrades.

**Scope:**
- Wire feature flags through the API handlers
  (rate-limit free tier harder, gate orchestrator behind Pro)
- "Upgrade to Pro" upsell at gate points

**Estimate:** 1 session.

### Horizon 4 — Tier 2 (Marketplace) — only if validated

This was scoped early but defer until we have ≥10 paying customers
asking for it. Building a marketplace before having sustained traffic
is a graveyard pattern.

When we get there:
- Vetted supplier directory (start: ~50 suppliers, manual vetting)
- Quote-request workflow with anonymised buyer profile
- Trade-finance partner referral (we don't underwrite — Tradeshift,
  Stenn, FreshFi referrals)
- Insurance partner referral (we don't underwrite — Cargo + trade-credit)
- Take rate or fixed referral fee — model undecided

### Horizon 5 — Always-on: GTM enablement tools

Code OrcaTrade can ship that helps the founder do GTM faster.
Don't treat as a sprint sequence — interleave with H1-H4 when the
specific need arises.

| Tool | When to build |
|---|---|
| Outbound email composer in agent UI | When founder is sending >10 cold emails/week |
| Lead enrichment from LinkedIn URL | When >50 inbound leads/month |
| Demo recorder / screen-share replay | When closing >2 demos/week |
| LinkedIn post drafter (uses brand voice from `docs/brand-kit/`) | If founder wants to post weekly |
| Press kit page (`/press/`) | When PR opportunities arrive |
| Partner page (`/partners/`) | When ≥3 partners signed |

### Horizon 6 — Things to NOT build

Items the user (or a future me) might be tempted to build that should
be explicitly resisted:

- **Mobile app.** Browser-first SaaS; nothing here demands native.
- **Multi-tenant white-label.** Premature; no customer asking.
- **Custom CRM.** Use HubSpot free / Pipedrive when needed.
- **Custom analytics.** Plausible / PostHog handle this; don't reinvent.
- **AI-generated supplier matching at scale.** We have a curated 5-country
  comparison; do not pivot to "any supplier on Alibaba" — destroys our
  moat (calibrated trust over breadth).
- **Crypto / blockchain payments.** Out of scope.
- **More languages beyond EN/PL/DE.** Until we have customers in another
  market driving demand, three is enough.

## Capacity reality check

A "session" in this plan is one continuous Claude Code conversation,
typically 1–2 hours of focused execution.

- Horizon 1 (3 sprints) = ~3 sessions = ~1 week of intermittent work
- Horizon 2 (3 sprints) = ~5 sessions = ~2 weeks
- Horizon 3 (3 sprints) = ~4 sessions = ~1.5 weeks

Total to fully-featured, paid-tier product: ~12 sessions = ~5 weeks of
work spread across however long the founder takes.

## How future-me should use this document

When user says "continue the development" or similar without specifics:

1. **Read this file.** Read `MEMORY.md` and `git log --oneline -10`.
2. **Check git status** for uncommitted work — finish that first.
3. **Pick the topmost open sprint** in the highest-priority horizon that
   isn't blocked.
4. **Confirm in 1–2 sentences** with the user: "Picking up Sprint 34 —
   localised AI agents. About 1 session of work, ends with a deploy.
   Sound good?"
5. **Execute.** Follow the sprint's scope + deliverables. Tests, commit,
   push. Update this file with a new row in the Done table.
6. **Update the open-sprint section** if scope shifted during execution
   (don't pretend a sprint was as scoped if it wasn't — write what
   actually shipped).

If the user pushes back on the topmost sprint ("no, I'd rather work on
X"), defer to them — but log a note here so future-me knows the user
prefers a different ordering than I would naively pick.

## Things this plan deliberately leaves out

- **Feature wishlist from old conversations.** I'd rather under-promise
  here and over-deliver in execution than ship a 100-item backlog that
  ages badly. If a feature came up in a past sprint and isn't here, it
  was deprioritised on purpose.
- **Marketing strategy.** The 7-employee marketing manual in
  `docs/marketing-ops.md` covers that. This plan is for code.
- **Hiring / team.** Out of scope for me to plan.
- **Fundraising milestones.** Out of scope.

## Update log

| Date | Sprint completed | Notes |
|---|---|---|
| 2026-05-08 | 33 — Localised wizard | EN/PL/DE end-to-end. 562/562 tests. |
| 2026-05-08 | H0 Sprint A — Trade defence DB | 30+ AD/CVD measures wired into customs calc. Bicycles ex-CN: 10% → 58.5% duty. 581/581 tests. |
| 2026-05-08 | H0 Sprint B — Preferential origin | EBA/GSP/FTAs/ATR proper regime DB. BD apparel: 0% with REX. AD stacks on top of preferential (TR steel). 605/605 tests. |
| 2026-05-08 | H0 Sprint C — Compliance overlay | 12 regimes (CBAM/EUDR/REACH/CE/RoHS/WEEE/Battery/Toys/Cosmetics/PPWR/GPSR/Footwear). Severity-sorted in plan output. 634/634 tests. |
| 2026-05-08 | Sprint 34 — Localised AI agents | 5 agents × 3 locales. EN/PL/DE landing pages, locale directive injected into system prompts, wizard CTAs route locale-correct. 648/648 tests. |
| 2026-05-08 | Sprint G — Origin sensitivity matrix | Re-runs customs+freight for CN/VN/IN/BD/TR + user origin. Surfaces cheapest alternative + €/% saving. Annual estimate when monthly volume given. 660/660 tests. |
| 2026-05-08 | Sprint I — Currency / FX risk overlay | 13-currency snapshot table, fx-quote calculator with vol-90d + hedge cost + recommendation logic. Wizard step 4 has currency + payment-terms fields. 681/681 tests. |
| 2026-05-08 | Sprint M — Trade defence SEO pages | 1 page per AD/CVD measure × 3 locales = 96 new pages. Each cites EU regulation, includes worked landed-cost example, CTAs into wizard with HS pre-filled. Total guides: 351 → 447. 699/699 tests. |
| 2026-05-08 | Sprint N — Preferential origin SEO pages | 7 regime guides (EBA/GSP+/GSP/EVFTA/EUKFTA/EUJEPA/ATR) + 7 country pivots × 3 locales = 45 new pages. Total guides: 447 → 492. 719/719 tests. |
| 2026-05-08 | Sprint O — Compliance overlay SEO pages | 13 regimes (CBAM/EUDR/REACH/CE×2/RoHS/WEEE/Battery/Toy/Cosmetics/GPSR/PPWR/Footwear) × 3 locales = 42 new pages. Total guides: 492 → 534. Completes the H0→SEO trilogy. 737/737 tests. |
| 2026-05-08 | Sprint H — Annual TCO calculator | Procurement-grade view: annual scaling, inventory carrying cost (avg inventory × WACC × days/365), 3PL roll-up, bonded deferral hint, sensitivity at 6/12/24/52 shipments/year. New wizard fields shipmentsPerYear / waccPct / daysInInventory in step 5. 757/757 tests. |
| 2026-05-08 | Sprint Q — Working capital cycle | CCC = DIO + DSO − DPO. Reuses paymentTermsDays as DPO + daysInInventory as DIO from prior sprints; adds daysReceivable. Surfaces working capital tied up + annual capital cost + verdict (tight/standard/capital-intensive/severe/supplier-funded) + 3 levers (DPO+30, DIO−20, DSO−15) with €/year savings each. 775/775 tests. |
| 2026-05-08 | Sprint P — H0 cross-linking on legacy guides | findRelatedH0 helper + pageShell linkContext parameter wired into all 12 sourcing/routing/customs/warehouse generators × 3 locales. Each of the 351 legacy guides now surfaces 2-5 contextual links into trade-defence / preferential / compliance H0 pages (or index fallback). 796/796 tests. |
| 2026-05-08 | Sprint S — Wizard PDF export | @media print CSS rules (A4, light theme, hide chrome, page breaks, URLs after links). Print-only header with brand/route summary/date. "Save as PDF" + "Print" buttons calling window.print(). i18n EN/PL/DE. 811/811 tests. |
| 2026-05-08 | Sprint Y — Worked example gallery | 8 curated import scenarios (CN apparel, BD-EBA, VN-EVFTA, CN e-bikes 87% AD+CVD, CN aluminium+CBAM, TR steel A.TR+AD, IN cosmetics, KR machinery EUKFTA) × 3 locales = 27 pages. Each runs composePlan, surfaces real numbers, links to wizard with permalink. Total guides: 534 → 561. 828/828 tests. |
| 2026-05-08 | Sprint AA — Homepage examples feature | New "Worked examples" section between hero and mission on EN/PL/DE homepages. 3 featured cards (e-bike 87% AD+CVD, BD EBA, TR A.TR+AD) + "See all 8 examples" link. PL/DE homepages also gained a primary `/start/` CTA replacing the old `#group` anchor. 846/846 tests. |
| 2026-05-08 | Sprint R — Sitemap hreflang | xhtml:link rel="alternate" hreflang=en/pl/de/x-default emitted in sitemap.xml + sitemap-guides.xml for all 210+ H0 + example URLs. Sub-generators now expose hreflangAlternates on return; sitemap builder consumes it. 840 xhtml:link entries total. 857/857 tests. |
| 2026-05-08 | Sprint AD — Legacy generator hreflang pass-through | All 12 legacy sourcing/routing/customs/warehouse generators (×3 locales) now expose hreflangAlternates on return — sitemap entries gained locale-specific slug alternates (e.g. apparel-from-cn ↔ apparel-z-cn ↔ apparel-cn). Total xhtml:link entries: 840 → 2,114. 862/862 tests. |
| 2026-05-08 | Sprint AC — Wizard origin-comparison mode | Each non-user row in the origin-sensitivity matrix gets a "Compare" button. Clicking renders an inline side-by-side panel: duty %, transport, landed cost, annual cost, preferential, AD/CVD with delta badges (green saving, red penalty). Uses matrix data already on the plan — no extra fetch. Verdict line summarises €/year impact. EN/PL/DE. 877/877 tests. |
| 2026-05-08 | Sprint AF — Comparison-mode share permalink | `?p=<base64>&c=<origin>` URL form auto-renders the share-plan AND opens the comparison panel for the named alternative. New "Copy comparison URL" button in the comparison header copies the exact view via clipboard API (with execCommand fallback). EN/PL/DE. 890/890 tests. |
| 2026-05-08 | Sprint AG — Scenario toggle (claim preferential) | "Re-run plan with this claimed" button in the preferential-available callout flips claimPreferential and re-fetches /api/start to show the alternate scenario end-to-end (duty drops to 0% via EBA/EVFTA/etc., downstream TCO + working capital recompute). State tracks baseline inputs; sticky banner with "Switch back" returns to original. EN/PL/DE. 905/905 tests. |
| 2026-05-08 | Sprint AB — Featured example shuffler | Homepage example cards expanded from 3 hardcoded to 6-in-pool; inline Fisher-Yates shuffler hides 3 per page load. All 6 stay in source HTML for SEO. EN/PL/DE. 918/918 tests. |
| 2026-05-08 | Sprint 37 — KV-store primitives | New lib/intelligence/kv-store.js exposes set/get/del/incr/listKeys/setJson/getJson with isConfigured + getMode. Durable backend (Upstash REST) when KV_REST_API_URL+TOKEN set; in-memory fallback otherwise. Foundation for upcoming auth (Sprint 38), saved plans (Sprint 39), event log (Sprint 36), plan-revision diff (Sprint 35). 943/943 tests. |
| 2026-05-08 | Sprint 38 — Magic-link auth | lib/auth.js (token gen, HMAC-signed session cookie, getCurrentUser). lib/handlers/auth.js dispatches /api/auth/{request,verify,me,logout} via second URL segment. /account/ page with 4 states (loading/signin/sent/signedin) + 3-tier fallback flow. Resend email send. Rate-limited 5 req / 5 min / IP. KV stores 15-min magic tokens; 30-day session cookies. 978/978 tests. |
| 2026-05-08 | Sprint 39 — Saved plans + per-user history | lib/saved-plans.js with CRUD keyed by email; ownership-checked. lib/handlers/plans.js dispatches POST/GET /api/plans + GET/DELETE /api/plans/<id>. Wizard result gets a "Save plan to my account" button (visible only when signed in). New /account/plans/ page lists user plans with Open/Delete actions. Plans capped at 50/user, 1-year TTL. 1,004/1,004 tests. |
| 2026-05-08 | Sprint 35 — Plan-revision diff | New lib/plan-diff.js (extractSnapshot, sanitiseSnapshot, diffSnapshots, enrichRecord). savePlan persists a 6-field snapshot {asOf, perShipmentLandedTotal, dutyEur, vatEur, transportEur, brokerageEur, dutyRatePct} computed via composePlan at save time. /api/plans (list + single GET) recomputes current and attaches {current, delta} per record; primaryDriver picks the component that moved most in absolute EUR; significance threshold ≥5%. /account/plans/ renders a "what's changed" badge on each card (red ▲ up / green ▼ down / flat). 1,024/1,024 tests. |
| 2026-05-08 | Sprint 36 — Conversion analytics dashboard | New lib/events.js (record/list/aggregate, KV-backed, 5,000-event cap). start handler records `import_plan_generated`; plans handler records `plan_saved`. lib/handlers/leads.js exposes GET /api/leads token-gated by ORCATRADE_LEADS_TOKEN (constant-time compare). New /dashboard/leads/ page renders summary tiles (total, email captured, mean landed cost, distinct routes) + 6 CSS-bar panels (categories, routes, origins, destinations, locales, types) + recent-events table. Token caches in sessionStorage so the dashboard stays open across reloads but clears on tab close. 1,041/1,041 tests. |
| 2026-05-08 | Sprint 40 — Pricing page + tier definition | New lib/tiers.js: canonical 5-tier catalogue (free/starter/growth/scale/enterprise) with quotas, features, hasFeature/getQuota lookups, and toCatalog() for JSON-safe export. New lib/user-tier.js: KV-backed tier:<email> persistence with default-to-free fallback. New lib/handlers/tiers.js dispatching GET /api/tiers (public catalogue) + GET /api/tiers/me (auth-only). Existing /pricing/ CTAs now carry data-tier="<id>" hooks for Sprint 41 + a /api/tiers/me bootstrap that flags the user's current plan. 1,067/1,067 tests. |
| 2026-05-08 | Sprint 41 — Stripe Checkout + webhook + billing page | New lib/stripe.js: zero-dep REST client, x-www-form-urlencoded encoder with PHP bracket-notation, HMAC-SHA256 webhook signature verify with constant-time compare + 5-min timestamp tolerance. New lib/handlers/billing.js dispatching POST /api/billing/{checkout,portal,webhook} + GET /api/billing/me; webhook is idempotent via stripe:event:<id> KV dedupe; reverse map stripe:email-by-customer:<id> lets subscription.deleted downgrade to free. Pricing page CTAs now POST to /api/billing/checkout (falls back to sign-in or contact). New /account/billing/ page surfaces tier + opens Stripe customer portal. Test-mode by default — env vars STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_PRICE_<TIER>_<CYCLE>. 1,093/1,093 tests. |
| 2026-05-08 | Sprint 42 — Tier gating across handlers | New lib/gating.js with checkFeature/checkQuota/gate primitives; signed-in users keyed by email, anonymous keyed by IP. Quota counters in KV at quota:<id>:<name>:YYYY-MM with 62-day TTL. Wired feature + monthly-query gates into orchestrator (Growth+), compliance-agent (Free entry), sourcing-agent (Starter+), logistics-agent (Growth+), finance-agent (Growth+). Plans handler enforces savedPlans quota at POST. Gate verdicts return 402 tier_gate (feature) or 429 tier_quota (counter), both with currentTier/minimumTier/upgradeUrl in body. gate(res, verdict) writes either Vercel-Express or raw-Node responses. 1,113/1,113 tests. |
| 2026-05-08 | Sprint AE — RSS / Atom feed | New lib/feed-builder.js scans guides/* + examples/* (recursive) and extracts title/description/canonical from each index.html. New lib/handlers/feed.js serves either RSS 2.0 or Atom 1.0 (?format=rss|atom, default rss) with Cache-Control max-age=3600 + stale-while-revalidate=21600. vercel.json rewrites /feed.xml + /atom.xml. Home page <link rel="alternate"> advertises both. 185 items in feed (178 guides + 7 examples). 1,132/1,132 tests. |
| 2026-05-08 | Sprint AH — Currency display toggle (EUR/USD/CNY/VND/PLN) | fmtEur in start/app.js now emits `<span class="amt" data-eur="N">€X,XXX</span>` so a post-render walker can re-format every amount on demand. Inline FX_DISPLAY snapshot mirrors lib/intelligence/data/fx-snapshot.js (EUR=1, USD=1.08, CNY=7.85, VND=26300, PLN=4.30). Pill-style currency-toggle in result section, asOf banner shows snapshot date when in non-EUR mode. Preference persists in localStorage. EN/PL/DE labels added to start/i18n.js. 1,144/1,144 tests. |
| 2026-05-08 | Sprint AI — Implementation roadmap per plan | New lib/intelligence/implementation-roadmap.js: 5-phase backbone (pre-departure, production+QC, logistics+customs, arrival+inland, post-arrival) with conditional tasks driven by plan content (bonded warehouse adds bonded-entry sub-tasks; preferential origin adds EUR.1/origin-cert prep; trade-defence adds TARIC + surveillance; CBAM/EUDR/REACH add reporting cadences; FX hedge rec adds forward-execution; matrix-cheaper origin flags re-source memo; high working-capital triggers payment-terms negotiation). composePlanWithRoadmap wraps composePlan so saved-plan snapshots stay cheap. Wizard renders phase cards + 3-col task tables (when/action/owner) with deliverable + evidence chips. EN/PL/DE i18n parity. 1,160/1,160 tests. |
| 2026-05-09 | Sprint H4 — Supplier directory shell | New lib/intelligence/data/supplier-exemplars.js: 15 anonymised composite cards (8 verticals × 8 countries) — ASEAN, South Asia, East Asia, Türkiye, EU. Each entry carries category, region, years operating, MOQ range, lead time, certifications, capabilities, preferential-origin eligibility flag, notes. New /marketplace/ page renders cards with country-filter pills + two CTAs (Request introduction + Apply to be vetted). Anonymisation banner explains the shell — no live marketplace until curated network + consent + vetting pipeline are real. 1,173/1,173 tests. |
| 2026-05-09 | Sprint H5 — GTM tooling (press + partners + outbound composer) | New /press/ page: positioning sentence, founder bio (Oskar Klepuszewski), key facts grid (HQ, AI agents, 180+ guides), brand assets (logo, brand-kit, RSS), quotable lines, press contact. New /partners/ page: three relationship modes (Recommended/Referral/Commercial) + 6 categories (freight, finance, FX, insurance, inspection, compliance). New /agent/outbound/ tool: founder-grade cold-email composer that POSTs to /api/orchestrator with a brand-voice-pinned prompt; handles 402 tier_gate gracefully (Growth+); recipient + company + hook + goal + tone inputs; copy/regenerate actions. 1,188/1,188 tests. |
| 2026-05-10 → 2026-05-12 | i18n marketing-layer parity (retro-logged) | Tri-lingual coverage of every marketing/marketing-adjacent page: PL+DE pages for /press/, /partners/, /marketplace/, /pricing→cennik/preise/, /search/, /returns/, /customs/, /routing/, /documents/, /warehouse/, /buyer-verification/, /samples/, /insurance/, /agents/, /logistics→logistyka/logistik/, /platform→platforma/plattform/, /analysis→analiza/analyse/, /supply-chain→lancuch-dostaw/lieferkette/. Localised slug overrides applied where appropriate. hreflang tags added to all EN originals; sitemap registers every PL+DE entry with xhtml:link alternates. PL+DE homepage overhaul to mirror EN exactly: Aurora background + right-edge Page TOC + globe-canvas hero (replacing video-bg) + "Find · Verify · Ship · Finance" framing + Five Stages section with new Logistics card + Pinned Story scrolling section + d3 + globe.js loaders. Tools mega-dropdown made locale-aware in js/site-nav.js with I18N table + SLUG_OVERRIDES; six pages (PL+DE index/finance/sourcing) swapped from hand-rolled inline headers to `<header data-site-header>`. CBAM compliance brief feature card added to pl/intelligence.html + de/intelligence.html linking to /pl/analiza/ and /de/analyse/. Pure content + nav work; no test changes — baseline still 1,188/1,188. |
| 2026-05-12 | Mobile-nav z-index + tap-target fix (retro-logged) | Open mobile menu was at z-100, sitting BELOW the sticky header (1000), the first-visit cache modal (990), and the page-toc dots (40). Bumped open `.nav-links` to z-1500 with `.nav-toggle` and `.lang-switcher` pinned at z-1600 so the close button stays reachable. Every nav link + Tools mega-dropdown item now has a 44×44 px tap area (WCAG 2.5.5). Pure CSS change. |
| 2026-05-12 → 2026-05-13 | Favicon v3 → v7 + Google Search Console verification | v3 added 192/512 PNGs that Google's crawler prefers and reordered link tags so 192 is first. v4 added `?v=` cache-busters. v5 moved icons to a fresh `/icons/orca-*` namespace plus a defensive inline JS script that re-injects link elements at runtime so browsers can't honour pre-decided "no favicon for this URL" cache state. v6 cropped the wordmark out of orcatrade_logo.png with PIL — `orcatrade-mark.png` (800×800) became the master, regenerated every size from it. v7 inlined the 32×32 + 48×48 PNGs as base64 `data:` URIs in the FIRST `<link rel="icon">` tags so the favicon is materially present in HTML bytes regardless of cache state. Google Search Console verified via DNS, indexing requested for /, /pl/, /de/. |
| 2026-05-13 | Sprint D — Live TARIC integration | New lib/intelligence/taric-client.js — async `lookupHsRate(hsCode, originIso)` with 7-day fresh + 30-day stale KV cache, 4 s upstream timeout, hard kill-switch via `ORCATRADE_DISABLE_LIVE_TARIC` env (set in `npm test`). Upstream: UK Trade Tariff API v2 as the initial free public source (sanity-check; EU may differ — disclosed in every result). Customs calculator gained `calculateQuoteAsync` wrapper that engages live lookup when hsCode is 8+ digits AND origin is set; trade-defence and preferential-origin still stack on top of the live MFN exactly as before. `resolveDutyRate` gained `mfnRateOverride` + `mfnRateOverrideSource` params; result exposes `duty.mfnSource`, `duty.chapterRate*`, and `duty.liveRateMeta`. composePlan and composePlanWithRoadmap migrated to async — call sites in start/cron/plans handlers + the example-plans script + every test updated (codemod over 15 files, +59 async openers, +65 awaits). New wizard duty-source badge ("live · cached" green vs muted "chapter estimator" tag, with chapter-vs-live divergence note when ≥0.5pp apart). Public EU TARIC verify deep-link surfaced in next-steps when live rate applied. Test count: 1,188 → 1,268 (+17 new TARIC tests; +63 from the post-2026-05-09 work that hadn't been logged). |
| 2026-05-16 | Sprint D.1 — TARIC parser validation + heading fallback | Hands-on smoke test against the real UK Trade Tariff API revealed the first-pass parser walked the wrong JSON:API shape — the production version would have always returned null. Split `fetchUpstreamRate` (network) and `parseUpstreamResponse` (pure parse, now exported) so we can fixture-test the response handling without monkey-patching `global.fetch`. Real shape: `measure` entries carry `relationships.measure_type.data.id` (type "103" = third-country MFN), which points at a `duty_expression` whose `attributes.base` is "12.00 %"-style; strips HTML from `formatted_base` as fallback. Added effective-window check so expired/future-dated measures are skipped. Added heading-level fallback (`/headings/<4-digit>`) because aluminium structures + furniture and a handful of other commodities only carry MFN measures at the 4-digit parent, not the 10-digit leaf. Live smoke test: 5/5 real HS codes (men's cotton trousers, aluminium structures, smartphones, e-bikes, metal furniture) resolve in <300 ms with rates within 1.5 pp of expectations. +8 fixture-based parser tests pinning the JSON:API contract. 1,268 → 1,276 tests. |
| 2026-05-16 | Sprint E — Wizard exposes optional HS code input | Sprint D made the customs calc reach for live TARIC data when an 8+ digit HS code was supplied; Sprint E lets users actually supply one. New optional field at the bottom of wizard step 1 ("HS code (optional, 8–10 digits)") with a `pattern="[0-9 .]{6,14}"` mask that accepts spaces and dots (the TARIC client's `normaliseHs` strips them). Helper text frames the trade-off: "drop in a precise code for a sub-chapter duty rate from live tariff data, otherwise leave blank — we'll use the chapter average." `readForm()` picks it up automatically via `FormData` since `hsCode` was already in `SHARE_KEYS`. EN/PL/DE wizards all carry the new field. New end-to-end test seeds a synthetic live rate in KV and asserts `composePlan` with a spaces-included HS code (`'6203 42 35'`) routes through `calculateQuoteAsync`, applies the live MFN, surfaces `mfnSource` as the upstream label, and preserves `chapterRatePercent` for the divergence badge. 1,276 → 1,277 tests. |
| 2026-05-16 | Sprint F — TARIC cache warmer + nightly GHA schedule | Sprint D + D.1 + E built the live-rate path end-to-end, but cold KV meant the first user with each new HS code paid the 200–300 ms upstream cost. Sprint F pre-warms the cache nightly. New `lib/intelligence/data/taric-warm-list.js` curates 30 HS×origin combos covering all eight wizard categories plus the AD/CVD hot-spots (bicycles ex-CN, aluminium extrusions, steel fittings). New `runTaricWarm({dryRun, max})` job inside `lib/handlers/cron.js` walks the list, calls `lookupHsRate` with `skipUpstream` first to count already-fresh entries, then refreshes the rest via the normal lookup (which writes to KV). Returns a structured summary `{attempted, hit, miss, cached, unchanged, written, durationMs, details[]}` so a failed upstream surfaces in the log instead of dying silently. Wired into the GHA cron workflow with daily 04:15 UTC schedule + manual `workflow_dispatch` option. New `test/taric-warm.test.js` covers WARM_LIST shape contract, dry-run never writes, fresh-cache counts as `unchanged`, upstream-dead path returns `miss`. 1,277 → 1,287 tests. |
| 2026-05-16 | Sprint F (ops) — first prod warm | Fired `taric-warm` via GHA `workflow_dispatch`. Result: 31/31 fetched from UK Trade Tariff in 14 s end-to-end, 0 misses. Per-entry latencies 295–681 ms (most ~400 ms). Confirmed rates: cotton t-shirts 12%, bicycles ex-CN 14% MFN (AD layered separately), aluminium structures 6%, smartphones 0%, leather sports footwear 8%, glassware 10%, taps + cocks + valves 2%. Sprint D → F now has real production data behind it; future users hitting the warm-list HS codes get a cache HIT on first request. |
| 2026-05-16 | Sprint G — HS-code engagement analytics | Closes the loop on Sprints D → F by measuring whether users actually fill in the new HS code field. `lib/handlers/start.js` now records `hsCodeProvided` (boolean), `hsCodeLength` (0/6/8/10), and `dutyMfnSource` (the actual MFN source used in the calculation — `chapter-estimator` vs the live upstream label) on every `import_plan_generated` event. `lib/events.js` aggregator gains `hsCodeProvided`, `hsCodeProvidedRate` (% of plan events, not all events — so plan_saved doesn't dilute the denominator), and `byDutyMfnSource` top-N. `/dashboard/leads/` renders a fifth tile: "HS code provided · N · X% of plans · drives live TARIC lookup." +3 aggregator tests cover the plan-events-only denominator, missing-field treated as false, and empty-input zeros. 1,287 → 1,290 tests. |
| 2026-05-16 | Sprint H — Open Graph + Twitter Card site-wide | Favicon work (v3 → v7) covered browser tabs + SERP; OG + Twitter cards cover link previews in LinkedIn, WhatsApp, iMessage, Slack, email. Before: sharing any orcatrade.pl URL rendered as a bare link with no image. New `og-1200x630.png` (cropped orca mark on navy background + wordmark + tagline + url, generated from `orcatrade-mark.png` via PIL). Favicon injector v9 now also emits `og:image`, `og:image:width/height/alt`, `og:type`, `og:site_name`, `og:locale`, `twitter:card`, `twitter:image`, `twitter:site`. Per-page `og:title` and `og:description` are preserved unchanged. `og:type` is detected pre-strip and preserved — SEO guides + worked examples keep `og:type="article"` (Google's Article rich-result eligibility); hand-rolled pages default to `og:type="website"`. All five SEO generators (sourcing/routing/customs/warehouse, trade-defence, preferential, compliance, example-plans) updated to point `og:image` at the new file, and `generate-seo-pages.js#run()` now invokes the injector at the end of every regen so re-runs leave the repo in a state that satisfies the OG meta tests. New `test/og-meta.test.js`: OG image file exists in the right size range; 12 hand-rolled landing-surface pages all carry the canonical block; idempotence test (no duplicate og:image on re-inject); marker version contract. 1,290 → 1,305 tests. |
| 2026-05-16 | Sprint J — Founding 10 pilot + homepage scarcity strip | Pricing + Stripe were already wired; what was missing was a concrete asset for founder-led outbound and a way to convert the absence-of-testimonials into a story instead of a gap. New `/founding/` page offers the first ten paying importers lifetime 50% off Growth (€199/mo instead of €399), a founder Slack channel, and homepage placement — in exchange for six months of usage on three real shipments + a 30-min bi-monthly founder call. New `lib/handlers/founding.js`: `GET /api/founding` returns the spots-remaining counter (computed from the KV event log on every request — no separate counter key, no race), `POST /api/founding` validates name + email, records a `founding_applied` event (new ALLOWED_TYPES entry in `lib/events.js`), and emails orca@ via Resend with structured fields. Past the 10th application the handler still accepts submissions but flips `waitlist: true` so the email subject + UI reflect that. New homepage Founding 10 strip sits between hero and worked-examples, gold-accent linear-gradient card, live `spots remaining` number refreshed from `/api/founding` on load, single CTA → `/founding/`. New `test/founding.test.js`: 8 tests covering GET counter shape, POST happy-path with event-log assertion, missing-name 400, invalid-email 400, waitlist flip past 10 (with 10 seeded events), OPTIONS preflight, PUT 405, ALLOWED_TYPES contract. `test/og-meta.test.js` SAMPLE_PAGES gains `founding/index.html` so the canonical OG block is enforced there too. The favicon injector ran post-write so the new page carries the v9 block. 1,305 → 1,314 tests. Path A → B in motion: this is the asset that makes the H1 outbound campaign (next sprint) measurable. |
| 2026-05-16 | Sprint J.1 — Vercel analytics in SEO generator | Commit b004cd97 added `window.va` + `/_vercel/insights/script.js` to hand-rolled pages but never patched the five SEO generators. Every `npm test` run regenerated 561 SEO pages without analytics, silently undoing the analytics rollout — uncommitted drift first surfaced during Sprint J's test run. Rather than patch each generator template separately, extended `scripts/inject-favicon-tags.js` with a second injection phase: `injectAnalytics()` inserts the analytics block before `</body>`, idempotent via a fresh `ANALYTICS_MARKER` comment, strips prior un-markered analytics tags so the b004cd97-era pages get normalised on first re-inject. Refactored `inject()` into `injectHead()` + `injectAnalytics()` for separable idempotence — a page can have one block already and gain the other on the next run. `generate-seo-pages.js#run()` already calls the injector post-regen, so the analytics now survives every future test cycle. `test/og-meta.test.js` gains a parallel "carries the Vercel analytics block" assertion per sample page, including a single-instance check to catch double-injection. Final state: 681 pages re-injected once (with marker added + b004cd97 tags stripped to single canonical form), then 0 changes on second run. 1,314 → 1,327 tests. |
| 2026-05-16 | Sprint J.2 — PL/DE Founding 10 localisation | Sprint J shipped EN-only; PL and DE homepages (which carry the localised wizard from Sprint 33) sent non-English visitors past the offer with no awareness it existed. Added the gold-accent Founding 10 strip section + matching CSS + spots-remaining counter JS to `pl/index.html` and `de/index.html` between hero and worked-examples, in lock-step with the EN homepage. Built `/pl/zalozyciele-10/` and `/de/gruender-10/` as full localised landing pages — translated offer cards, ask list, six-field application form, FAQ — talking to the same locale-agnostic `/api/founding` handler (no backend changes; the offer is identified by email, not locale). Hreflang alternates wired tri-laterally between the three pages plus an `x-default` pointing at EN. `js/site-nav.js` SLUG_OVERRIDES gains `/founding/ → /pl/zalozyciele-10/` and `/founding/ → /de/gruender-10/` so any nav link to `/founding/` on a PL/DE page auto-routes to the localised slug. Favicon injector ran post-write to give both new pages the v9 head block + analytics. `test/og-meta.test.js` SAMPLE_PAGES extended with both new pages — OG canonical block + analytics presence enforced. 1,327 → 1,331 tests. H1 conversion surface now covers all three locales. |
| 2026-05-16 | Sprint J.3 — Founding 10 funnel on leads dashboard | Without a tile on `/dashboard/leads/`, every Founding 10 application would silently hit KV with the user blind to the funnel they're about to drive outbound traffic into. `lib/events.js` `aggregate()` gained `foundingApplied` (total count), `foundingWaitlist` (count past the tenth spot), and `foundingRecent` (newest-first list of 10 with name/company/email/role/monthlyValueEur/waitlist) — pulled off the same event stream the dashboard already reads, no new endpoint needed. Empty-input case returns explicit zeros + empty list so the dashboard doesn't NaN. `/dashboard/leads/app.js` renders a sixth tile ("Founding 10 applied · N / 10 · M spots remaining" with waitlist roll-in past 10) and a new "Recent Founding 10 applications" panel below the events table — hidden until at least one application lands so the dashboard stays clean pre-traffic. Status badge in the table flips green/Founding ↔ amber/Waitlist. +3 events tests: founding aggregation with mixed event types, empty-input zeros, 10-cap on recent. 1,331 → 1,334 tests. |
| 2026-05-16 | Sprint J.4 — Homepage honesty pass | The "60+ vetted manufacturers" line at the top of each homepage mission section was flagged in the original site audit as the weakest claim — asserted without backing, contradicted by the openly-anonymised marketplace shell, and tonally at odds with the "built in public" Founding 10 narrative just shipped in J/J.2. Replaced the claim across all three locales (`index.html`, `pl/index.html`, `de/index.html`) with a single line that's both honest and a CTA: "Built in public with the Founding 10 · 18 calculators · Live TARIC integration · 561 guides across EN, PL, DE · Orders €50k–€500k", with the "Founding 10" phrase linking gold-accent to `/founding/` (or locale-correct slug per Sprint J.2 SLUG_OVERRIDES). The expanded "read more" mission paragraphs were left untouched — they refer to OrcaTrade's *intent* to vet factories per shipment, not to a count claim. Pure HTML/copy change; no test changes, baseline holds at 1,334. The mission section is now a conversion driver into Founding 10 from the most visited piece of homepage real estate. |
| 2026-05-16 | Sprint J.5 — Applicant confirmation email (locale-correct) | Before J.5 the apply flow ended with a UI toast — applicants got no email and Oskar's next-day reply landed cold instead of as the second touch. Two-half sprint: (1) three founding-page frontends now POST `locale: 'en'/'pl'/'de'` alongside the form payload; (2) handler accepts the locale (validates against ALLOWED_LOCALES, defaults to `en` for missing/invalid), stamps it onto the `founding_applied` event, and fires two Resend emails in parallel — the existing internal notification to `orca@orcatrade.pl` (now annotated with locale + waitlist status), plus a new locale-correct confirmation to the applicant with `reply_to: orca@orcatrade.pl` so any reply continues the founder conversation. `APPLICANT_TEMPLATES` per locale: subject + plain-text body, first-name personalisation, waitlist branch when spots 1–10 are filled. Both emails soft-fail independently (no `RESEND_API_KEY` returns 200 with `emailed: false / applicantEmailed: false`; transient Resend errors don't break the user flow or the event log). Refactored `sendEmail` into a single `resendSend()` core + `sendInternalEmail` + `sendApplicantEmail` so the two paths share transport but diverge on content. +4 tests: locale recorded on event, EN-fallback on missing/garbage locale, EN/PL/DE template render shape with first-name + waitlist switch, locale preserved on waitlist flip past spot 10. 1,334 → 1,338 tests. |
| 2026-05-16 | Sprint J.6 — Wizard result → Founding 10 cross-sell | The wizard result is the highest-intent moment on the site — a visitor just spent six steps describing their import. Until J.6 the page surfaced agent CTAs + a share section but no path into Founding 10, so peak-intent traffic landed in the share flow instead of converting. Added a gold-accent cross-sell card between the agent CTAs and the share section, with locale-correct headline + body in EN/PL/DE (`foundingCrossSell*` keys in `start/i18n.js`) and a locale-aware `foundingCrossSellHref` so EN→`/founding/`, PL→`/pl/zalozyciele-10/`, DE→`/de/gruender-10/`. `start/app.js` fetches `/api/founding` on result render — meta line shows "N of 10 spots remaining" or the waitlist variant when full. Card is hidden by default and only revealed once the counter loads (so a slow API doesn't show a flash of broken state) and stays hidden if the visitor dismissed it before (localStorage `orcatrade-founding-dismissed`). `wizard.css` matches the homepage strip's linear-gradient gold treatment and adds a `@media print { display: none }` rule so the CTA doesn't show in saved-as-PDF plans. +1 test in `start-i18n.test.js` enforcing all eight cross-sell keys exist in all three locale blocks plus the three locale-correct href patterns. The existing locale-parity test already protects the keys against future drift. 1,338 → 1,339 tests. |
| 2026-05-16 | Sprint J.7 — Founding pages in master sitemap with hreflang | Without sitemap entries the three founding pages aren't crawled on Google's regular cadence and don't get grouped as one logical surface across markets — meaning organic searches for "founding importer pilot" or German/Polish equivalents wouldn't reach them via SERP. Added the three pages to `scripts/generate-seo-pages.js#generateMasterSitemap` `sitePages` array at priority 0.9 / changefreq weekly, each carrying a tri-lateral hreflang block (en/pl/de + x-default → EN canonical). Regenerated: `sitemap.xml` URL count rose from prior baseline to 601, with 12 `xhtml:link rel="alternate"` entries for the founding cluster (3 entries × 4 hreflang each). +1 test in `generate-seo-pages.test.js` asserting all three `<loc>` entries plus the EN/PL/DE alternate hrefs appear at least 3× each. 1,339 → 1,340 tests. With this, the founding pages are now discoverable both ways: direct CTA from any home/result surface, AND organic via SERP. |
| 2026-05-16 | Sprint K — Marketplace honesty pass tied to Founding 10 | The marketplace page had been flagged in the original site audit as the second-weakest claim after "60+ vetted manufacturers" (fixed in J.4). It openly admitted being anonymised exemplars but never explained *when* the live directory ships, and didn't cross-link into the new Founding 10 positioning — so visitors hitting `/founding/` and then `/marketplace/` got inconsistent stories about how the network was being built. Re-wrote the anon-banner across all three locales (`marketplace/index.html`, `pl/marketplace/index.html`, `de/marketplace/index.html`) to surface both halves of the question: WHY anonymised (vetting + consent pipeline, same as before) AND WHEN it goes live (after we've onboarded the first Founding 10 importers and run real sourcing engagements with them — so the directory ships against actual buyer demand, not guessed personas). Each banner now carries a gold-accent anchor into the locale-correct Founding 10 page (`/founding/`, `/pl/zalozyciele-10/`, `/de/gruender-10/`). +4 tests in `test/marketplace.test.js`: per-locale assertion that the page contains the right Founding 10 href + visible phrase, plus an EN-specific assertion that both halves of the explanation (WHY + WHEN) are present. The two H1 positioning surfaces are now in lock-step — buyers see one coherent story whether they land on the home page, the founding page, or the marketplace. 1,340 → 1,344 tests. |
| 2026-05-16 | Sprint BG-0a — customs dedupe + effectiveLandedCostEur | First backend-grade sprint shipped alongside docs/backend-grade-plan.md. Eliminated ~70 lines of duplication between calculateQuote (sync) and calculateQuoteAsync (TARIC-aware) via a new composeQuoteResult helper; BONDED_EDUCATION + NEXT_STEPS_STATIC + parseInput extracted as shared constants/helpers. Added finance-grade effectiveLandedCostEur (customs + duty + brokerage + ENS — P&L cost net of recoverable VAT) and vatRecoverableEur on standard + bonded scenarios. +4 tests including a sync/async shape-parity check that pins the dedupe by failing if a future patch makes one path emit different keys than the other. 1,344 → 1,348 tests. |
| 2026-05-16 | Sprint BG-0b — surface P&L cost end-to-end | lib/handlers/start.js totals gain effectiveLandedTotal + vatRecoverableEur; every originSensitivity.matrix row gains effectiveLandedTotal + annualEffectiveLanded so cross-origin comparisons reflect P&L cost too, not just gross cash. New statLandedEffective i18n key in EN/PL/DE ("P&L cost (VAT recoverable)" / "Koszt w P&L (VAT odzyskiwalny)" / "GuV-Kosten (EUSt rückforderbar)"). Sub-stat surfaced under the Landed cost tile in the wizard result. +1 i18n parity test, integration test extended to assert the math (perShipment - vat = effective) and walk the origin matrix. Why this matters: a €100k DE shipment at 12% duty + 19% VAT shows €119k landed (cash) vs €100k P&L (after VAT recovery); the €19k delta is the single biggest source of confusion in importer conversations. 1,348 → 1,349 tests. |
| 2026-05-16 | Sprint BG-1 phase 1 — money.js integer-cents primitives | Foundation for Track 1 of backend-grade-plan.md. New lib/intelligence/money.js with fromEuro / toEuro (avoids the 0.1 * 100 = 10.000000000000002 float trap via toFixed-string routing), add / sub / sum / mulInt / divInt / mulRate / pct, half-even rounding (banker's, EU customs-grade — Math.round(0.5)=1 vs halfEvenRound(0.5)=0), MAX_SAFE_CENTS guard. 25 new tests pinning: 0.1 + 0.2 = 0.3 exact, banker's vs naive asymmetry, customs compounding at €100k and €5M scale with zero drift, Finland 25.5% VAT on irregular base, type-guard throws on bad inputs. No calculator migrated yet — phase 2. 1,349 → 1,374 tests. |
| 2026-05-17 | Sprint BG-1 phase 2 — customs-quote migrated to integer cents | First calculator now lives on money.js. lib/intelligence/customs-quote.js: calculateStandardClearance, calculateBondedWarehouse, and composeQuoteResult all do compound arithmetic in integer cents internally; floats reappear only at the result-object boundary via M.toEuro(). Half-even rounding now happens once at the boundary instead of float drift accumulating across multi-step compounding (customs × duty → vatBase × VAT rate → landed-cost sum). +2 precision tests on a €4.5M shipment compound and a bonded-ops component sum that assert cent-level exactness. Migration was behavior-preserving for every value the test corpus already pinned — all pre-existing assertions pass unchanged. Track 1 of backend-grade-plan.md gains its first consumer. 1,374 → 1,376 tests. |
| 2026-05-17 | Sprint BG-0c — plan-diff tracks effectiveLandedTotal | Closes the P&L cost feature loop. lib/plan-diff.js SNAPSHOT_FIELDS gains effectiveLandedTotal; extractSnapshot captures it from totals; diffSnapshots emits new effectiveLandedDeltaEur + effectiveLandedDeltaPct fields when both sides recorded the value. Legacy snapshots (pre-BG-0c) safely return null for the delta — no breakage on rolling forward. +5 plan-diff tests covering snapshot extraction with + without the field, both-sides-present delta math, legacy null-handling, and SNAPSHOT_FIELDS round-trip via sanitiseSnapshot. Why it matters: a returning user no longer just sees "your landed cost moved €X" — they see "your P&L cost moved €Y" too, which is the number their finance team actually compares against budget. 1,376 → 1,381 tests. |
| 2026-05-17 | Sprint BG-4.1 — structured logging foundation | First implementation of Track 4 of backend-grade-plan.md (observability). New lib/log.js with four levels gated by ORCATRADE_LOG_LEVEL, JSON-per-line output via the matching console method (so log aggregators preserve severity), withContext({ handler, action }) bindings, automatic PII redaction (email/token/secret/apiKey/cookie/authorization → first-2-chars + ***), recursive walk into nested objects + arrays, Error instance serialisation, 12-hex-char generateRequestId. api/[...path].js gains request-id middleware: honours x-request-id if shaped right, else mints one; attaches to req.requestId; echoes on response header; included in router-level error log so a thrown handler is greppable by id. Three highest-leverage handlers migrated to structured logging: auth (security-sensitive), founding (active funnel), start (highest traffic) — every console.* call gone. 18 log.js tests cover level filtering, JSON output shape, PII redaction (incl. case-insensitive + nested + arrays), Error serialisation, context chaining, request-id generation. Unlocks Track 4.2 (Sentry/Axiom drain) since every log line is now structured. 1,381 → 1,399 tests. |
| 2026-05-17 | Sprint BG-4.3 — /api/health operational probe | Second piece of Track 4. New lib/handlers/health.js with GET /api/health returning structured status for kv (round-trip probe + latency), taric (read kv timestamp from nightly cron warmer, degrade > 25h), resend / stripe / anthropic (env-var presence; anthropic accepts ANTHROPIC_API_KEY or legacy ORCATRADE_OS_API). Aggregation: kv-down → status "down" + HTTP 503 (uptime probe pages); any subsystem degraded → "degraded" + HTTP 200; all-ok → "ok". Cache-Control: no-store. Payload carries requestId, ts, and version (Vercel commit SHA short). lib/handlers/cron.js taric-warm now stamps `taric:warm:lastRun` after a non-dry-run with ≥1 success, 7-day TTL. 17 tests cover the aggregate matrix, every probe branch (incl. invalid-timestamp + never-warmed), the anthropic-OR-legacy env fallback, 405 on non-GET, and the end-to-end 200/degraded paths. Test helper async-env bug caught + fixed (sync finally restored env vars before awaited code ran). 1,399 → 1,416 tests. |
| 2026-05-17 | Sprint BG-4.4 — circuit breakers around upstreams | Third piece of Track 4. New lib/circuit.js: three-state breaker per upstream (closed → open after threshold consecutive failures → half-open after cooldown → closed on probe success). State in KV at `circuit:<name>`, 24h TTL, auto-heals via half-open probe. API: `circuit.run(name, asyncFn, { fallback, threshold?, cooldownMs? })` with required fallback (no escape hatch). Resend wrapped in both lib/handlers/start.js (wizard summary email) and lib/handlers/auth.js (magic-link send) under shared 'resend' breaker name. /api/health output now overlays circuit state onto the resend subsystem — env present but circuit open → degraded with `reason: "circuit open"`. 15 circuit unit tests + 2 health integration tests. Defaults: 5 consecutive failures opens, 30s cooldown to half-open. Track 4.4 of backend-grade-plan.md complete. 1,416 → 1,433 tests. |
| 2026-05-17 | Sprint BG-4 closeout — public /status/ + uptime workflow | New status/index.html: polls /api/health every 30s, renders subsystem cards with status pills + traffic-light overall banner + circuit-state details + a fallback banner if the fetch itself fails. noindex (operational page, not content). New .github/workflows/uptime.yml fires every 5 min, calls /api/health from GHA (using free 2,000 min/mo quota), fails the run on 503 (paging condition), surfaces ::warning:: annotations for degraded subsystems on 200. 8 markup-contract tests pin the page (noindex, fetch target, DOM ids, legend, subsystem labels, refresh cadence, contact link). Closes Track 4 of backend-grade-plan.md: structured logs (4.1) → health probe (4.3) → circuit breakers (4.4) → visible UI + monitor. 1,433 → 1,441 tests. |
| 2026-05-17 | Sprint BG-5.1 — GDPR data subject endpoints | First piece of Track 5 (compliance + enterprise readiness). lib/handlers/account.js dispatches GET /api/account/export (Article 20 — portability) and POST /api/account/delete (Article 17 — erasure). Export returns a JSON file (Content-Disposition: attachment) with format:"orcatrade-gdpr-export-v1", carrying saved plans + matching events + session metadata + a notes block. Delete requires { confirm: true } in the body, hard-deletes plans, pseudonymises events (email → `deleted-<sha256-prefix>@anonymised.local`, free-text fields scrubbed, pseudonymised:true marker added), clears session cookie, writes a no-PII audit log line with the email hash. Both 401 without a valid session. The same user always pseudonymises to the same identity (deterministic emailHash, case-insensitive). 14 tests cover auth gating, unknown sub-action, emailHash properties, export shape (filtering by email), Cache-Control + Content-Disposition headers, empty-inbox export, delete confirmation guard, delete pseudonymisation end-to-end (own plans gone, own events pseudonymised, other user untouched, cookie cleared), empty-user delete, OPTIONS preflight, method gating. Enterprise procurement: Article 17 + 20 questions are now answered with "yes, wired and tested." 1,441 → 1,455 tests. |
| 2026-05-17 | Sprint BG-5.1 closeout — /account/privacy/ UI | New account/privacy/index.html: two cards (Export your data · Delete your account) wired to the BG-5.1 endpoints. Export card calls /api/account/export, triggers a browser file-download via Blob + ObjectURL with the right filename pattern. Delete card requires typed-email confirmation (typing your own email enables the button, browser confirm() dialog on click) before POSTing { confirm: true } to /api/account/delete; surfaces deletion counts on success and redirects to / after 3 s. Bootstraps from /api/auth/me — shows an "auth needed" panel and a Sign in link when no session exists. /account/ index gets a new quick-link card pointing to /account/privacy/. 9 markup-contract tests pin the page (noindex, both endpoint references, JSON.stringify({confirm:true}) shape, /api/auth/me gate, typed-email confirmation, Articles 17 + 20 cited, contact link, /account/ → privacy link). Closes Track 5.1: the data subject rights now have a self-service path, not just a JSON contract. 1,455 → 1,464 tests. |
| 2026-05-17 | Sprint BG-5.4 — security documentation set | Track 5 (compliance + enterprise readiness) gains its written-policy layer. New docs/security/ folder with five enterprise-grade docs + README index: data-flow.md (data inventory + retention + GDPR right map), subprocessors.md (third-party DPA register with regions + transfer mechanisms + cookies-by-provider), dpa-template.md (Article 28 DPA with Annex A technical/organisational measures), incident-response.md (4-class severity model + runbooks + honest section on current gaps), soc2-readiness.md (TSC ✅/🟡/❌ gap analysis with headline gaps-to-close before Type I in 2026-Q4). Each doc dated + signed by the founder. 10 contract tests pin: existence of every required file, non-stub content (no TODO/FIXME/lorem), Last-reviewed dates, README indexes everything, cross-doc links resolve, subprocessor coverage, GDPR Art 28 essentials, SEV class coverage, TSC coverage with the right symbols, Article 15/17/20 endpoint map. Procurement security questionnaires now have real artefacts to point at — Tier 1/2/3 share-tier guidance baked into the README. 1,464 → 1,474 tests. |
| 2026-05-17 | Sprint BG-5.2 — cookie consent banner v2 (granular, consent-gated analytics) | Track 5 customer-facing layer. New js/cookie-consent.js with two-category granular consent (essential always on; analytics opt-in, default off), tri-locale (EN/PL/DE) banner, version-stamped localStorage persistence, tamper-resistant (force-rewrites essential=true on read). Vercel Analytics no longer loads unconditionally: scripts/inject-favicon-tags.js now ships only the va() stub + the consent loader, and the actual `/_vercel/insights/script.js` is appended dynamically only after the user grants analytics consent. Marker bumped to "analytics v2 (consent-gated)" and 685 pages re-injected (legacy v1 marker auto-stripped). Public API: window.orcatradeConsent.{get,has,set,open}; `[data-cookie-preferences]` data-attribute on any element re-opens the banner. 10 new tests cover module-source contract (locales, API surface, versioned storage, anti-tamper, footer hook, gated Vercel install), favicon-injector contract (loads consent module not Vercel directly), live module behaviour via node:vm with stubbed DOM. test/og-meta.test.js assertion updated to enforce the new shape across all sample pages. Security docs (data-flow.md, subprocessors.md, soc2-readiness.md) updated to mark Track 5.2 as shipped — P2 Privacy criterion moves 🟡 → ✅. 1,474 → 1,484 tests. |
| 2026-05-17 | Sprint BG-6.1 — prompt registry foundation | First piece of Track 6 (AI quality moat). New lib/ai/prompts/registry.js with versioned + immutable-once-shipped prompts. API: getPrompt(agent, version) with deploy-grade throw on missing, listVersions / latestVersion / getLatest convenience, hashPrompt for SHA-256-first-12-hex eval-log correlation, path-traversal guards on agent + version params. Storage: one folder per agent, one v<n>.txt file per version, plain text (no JS templating). lib/handlers/orchestrator.js migrated — its previously inline SYSTEM_PROMPT moved to lib/ai/prompts/orchestrator/v1.txt; handler reads via registry.getPrompt() and exports ORCHESTRATOR_PROMPT_VERSION + SYSTEM_PROMPT_HASH for the future eval harness to assert against. 15 tests pin every contract: empty for unknown agent, sorted versions, latest = highest, throw on unknown agent, path-traversal guards (uppercase / slashes / dot-dot), invalid version format (no v prefix / .txt suffix / "latest" alias), content + line-ending normalisation, cache identity, throw on missing version (deploy-bug), getLatest convenience, hashPrompt determinism + format, content tripwire (Operations Orchestrator + ABSOLUTE RULES + VERDICT + requestHumanReview), agent-dir invariant. Track 6.2 (eval harness) can now A/B old vs new prompt versions without code surgery. 1,484 → 1,499 tests. |
| 2026-05-17 | Sprint BG-6.2 phase 1 — offline AI eval harness | Track 6 second piece. New lib/ai/evals/scorer.js: pure-function scorer that runs in <50ms with zero API calls, validates response text against per-case mustContain / mustNotContain pattern lists (regex shorthand `/foo/i` or substring). validateAll() walks every cases.v1.json and asserts shape + cross-checks every case's promptVersion against the BG-6.1 registry — a case referencing an unshipped version FAILS CI. lib/ai/evals/orchestrator/cases.v1.json seeded with 6 canonical cases pinning expensive failure modes: CN bicycles AD/CVD stacking, VN apparel EVFTA preferential, BD textiles EBA zero-duty, CN aluminium CBAM applicability, large-cargo human-review escalation, out-of-scope MDR/IVDR escalation. Each case targets a specific prompt version + describes WHY it exists. The pre-existing scripts/agent-eval.js (compliance, LLM-integration) remains; this layer runs on every `npm test` without API credits. 19 tests cover load/listAgents/path-traversal/parsePattern (regex+substring+non-string)/score (all variants)/validateAll/orchestrator-cases-shape/prompt-version-cross-check/golden-path synthetic. 1,499 → 1,518 tests. |
| 2026-05-17 | Sprint BG-6 foundation closeout — all 5 agents on the prompt registry + eval coverage | Track 6 foundation finished. Programmatic extraction: compliance/sourcing/logistics/finance SYSTEM_PROMPTs moved from inline JS const declarations to lib/ai/prompts/<agent>/v1.txt; each handler now reads via registry.getPrompt() and exports its versioned constant + SYSTEM_PROMPT_HASH for eval correlation. Eval coverage added: sourcing (country-comparison apparel CN/VN/BD, out-of-scope factory recommendation, VN EVFTA preferential), logistics (urgent-air mode-selection, bonded re-export use case), finance (first-time CN supplier confirmed-LC, working-capital CCC framing). New handler-integration test asserts every one of orchestrator/compliance/sourcing/logistics/finance reads via the registry, exports its version + hash, and has ZERO inline-prompt drift — drift fails CI immediately. validateAll() now cross-checks 5 agents (not 1) against the registry on every npm test. From "1 of 5 on registry" → "5 of 5 on registry with eval coverage" in one focused sprint. Track 6.2 phase 2 (live LLM runner reading from new tree) becomes a mechanical follow-up. 1,519 → 1,520 tests (modest count delta because the migration is mechanical; the win is breadth — 4 agents on the moat infrastructure). |
| 2026-05-18 | Sprint nav-account-link — Global "Sign in" link in site nav | Trivial-but-important UX gap. Until today the only way to reach /account/ from the public site was typing the URL. Added "Sign in" to NAV.secondary in js/site-nav.js so every page (homepage, marketing, agents, dashboards, examples, guides) shows it in the top-right. Works for both signed-in and signed-out states because /account/ already renders the right view based on the auth cookie — no JS gating needed. Locale-aware translations (PL: Zaloguj, DE: Anmelden); localizeHref short-circuits /account/ so the URL stays /account/ across all locales (same /agent/ + /dashboard/ pattern). Zero new tests; existing nav tests still pass because they don't pin the NAV.secondary contents. The site nav now properly bridges visitor → user. |
| 2026-05-21 | Sprint sso-oidc-v1 (phase 3) — self-serve SSO: owner config + email-domain discovery | Makes SSO usable without operator hand-holding: an org owner configures their own IdP, and end users get a "Sign in with your company SSO" box that routes them by email domain. Completes the SSO capability end-to-end. **Domain index** in lib/sso-config.js: setConfig now reconciles a sso:domain:<domain> → orgId index from the config's allowedDomains (adds new, drops this org's stale ones), deleteConfig clears it, and findOrgByDomain(email) resolves a work email to its org. The reconcile only removes an entry that still points at THIS org — a test pins that org B re-claiming acme.test, then org A deleting its config, leaves B's claim intact (no cross-org wipe). **Owner-gated config endpoints** on the orgs handler: GET/POST/DELETE /api/orgs/<id>/sso, gated to the org OWNER specifically (not admins) via requireOwner — SSO config holds the client secret + governs who can sign in as the org, so it's the highest-trust org operation. GET masks the secret (returns clientSecretSet:true, never the value — a test greps the response to confirm 'shh' never appears); POST validates completeness (400 on incomplete); both emit org_sso_configured / org_sso_removed audit rows (actor + orgId, never the secret). Routed before the POST-only sub-action guard so GET+DELETE work. **Discovery endpoint** GET /api/auth/sso/discover?email= (public, rate-limited 30/min): resolves the email's domain to its org and returns ONLY the initiate URL (no config, no secret); unknown domain → ssoAvailable:false so the UI falls back to password/magic-link. **UI**: /account/ sign-in gains a "Sign in with your company SSO" link (tri-locale via auth-i18n: EN/PL/DE) that takes the typed email → discover → redirect to the org's IdP (or a friendly "no SSO for that domain" message). New /account/orgs/sso/ owner page: a config form (issuer, clientId, clientSecret, authz/token/jwks endpoints, optional allowedDomains) that loads current config (secret masked), shows the redirect URI to register at the IdP, saves, and surfaces the team's initiate URL on success + a Remove button; 401/403/404 → an owner-access gate. 13 new tests: domain index (write/resolve, reconcile drops-stale/adds-new, delete clears, no-cross-org-wipe), owner gating (403 non-owner, 401 anon), POST-then-GET-without-secret (+ no-'shh'-in-body), incomplete→400, DELETE + both audit rows, discover (resolves configured domain → initiate URL, unknown → no-leak), ALLOWED_TYPES, and UI contracts (/account/ SSO entry + 3-locale string, owner config page form + secret-mask + DELETE). 2,694 → 2,707 tests. SSO is now fully self-serve: an org owner sets up their IdP at /account/orgs/sso/?org=<id>, and their team signs in by typing their work email on /account/ — no operator involvement, no shared secrets. The last enterprise-gate capability on the backend roadmap is complete. |
| 2026-05-22 | Sprint hs-suggest-expand-v1 — broaden the plain-language HS lookup to ~40 more everyday SME imports | Pure intelligence-breadth follow-on to hs-suggest-v1 / hs-refine-v1: more importers can now describe their product and land on a 6-digit code (which routes them onto the live-TARIC duty path instead of the coarse chapter average). Expanded lib/intelligence/data/hs-suggest.js HS_ENTRIES from 63 → 103 curated headings. **Accuracy-safe by construction**: every new HS6 was restricted to a chapter ALREADY present in customs-quote.js HS_CHAPTER_DUTY, so each suggestion is guaranteed to resolve to a priceable chapter (no orphan codes that would dead-end the duty calc) and the core duty math is untouched. New coverage spans the everyday SME import long-tail: coffee/tea/spices (09), confectionery/chocolate/biscuits (17/18/19), food supplements & protein powder (210690), beer/wine/spirits (22), bags/wallets/belts/pet-collar (42), sunglasses & reading glasses (90), watches (910219), rugs/curtains/mattress/pillow/mirror (57/63/94/70), fitness equipment & dumbbells (950691), notebooks/pens/thermos/diapers (48/96), LED bulbs/cables/microphones/cookers (85), kitchen knives/hand tools/drills/aluminium cookware (82/84/76), socks/swimwear/scarf (61/62), candles (340600), and car parts (870829). The scorer (tokenise/scoreEntry/suggest), the endpoint, the gate, and the wizard wiring are all unchanged — this is data only. 3 new tests on top of the existing 18: dataset count ≥103, every suggested chapter has a duty-table fallback rate (no orphan codes), and 20 new common-import top-hit probes (coffee→090121, wine→220421, beer→220300, handbag→420221, wallet→420231, sunglasses→900410, watch→910219, mattress→940421, curtains→630392, led bulb→853950, usb cable→854442, protein powder→210690, scarf→621430, candle→340600, dumbbells→950691, car parts→870829, kitchen knife→821192, diapers→961900, thermos→961700, notebook→482010). 2,709 → 2,712 tests. The plain-language lookup now covers the goods a typical European SME actually ships from Asia, so more wizard users skip the chapter estimate and get a live-tariff rate — deepening the same moat line without touching the duty math. |
| 2026-05-21 | Sprint sso-oidc-v1 (phase 2) — enterprise SSO endpoints (initiate + callback) | Wires the proven OIDC core (phase 1) into the live auth path: an org's users can now sign in via their corporate IdP. Built carefully on the security foundation, fail-closed throughout. New lib/sso-config.js — per-org config (sso:config:<orgId>: issuer, clientId, clientSecret, authorization/token/jwks endpoints, optional allowedDomains; sanitised on write, dropping unknown fields + lower-casing domains; isComplete gate) + flow-state store (sso:flow:<state>: { orgId, nonce, codeVerifier }, 10-min TTL, SINGLE-USE — consumed/deleted on callback so a captured state can't be replayed). Two endpoints on the auth dispatcher: GET /api/auth/sso/initiate?org=<id> (load config → mint state/nonce/PKCE → stash the flow → 302 to the IdP authorization endpoint; 400 no-org, 404 no-config) and GET /api/auth/sso/callback?code=&state= (consume the single-use flow → exchange the code at the token endpoint with the PKCE verifier → fetch the JWKS → oidc.verifyIdToken against it → optional domain allowlist → mint our email-keyed session, JIT). Every failure path 302s to /account/?sso_error=<reason> and mints NO session — fail closed. The token-exchange + JWKS network calls are isolated into small helpers (exchangeCodeForTokens, fetchJwks) so tests stub global.fetch. auth_signin audit row gains source:'sso' + orgId. 12 new tests (on top of phase 1's 18 adversarial): config store (incomplete rejected, sanitise drops unknowns + lowercases domains, emailDomainAllowed open-vs-enforced), single-use flow, initiate (400/404/302-with-PKCE), and the callback against a stubbed IdP serving a REAL RSA-signed token — happy path (verifies + mints a session carrying the IdP-asserted email), missing-params + unknown-state → error redirect, a wrong-audience token → token-bad-audience + NO cookie (fail closed), domain-allowlist blocks an out-of-domain email, token-exchange failure fails closed, and a replayed (already-consumed) state rejected. The test had to thread the per-flow nonce from the initiate redirect into the minted token — exactly mirroring how a real IdP echoes the nonce, which doubles as a check that the nonce replay-protection is wired end-to-end. 2,682 → 2,694 tests. Enterprise SSO is now functional end-to-end against any compliant OIDC IdP (Okta, Azure AD, Google Workspace, Auth0): configure the org's IdP, point users at /api/auth/sso/initiate?org=<id>, and they sign in with their corporate identity. (Phase 3 — an admin UI to set sso:config + a "Sign in with SSO" entry point — is the remaining follow-on; config is currently set via ssoConfig.setConfig.) |
| 2026-05-21 | Sprint portfolio-fx-display-v1 — display-currency toggle on the portfolio result | Parity + real utility: the single-product wizard result already lets you view figures in USD/CNY/etc. at snapshot rates, but the portfolio result was EUR-only — a gap for a UK-registered importer thinking in GBP or anyone whose supplier quotes in USD. Now the portfolio result carries an EUR/USD/GBP/PLN toggle that re-displays every figure (stat tiles, per-SKU table, totals, drift callout) in the chosen currency. Display-only: all duty/VAT/freight genuinely clear in EUR for EU-bound goods, so the conversion is a view convenience at indicative snapshot rates (a "converted from EUR at snapshot rate" note shows for non-EUR), mirroring the wizard's discipline. Implementation in portfolio/app.js: a small FX table (EUR/USD/GBP/PLN rates), a display-currency state persisted to localStorage (orcatrade.portfolio.displayCurrency) + restored on load, and the existing money formatter made currency-aware (multiplies the EUR amount by the active rate — kept the fmtEur name so every call site converts automatically). renderResult stashes the last response (lastResultData) so a currency switch re-renders the SAME data with no recompute (and skips the scroll-into-view on re-render so toggling doesn't jump the page). **The CSV export stays canonical EUR** regardless of display currency — a test guards that the /api/portfolio/csv request never carries a display currency, so the filed record is always the real EUR figure, not an indicative conversion. 6 new tests: FX table incl. all four currencies, the formatter converts via rate, localStorage persist+restore, toggle markup + re-render-with-skipScroll wiring, CSV-stays-EUR guard, and the toggle CSS (active state + indicative note). 2,658 → 2,664 tests. A non-EUR importer can now read their whole-catalogue landed cost in their home currency at a glance, while the exportable record stays the canonical EUR. |
| 2026-05-21 | Sprint portfolio-csv-v1 — portfolio CSV export (the procurement filing format) | Closes a real operations gap: single plans have print/PDF, but a multi-SKU portfolio — exactly the table a procurement manager files internally or sends to finance — had no export. (Verified first that /account/plans/ already enriches + renders single-plan drift via plan-diff.enrichRecord, so that parity was already done — no rebuild.) New pure lib/portfolio-csv.js: escapeCsvField (RFC 4180 — quote on comma/quote/CR-LF, double embedded quotes; mirrors the leads/audit export discipline) + portfolioToCsv(aggregate, lines) emitting a header row, one row per SKU (category, origin, destination, hs_code, customs value, duty €, duty %, vat, brokerage, transport, landed total), a TOTAL row (carrying the blended duty rate in the duty_pct column), and summary rows (blended_duty_rate_pct, consolidation_saving_eur, sku_count). CRLF line endings + trailing newline per spec. New POST /api/portfolio/csv (registered in the dispatcher): recomputes the posted { lines } via the shared composeAndAggregate (so the CSV always matches what the on-screen plan shows — never a stale client copy), serialises, and streams text/csv; charset=utf-8 with Content-Disposition: attachment; filename="orcatrade-portfolio-<iso>.csv" + Cache-Control: no-store. Public + anonymous like the compute path, rate-limited 12/min/IP; 400 on missing/empty lines, 405 on non-POST. The recompute-on-download is a deliberate user action so the extra fan-out is fine, and it keeps the escaping server-side (safe) + the serialiser unit-testable. /portfolio/ result gains a "Download CSV" button (for everyone, signed-in or not) that POSTs the remembered lines, reads the Blob, and triggers a hidden-anchor download with the ISO-stamped filename. 9 new tests: escapeCsvField (null/empty/plain + comma/quote/newline quoting), portfolioToCsv (header + per-SKU rows + TOTAL with blended rate + summary rows, comma-bearing category escaped), csvFilename pattern, endpoint (200 text/csv + attachment disposition + header-row + TOTAL-row body, 400 empty, 405 non-POST), UI contract (Download CSV button → /api/portfolio/csv + Blob download). 2,649 → 2,658 tests. Procurement can now file the whole-catalogue landed-cost breakdown in one click — the operational-completeness piece that makes the portfolio a real working document, not just a screen. |
| 2026-05-21 | Sprint orchestrator-plan-drift-v1 — getMySavedPlanDrift orchestrator tool (drift parity) | Completes the AI assistant's drift coverage to BOTH saved object types: it could already drift a saved portfolio (orchestrator-drift-tool-v1); now it can drift a saved single plan too. New read-only tool getMySavedPlanDrift(planId) in lib/handlers/orchestrator-personal.js, the single-plan twin of getMyPortfolioDrift: ownership-checked via savedPlans.getPlan(id, <closured email>) (a test confirms another user's id → "not found"), recomputes the plan against today's data via startHandler.composePlan (lazy-required to avoid a load-time cycle), extracts the current snapshot with planDiff.extractSnapshot, and returns { label, route, savedAt, current:{landedEur}, drift } where drift is the existing planDiff.diffSnapshots result (landedDeltaEur/Pct, daysSinceSaved, primaryDriver, significant ≥5%, per-component breakdown) or null when the saved plan had no baseline snapshot. Reuses the EXACT same plan-diff machinery the plan-revision cron + /account/plans/ already use, so the AI's drift answer matches the email + UI. listMySavedPlans' summary now carries the plan id so the model can take it from the list and feed the drift tool — the same two-step flow the portfolio tools use, guided by the tool descriptions (no prompt change; PERSONAL_TOOL_NAMES + classifyTool('account') + buildToolset auto-pick it up). 4 net new tests (file 16→20): four-tool shape with getMySavedPlanDrift's required planId, id-in-plan-summary, drift recompute for owner (significant-up via stale baseline), drift-null-without-snapshot, error branches (missing id / unknown / another-user's-plan→not-found). 2,645 → 2,649 tests. The orchestrator now reasons over a logged-in importer's ENTIRE saved footprint with live drift on either object type — "across everything I've saved, what's gone up most since I saved it?" is now answerable end-to-end. |
| 2026-05-21 | Sprint account-overview-v1 — operations cockpit on /account/ | Turns the signed-in /account/ home from a bare links page into an at-a-glance "operations cockpit" that surfaces the importer's whole footprint — saved single plans + multi-SKU portfolios — without any live recompute, so it loads instantly. (Verification note: before building, confirmed the supplier directory / marketplace path is the deliberately-deferred Horizon-4 shell — supplier-exemplars.js is anonymised, no /sourcing/ UI — and the dev plan's Horizon 6 explicitly says don't build the marketplace until ≥10 paying customers, so this sprint stays on the non-deferred side.) New GET /api/account/overview (lib/handlers/account.js, getCurrentUserStrict-gated like the rest of /api/account/*): runs listPlans + listPortfolios + onboarding.getProgress in parallel and returns { plans:{count, recent[≤3]}, portfolios:{count, recent[≤3]}, onboarding:{completed,total,allDone} }. Recent items are trimmed to display fields (plan: label/route/landedEur/savedAt; portfolio: label/skuCount/landedEur/savedAt) — no PII, no full inputs (a test greps the response to confirm the email never echoes). /account/app.js gains loadOverview() (called inside the /api/auth/me success branch, so signed-out visitors never trigger a 401) + renderOverview(): a cockpit card with two stat tiles (saved-plan count, portfolio count) and up-to-three recent items of each, deep-linked (plans → /account/plans/, each portfolio → /portfolio/?id= which recomputes-with-drift via the refresh path from portfolio-drift-v1) plus "all plans / all portfolios →" CTAs. The card stays hidden when the user has nothing saved (the onboarding checklist carries the page for brand-new users — no empty cockpit). 6 new tests: endpoint (401 unauthed, empty→zero counts, counts + recent for plans AND portfolios with cross-user isolation, recent capped at 3, no-email-in-response PII grep), UI contract (overview-card slot hidden-by-default + CSS + loadOverview/renderOverview wired + loaded in the post-auth branch). 2,639 → 2,645 tests. The /account/ home now answers "what have I got on the platform and what's it costing me" the moment a returning importer lands — the cockpit that ties together this session's plans + portfolios + monitoring work into one operations home. |
| 2026-05-21 | Sprint orchestrator-drift-tool-v1 — getMyPortfolioDrift orchestrator tool | Follow-on to orchestrator-personal-v1: connects the portfolio-drift-v1 comparison to the AI assistant so the orchestrator can answer "has my cost gone up since I saved this?" New read-only tool getMyPortfolioDrift(portfolioId) added to lib/handlers/orchestrator-personal.js: ownership-checked via savedPortfolios.getPortfolio(id, <closured email>) (the model can only drift the CALLER's own portfolios — a test confirms another user's id returns "not found"), recomputes the stored lines against today's data via the shared portfolioHandler.composeAndAggregate (lazy-required to avoid any load-time cycle), and returns { label, savedAt, current:{landedEur, blendedDutyRatePct}, drift } where drift is the comparePortfolioSnapshots result (€/% move, direction, material flag) or null when the saved portfolio had no baseline snapshot. listMyPortfolios' summary now includes the id so the model can take it from the list and feed it to the drift tool (the natural two-step tool-use flow, guided by the tool descriptions). PERSONAL_TOOL_NAMES + classifyTool('account') + buildToolset auto-pick up the new tool (derived from personalTools), so no orchestrator-handler change was needed. 4 net new tests (3→16 in the file): three-tools shape with the drift tool's required portfolioId, id-in-summary, drift recompute for owner (material-up via stale baseline), drift-null-without-snapshot, and error branches (missing id / unknown / another-user's-portfolio→not-found). 2,635 → 2,639 tests. The orchestrator can now do the full loop for a logged-in importer: list your portfolios → pick one → tell you exactly how its landed cost has moved since you saved it, reasoning over real recomputed numbers. |
| 2026-05-21 | Sprint orchestrator-personal-v1 — the Operations Orchestrator can now reason over YOUR saved plans + portfolios | Closes the AI-layer gap both surveys flagged: the orchestrator (and the 4 domain agents) answered only generic questions — they had no awareness of the signed-in user's own saved items, so "which of my portfolios is most exposed to the new CBAM phase?" was unanswerable. This adds two read-only personal tools to the orchestrator's tool-use loop, merged in PER-REQUEST only when the caller has a valid session (the orchestrator is already Growth-tier gated, so the caller is authed). The deterministic-numbers rule holds: the tool impls are plain KV reads of the user's saved data; the AI value is the model REASONING over the returned summaries, never inventing a figure. **New module** lib/handlers/orchestrator-personal.js: personalTools = [listMySavedPlans, listMyPortfolios] (Anthropic tool schemas, empty input — they operate on the caller's identity, not a model-supplied arg, so the model can't request another user's data); buildPersonalImpls(email) returns impls closured over the AUTHENTICATED email (privacy by construction — there is no email parameter the model could spoof); summarisePlan/summarisePortfolio trim records to what the model needs (label, category, route, landed total, blended duty, sku count, consolidation saving, savedAt — no PII beyond the user's own data). Capped at 50 items. **Orchestrator wiring** — new buildToolset(req): resolves getCurrentUserStrict (revocation-aware); anonymous/invalid-session → base TOOLS/toolImpls unchanged; signed-in → base merged with the personal tools + impls. The handler now builds the toolset once per request and uses turnTools/turnImpls in the Anthropic loop + tool dispatch (replacing the static module-level references). No system-prompt change — the tool descriptions guide usage, so the BG-6.1 immutable prompt registry version stays put. classifyTool gains an 'account' branch so the SSE tool-call traces + domainsTouched correctly tag personal-tool calls. The base TOOLS export is unchanged (personal tools are per-request, never in the static set) so the existing "orchestrator exposes N unique tools" + tool-coverage tests stay green. 12 new tests: tool schemas (2 read-only, empty input), PERSONAL_TOOL_NAMES, summaries (plan route/landed/category + hasActual, portfolio sku/blended/landed), impls (return only the requesting user's plans/portfolios, empty-email→empty-no-leak), classifyTool→'account', base-TOOLS-unchanged invariant, buildToolset (anonymous→base only, signed-in→merged personal + base estimateLandedCost still present, impls scoped to the requester so an intruder's items never appear). 2,623 → 2,635 tests. The orchestrator is now a genuine operations assistant for a logged-in importer: ask "compare landed cost across my saved plans" or "which portfolio has the highest blended duty" and it calls the personal tool, reads your real data, and reasons over it — the differentiator vs. a generic chatbot or an Excel template. (Follow-on: a getMyPortfolioDrift tool surfacing the portfolio-drift-v1 comparison so the model can answer "what moved since I saved it" too.) |
| 2026-05-21 | Sprint portfolio-revision-v1 — weekly portfolio cost-drift email cron | Extends the proactive-monitoring layer from single plans to portfolios. plan-revision-emails has watched saved single plans for ≥5% cost drift since the BG-J era; this is the portfolio analogue, so the "24/7 import-ops team watching your costs" promise now covers whole catalogues, not just individual plans. New cron job runPortfolioRevisionEmails (lib/handlers/cron.js): scans every user's saved-portfolios index (kv.listKeys on USER_PORTFOLIOS_PREFIX, filtered to the :portfolios suffix so it doesn't pick up the :plans lists — pinned by a test), and for each user recomputes every saved portfolio against today's data via the shared portfolioHandler.composeAndAggregate (the same fan-out the live planner uses, so the cron and the UI never disagree), diffs each fresh aggregate vs the snapshot saved at save time via comparePortfolioSnapshots, and collects the material movers (≥5%). When a user has ≥1 mover it emails them a tri-locale alert; users with none are skippedNoMovers. Guard order mirrors the digest cron: 6-day dedupe (portfolio:revision-sent:<email>) FIRST, then the opt-out check (reuses the existing planRevisionEmails preference — same "we alert you when your saved costs drift" category, so one toggle governs both plan + portfolio alerts and nobody gets surprise email), THEN the expensive recompute — so opted-out / recently-emailed users cost zero compose work. Returns { scannedUsers, portfoliosChecked, usersWithMovers, sent, skippedRecent, skippedOptOut, skippedNoMovers, failedSend, dryRun } for the /dashboard/cron/ observability pane. New pure lib/portfolio-revision.js#buildPortfolioRevisionEmail(locale, movers, urls) — tri-locale (EN/PL/DE, unknown→EN) subject + body listing each moved portfolio with direction (up/down), € + % delta, and the save date, plus reopen/prefs/unsubscribe links; returns null when there are no movers (so the cron can't send an empty alert). saved-portfolios.js now exports USER_PORTFOLIOS_PREFIX/SUFFIX for the scan. GHA .github/workflows/cron.yml gains a Thursday 08:30 UTC slot (its own slot so the multi-portfolio recompute load doesn't pile onto the Tue/Fri plan-revision run) + the workflow_dispatch option + the schedule→job resolver branch. 13 new tests: email builder (null-on-no-movers, EN body with direction+delta+date+links, PL/DE localisation, unknown-locale EN-fallback, singular vs plural subject), cron lifecycle (ok:false when RESEND unset, material-mover counts in dry-run, no-drift→skippedNoMovers via a save-then-recompute-matched snapshot, opt-out→skippedOptOut, 6-day dedupe→skippedRecent, only-scans-:portfolios-not-:plans), JOBS registration, GHA workflow schedule+dispatch. The pre-existing JOBS-map cross-check test updated to include the new job. 2,610 → 2,623 tests. The portfolio capability now has its full monitoring leg: reopen shows drift on demand (portfolio-drift-v1), and the weekly cron pushes the material movers to the user's inbox unprompted — the proactive half of "your costs are being watched." |
| 2026-05-21 | Sprint portfolio-drift-v1 — "what changed since you saved" monitoring for portfolios | First piece of the proactive-monitoring capability the AI-layer survey flagged as a gap. The single-plan revision cron already watches saved plans for ≥5% cost drift; portfolios were a brand-new saved object that NOTHING watched. This adds drift detection on reopen: when a user revisits a saved portfolio, it recomputes against today's tariff/freight data and shows how the numbers moved since they saved it — the "your import-ops team is watching your costs" payoff for the portfolio capability. (Investigation note: the survey also flagged document generation as "built but unwired" — but /documents/ already has working generator pages + a /api/documents endpoint, so that was a false gap; checking first avoided a redundant rebuild.) **Pure comparison** lib/intelligence/portfolio-aggregate.js#comparePortfolioSnapshots(saved, fresh, {thresholdPct=5}) → { baselineLandedEur, currentLandedEur, landedDeltaEur, landedDeltaPct, dutyDeltaEur, blendedDutyDeltaPct, direction (up/down/flat), material }. Deterministic arithmetic, same significance bar (5%) as the plan-revision cron. Returns null when there's no usable baseline (legacy save with no snapshot, zero base, missing totals) — divide-by-zero-safe. **Refactor** the compose+aggregate fan-out out of handleCompute into a shared composeAndAggregate(rawLines) helper (DRY) used by BOTH the anonymous compute and the new refresh path. **Endpoint** POST /api/portfolio/item/<id>/refresh (authed, ownership-checked 404): loads the saved record, recomputes its stored lines against current data, diffs the fresh aggregate vs the snapshot saved at save time, returns { aggregate, lines, drift, savedAt, label }. This is server-side (testable, reusable by a future digest/cron) rather than a client-side subtraction. **UI** — /portfolio/?id=pf_… now calls /refresh instead of a client recompute: it refills the editable rows from the stored line inputs and renders the result WITH a "What changed" callout (red drift-up / green drift-down / grey flat, with the € + % move and blended-duty pp change "since you saved this on <date>"). The callout only appears on a saved-portfolio reopen (the drift field), never on a fresh anonymous compute. 10 new tests: comparison helper (material up/down with exact deltas + direction, sub-threshold not-material, null on no-baseline/zero/missing-totals/null-fresh, custom threshold), refresh endpoint (401 unauthed, 404 non-owner, 200 recompute + aggregate + lines + drift + savedAt with a stale-snapshot→material-up assertion, drift null when no saved snapshot), UI contract (/portfolio/ uses /refresh + renders pf-drift). 2,600 → 2,610 tests. The capability completes the portfolio lifecycle's monitoring leg and sets up a clean follow-on: a weekly cron that runs the same refresh+compare across all of a user's saved portfolios and emails the material movers (extending the existing plan-revision/digest monitoring to the portfolio object type). |
| 2026-05-21 | Sprint portfolio-v1 (phase 4: sharing) — public read-only portfolio links | Completes the portfolio capability to full parity with single plans (which already had shares-v1). A procurement manager can now send their whole-catalogue plan to a CFO or supplier via a public link — no sign-in for the recipient, and they see the portfolio recomputed LIVE against today's tariff/freight data (the stored lines, re-run client-side), same "CFO sees today's numbers" philosophy as single-plan shares. **Share helpers** in lib/saved-portfolios.js mirror saved-plans exactly: generateShareCode (10 hex), createShare (idempotent — reuses an existing code; ownership-checked), revokeShare (deletes the reverse index), getByShareCode (resolves via portfolio:share:<code> → id, returns label+lines+snapshot with the OWNER EMAIL AND raw share block STRIPPED — a test greps the payload for '@'), incrementShareViews. **Endpoints** added to the dispatcher: POST|DELETE /api/portfolio/item/<id>/share (authed mint/revoke; emits portfolio_share_created), GET /api/portfolio/shared/<code> (PUBLIC, rate-limited 60/min/IP, fire-and-forget view-increment + portfolio_share_opened audit with code only — no PII, 404 on unknown/revoked). **UI** — /account/portfolios/ cards gain a green "Share" button that mints a code and reveals a copyable /portfolio/?share=<code> URL + a Revoke button (with the read-only/live-recompute caveat). /portfolio/ handles ?share=<code>: it hits the public shared endpoint, shows a gold "Viewing a shared portfolio — recomputed live" banner, refills the rows from the shared lines, and auto-generates — so the recipient sees fresh numbers and can edit the rows to branch into their own plan. The share-code shape (10 hex) is validated client-side before the fetch. 12 new tests: share helpers (mint+resolve with no-PII grep, idempotent+ownership, revoke invalidates, view counter, unknown-code), endpoints (mint 401/200, revoke, public read 200-no-PII + 404-when-revoked + 404-unknown, portfolio_share_opened audit no-PII), ALLOWED_TYPES, and UI contracts (/portfolio/ ?share handling + banner, /account/portfolios/ share mint/copy/revoke wiring). 2,588 → 2,600 tests. **The portfolio capability is now fully complete** — plan a catalogue → save → revisit (recomputed) → share read-only → revoke — matching every affordance single saved plans have, but for the multi-SKU bulk/enterprise use case the original wizard couldn't serve. |
| 2026-05-21 | Sprint portfolio-v1 (phase 3: persistence) — save + revisit multi-SKU portfolios | Completes the portfolio capability to parity with single saved plans: a signed-in user can save a whole-catalogue portfolio, see it in their account, reopen it (recomputed against today's tariff/freight data), and delete it. **Persistence module** lib/saved-portfolios.js mirrors lib/saved-plans.js (KV: portfolio:<id> + user:<email>:portfolios index, 30-per-user cap, 1-year TTL, ownership-checked on every read/write, pf_<16hex> non-enumerable ids). savePortfolio sanitises each line to a fixed allowed-key set (drops stray/malicious fields incl. any email), requires ≥1 valid line (productCategory+origin+destination), and stores a sanitised aggregate snapshot (lineCount, blendedDutyRatePct, consolidationSavingEur, totals) so the list view renders without recomputing — the snapshot sanitiser strips anything not in the aggregate shape (a test pins that an injected email/secret never persists). autoLabel defaults to "N SKUs · M lanes". **Endpoints** — lib/handlers/portfolio.js refactored from a single POST into a dispatcher: bare POST /api/portfolio stays the anonymous compute (unchanged); new authed sub-actions POST /api/portfolio/save, GET /api/portfolio/list (returns snapshots, NOT full line inputs), GET|DELETE /api/portfolio/item/<id> (full record incl. lines for revisit; 404-not-403 on non-owner so existence doesn't leak). All authed via getCurrentUserStrict (revocation-aware). Records a portfolio_saved analytics event (line count only, no PII). **UI** — /portfolio/ result gains a Save block (label input + "Save to my account") shown only when /api/auth/me confirms a session (signed-out users see a "Sign in to save" prompt with a returnTo back to /portfolio/, composing with returnto-resume-v1). It remembers the submitted lines + aggregate and POSTs them to /save, then surfaces a "View saved portfolios →" link. /portfolio/?id=pf_… reopens a saved portfolio: app.js fetches the item, refills the line rows from the stored inputs, and auto-generates — so reopening recomputes with current data rather than showing a stale snapshot. New /account/portfolios/ page (noindex, 401→sign-in prompt) lists saved portfolios as cards (label, SKU count, saved date, landed total + blended duty + consolidation saving from the snapshot) with Open (→ /portfolio/?id=) and Delete (with confirm). /account/ quick-links gain "My saved portfolios". 18 new tests: persistence (save+list round-trip, drops-invalid-lines + requires-one, line-input sanitisation drops evil/email fields, autoLabel, getPortfolio + deletePortfolio ownership, snapshot sanitiser no-email-leak), endpoints (save 401/200+event, list 401/200-snapshots-not-lines, item owner-200 vs non-owner-404, delete owner-200, unknown-subaction-404, bare-POST-still-computes, portfolio_saved ALLOWED_TYPE), UI contracts (/portfolio/ signedIn-gated save + ?id= revisit, /account/portfolios/ list+open+delete, /account/ link). 2,570 → 2,588 tests. The capability is now complete end-to-end: plan a catalogue → save it → reopen it next quarter recomputed against fresh tariffs → or delete it. (Phase 4 — share a portfolio via public read-only link, like single-plan shares — remains a clean follow-on.) |
| 2026-05-21 | Sprint portfolio-v1 (phase 2: UI) — the /portfolio/ multi-SKU planner is now customer-usable | Makes phase 1's engine reachable by real users. New /portfolio/ page (single-URL app surface, noindex, same dark design language as /start/): a multi-line catalogue builder where each row is a compact form (category / origin / destination select + customs value + weight + optional HS code), Add/Remove rows (keeps ≥1), and a "Generate portfolio plan" button. Dropdowns populate from the existing GET /api/start catalogue (categories key/label, origins + destinations code/name) — no new catalogue endpoint. On generate, /portfolio/app.js collects the rows (skipping entirely-blank ones), POSTs { lines } to /api/portfolio, and renders: (1) headline stat tiles — total landed cost, blended duty rate across N SKUs, duty+VAT+brokerage, and (when > 0) the consolidation saving in green; (2) a per-SKU breakdown table (product + chapter label + HS code, lane, customs value, duty, duty %, landed) with a bold totals row carrying the blended rate; (3) a green "Consolidation opportunity" callout listing each lane's combined weight + brokerage saving + the LCL→FCL transport note; (4) a per-line-errors panel surfacing any rows the server skipped (with the original row number). All money via Intl EUR formatting, all output escapeHtml'd. Cross-links added so users discover it: /account/ quick-links gain "Plan a whole catalogue", and the single-plan wizard result gains a "Plan your whole catalogue →" link (tri-locale via new start/i18n.js btnPlanCatalogue key in EN/PL/DE). 9 new tests: page markup (line builder + template + all field classes + add/generate/result hooks + noindex + app.js load), app.js wiring (loads catalogue from /api/start, POSTs lines to /api/portfolio, renderResult surfaces total/blended/consolidation + transportConsolidatable + lineErrors, keeps ≥1 line), and the cross-links (/account/ link, wizard-result link + btnPlanCatalogue in all 3 locales). 2,561 → 2,570 tests. The capability is now end-to-end usable: add your SKUs → one click → total landed cost, real blended duty rate, and the exact brokerage saving from consolidating same-lane products. Phase 3 (save/share a portfolio to the account, like saved single plans) is the clean next follow-on. |
| 2026-05-21 | Sprint portfolio-v1 (phase 1: engine + endpoint) — multi-SKU portfolio planning, the biggest capability gap | Two independent codebase surveys both flagged the SAME #1 missing capability: the wizard plans ONE product at a time, but a real importer brings in dozens of SKUs from (often) one supplier and needs the numbers a per-SKU view can't show — total portfolio landed cost, the BLENDED effective duty rate across the catalogue, and the consolidation saving from clearing SKUs that share a lane as a single customs entry instead of N separate ones. This is the logical evolution of /start/ and roughly a ~2-3× TAM expansion (bulk/enterprise importers). Phase 1 ships the trustworthy core: the aggregation engine + the API, reusing the existing per-line calculators so every number stays as defensible as a single plan. **Pure aggregator.** New lib/intelligence/portfolio-aggregate.js#aggregatePortfolio(plans, { brokerageFee }) — takes already-composed per-line plans (each a start.composePlan output) and a brokerage-formula function (dependency-injected so the math is unit-testable without importing the customs module) and returns: portfolio totals (sum of every per-line total), blendedDutyRatePct (total duty ÷ total customs value — the single number that answers "what rate am I actually paying across my whole catalogue", invisible in any per-SKU plan, divide-by-zero-guarded), and per-lane consolidation groups. Grouping is by origin+destination: SKUs on the same lane can clear as ONE customs entry, so the group's consolidated brokerage = brokerageFee(Σ customs-lines) vs the sum of independent per-line brokerage fees — a real, deterministic saving (reuses the exact customs BROKERAGE constants, no invented numbers). Groups also surface combined weight + a transportConsolidatable flag (LCL→FCL is a further opportunity beyond the quantified brokerage saving — flagged honestly, not fabricated). Groups sort biggest-lane-first so the UI leads with the largest opportunity. Non-ok/malformed lines are filtered defensively. **Endpoint.** New POST /api/portfolio (lib/handlers/portfolio.js, registered in the consolidated router — no new serverless function): accepts { lines: [<plan-input>, …] } (1–20), composes every line in PARALLEL via the existing composePlan (so live-TARIC duty + trade-defence + preferential all apply per line exactly as in a single plan), keeps per-line validation errors so one bad SKU doesn't sink the portfolio (returns lineErrors[] with the original index), 400s only when EVERY line is invalid, rate-limited 6/min/IP (each call fans out to N composePlan calls), records a portfolio_generated analytics event (line shapes only — no PII). Response carries the aggregate + trimmed per-line plans (inputs + totals + duty summary + trade-defence + chapter label — full plans would bloat the payload at 20 lines). 19 new tests: aggregator (sums, blended rate, divide-by-zero, same-lane consolidation brokerage math pinned to the exact saving, different-lanes-don't-consolidate, biggest-lane-first sort, ignores malformed entries, no-brokerageFee→0-saving), endpoint (405, 400 empty/missing/too-many, 200 with aggregate + per-line plans + single consolidation group for two same-lane SKUs, per-line-errors-kept-while-aggregating-the-good-ones, 400 when all invalid, portfolio_generated event with no-PII grep, 429 rate limit), router registration, ALLOWED_TYPES, brokerageFee export. 2,542 → 2,561 tests. Phase 2 (a /portfolio/ UI to build + visualise a multi-line catalogue with the consolidation callout) and phase 3 (save/share portfolios) are clean follow-ons on this engine. The capability line: "plan your whole catalogue at once — see total landed cost, your real blended duty rate, and exactly how much you save by consolidating SKUs that ship the same lane." |
| 2026-05-21 | Sprint hs-refine-v1 — in-result "get a precise rate" lookup (closes the hs-suggest loop end-to-end) | Direct follow-on that compounds hs-suggest-v1. Before this, the lookup helper only existed on the wizard FORM (step 1) — but a user who already submitted and got a "chapter estimator" duty (because they left the HS field blank) had no path to a precise rate short of re-doing the whole wizard. This adds an inline lookup to the RESULT: when the duty is a chapter estimate AND the user supplied no usable (6+ digit) code, the customs section shows "This duty is a chapter average. Want a precise rate?" + a lookup; picking a code re-runs the same inputs with that HS code via the existing rerunPlan() helper → live-TARIC heading rate, in place, with no wizard round-trip. **DRY refactor.** The lookup UI from hs-suggest-v1 was extracted into a reusable createHsLookup(onPick, opts) builder (returns { toggle, panel }; class-based selectors instead of fixed IDs so it can mount more than once on the same page without id collisions; the debounced /api/hs-suggest search + candidate rendering live inside it). mountHsLookup (the wizard-form instance) is now a thin caller whose onPick fills #hsCode; the result instance's onPick calls rerunPlan({ ...state.lastInputs, hsCode }). renderPlan stashes state.lastInputs = {...inputs} so the re-run carries every original field plus the chosen code. **Gating keeps it honest + non-naggy.** The refine block only renders when mfnSource === 'chapter-estimator' AND the provided HS code has < 6 digits — so it never shows when the rate is already live-precise, and after the user picks a code (now 6 digits) it won't reappear on a subsequent TARIC miss (they supplied a code; we don't nag). Hidden in print. Tri-locale via two new start/i18n.js keys (hsRefineIntro/hsRefineCta in EN/PL/DE), so all three wizard variants get it from the one shared code path. 9 new tests: reusable builder shape (returns toggle/panel, fires onPick, owns the debounced suggest fetch), mountHsLookup built on it + fills #hsCode + idempotent guard, renderPlan stashes lastInputs, the gate logic (chapter-estimator AND <6 digits), the rerun integration (rerunPlan with merged inputs, mounted only when canRefineDuty), mount placement after the duty-source badge, i18n completeness across 3 locales, CSS presence + print-hide, and the class-based (no-dup-ID) query selector. 2,533 → 2,542 tests. The end-to-end loop is now whole: blank code → chapter estimate → "want it precise?" → describe product → pick code → live-tariff rate, all without leaving the result. |
| 2026-05-21 | Sprint hs-suggest-v1 — plain-language HS-code lookup that pushes wizard users onto the live-TARIC duty path | Pure intelligence-depth play (the dev plan's H0 north star: "without depth, paywalling is charging Pro fees for what Excel replicates"). The wizard already has a precise duty path — calculateQuoteAsync fetches the real heading rate from live TARIC — but it only fires when the user enters a commodity code, and the HS field is OPTIONAL free-text whose helper literally said "Otherwise leave blank — we'll use the chapter average." Most SME importers don't know their 8-digit code, so they left it blank and got the coarse chapter estimate — the exact "Excel template" accuracy the plan warns against. This sprint doesn't add more duty data; it gets users to the precision the system already has. **Deterministic, not LLM.** New lib/intelligence/data/hs-suggest.js — a curated keyword→HS6 map (~60 headings across the 8 wizard categories + the worked-example specials: bicycles/e-bikes, steel, aluminium) with a pure token-overlap scorer. HS6 only — the first 6 digits are the globally-harmonised, stable, verifiable System level; we deliberately do NOT invent EU CN8 suffixes (less stable, easy to get wrong). This is a SUGGESTION aid: the user confirms the pick (they see the plain-English label), and the duty RATE still comes from live TARIC, never from a number in this file. suggest(query) handles both plain-language ("cotton t-shirt" → 610910, "laptop" → 847130, "running shoes" → 640411, "e-bike" → 871160) and digit-prefix queries ("6109" → cotton tees, "85" → chapter 85), ranked by keyword-overlap score with multi-word-phrase bonuses. **Gate lowered to make HS6 count.** The live-TARIC path was gated at >= 8 digits; lowered to >= 6 because normaliseHs already validates 6-digit codes and the TARIC client resolves an HS6 to its heading MFN rate via the /headings/<4-digit> fallback — so a 6-digit suggestion now beats the chapter estimate. Strictly an accuracy improvement for ALL wizard users who supply a 6-digit code, with benign fallback-to-chapter on any TARIC miss. **Endpoint.** New GET /api/hs-suggest?q=<description> (registered in the consolidated router, no new serverless function): public + anonymous (the wizard runs signed-out too), rate-limited 60/min/IP, query length-capped at 80 chars, no PII at the wire (a test greps the response for '@'). **UI — injected once, tri-locale.** Rather than triplicate markup across /start/, /pl/start/, /de/start/ (drift risk), the shared start/app.js#mountHsLookup builds the helper after the #hsCode field: a "Don't know your code? Look it up →" toggle reveals a search box; typing (debounced 250ms, min 2 chars) hits the endpoint; candidates render as clickable rows (code · label · chapter); clicking one fills #hsCode and collapses the panel — so the next plan generation runs the precise duty path. All strings come from start/i18n.js (T.*) in EN/PL/DE, so the single code path localises automatically for all three wizard variants. New CSS in start/wizard.css (accent-coloured code, hover rows, mobile single-column). 18 new tests: dataset shape (hs6 is 6 digits, chapter = first 2, keywords present, no duplicate codes), scorer (8 common descriptions → correct top hit, digit-prefix match, 2-digit chapter match, empty/whitespace/no-match/null → empty, limit respected, score-desc ordering), endpoint (200 + candidates, empty-q → empty, over-long truncated not rejected, 405 non-GET, 429 after rate limit, no-PII grep), router registration, the >=6 gate change pinned in customs-quote.js, i18n completeness (every hsLookup* key present in all 3 locale blocks), and wizard wiring (mountHsLookup present + called, fetches /api/hs-suggest?q=, fills hsInput.value, debounced). 2,515 → 2,533 tests. The moat line: "describe your product in plain words, pick the code, and the duty comes from live tariff data — not a chapter average an Excel template could match." |
| 2026-05-21 | Sprint mfa-totp-v1 — TOTP two-factor authentication (Track 3.2 closeout) | Completes the auth-hardening arc: visible sessions (BG-3.2) → password (password-auth-v1) → now optional TOTP two-factor. Enterprise/procurement and investor-diligence both expect MFA; this ships it end-to-end (enrollment + login enforcement + backup-code recovery) because MFA you can enrol but that isn't enforced at login is worse than none (false security). **Zero-dep crypto.** New lib/totp.js implements RFC 6238 with node:crypto only — no otplib/speakeasy (keeps cold starts fast + supply chain small, same call as lib/stripe.js). HMAC-SHA1 / 6 digits / 30s step / T0=0 — the Google-Authenticator/Authy/1Password-compatible defaults. Validated against the RFC 6238 published test vectors (the test pins 287082 / 081804 / 050471 / 005924 at the canonical timestamps; base32 "Hello"→JBSWY3DP). verifyTotp allows ±1 step (±30s) for clock skew + boundary typing, constant-time-compares each candidate, and rejects non-6-digit shapes before doing any HMAC work. Secrets are raw bytes stored hex, surfaced base32 for the otpauth:// URI + manual key entry (the raw hex never reaches the client). **Two-step enrollment so no one locks themselves out.** beginMfaEnrollment writes a record with enabled=false; the gate at login checks enabled, not mere presence, so MFA does NOT bite until the user confirms a code via enableMfa — proving their authenticator is correctly configured. enableMfa flips enabled=true and generates 10 single-use backup codes (returned ONCE in plaintext, stored as SHA-256 hashes — fast hash is fine because the codes are high-entropy random, the slow-hash threat model is human passwords). **Storage:** mfa:<email> → { secretHex, enabled, enabledAt, backupCodes:[sha256], algo }; challenge tokens mfachal:<id> → email (5-min TTL). **Login enforcement on BOTH first factors.** handleLogin: after a correct password, if MFA is enabled it does NOT mint a session — it creates a challenge and returns { mfaRequired:true, challengeId, returnTo }. handleVerify (magic-link): after consuming the token, if MFA is enabled it 302s to /account/?mfa=<challengeId>(&return=…) instead of minting — and FAILS CLOSED (500, no session) if the challenge can't be created, so a KV blip can't silently bypass the second factor. handleMfaVerify (the only unauthenticated MFA endpoint) completes the challenge: rate-limited 5 attempts per challengeId (the 6th burns the challenge — critical against brute-forcing the 10^6 code space inside the 5-min window), peeks the challenge WITHOUT consuming on a wrong code (so a typo doesn't force full re-auth), accepts a TOTP code OR a single-use backup code, then deletes the challenge + mints the session + echoes a safe returnTo (composes with returnto-resume-v1 — a subscribe-flow login still lands on Stripe Checkout after the second factor). Backup codes only attempted for the 8-hex shape so a mistyped TOTP can't burn one. **Re-enroll + disable safety.** handleMfaBegin 409s if MFA is already enabled (re-enrol = disable first), so a stolen session can't downgrade via begin-clobber. handleMfaDisable requires a current code (TOTP or backup) so a stolen session can't strip the factor. **UI.** /account/security/ gains a two-factor card: Begin → shows the base32 setup key + an otpauth:// deep-link + a confirm-code field → Verify&enable → shows the 10 backup codes once; enabled state shows a Disable button that reveals a code field. /account/ gains an inline #state-mfa challenge: the password path switches to it on mfaRequired; the magic-link path lands on it via ?mfa= (stripped from the URL via replaceState so a refresh can't replay a consumed id). /api/auth/me surfaces mfaEnabled for first-paint rendering. **Deliberate v1 scope note:** MFA gates password-login + magic-link, NOT password-reset-confirm — the reset link already proves email control, and gating reset behind MFA raises lockout risk; documented as the standard "email is the recovery factor" tradeoff, revisitable as an enterprise toggle. 3 new audit types (auth_mfa_enabled / _disabled / _challenge_failed — the last is the brute-force tripwire); successful MFA sign-in folds into auth_signin with mfa:true + method so the funnel stays one stream. 26 new tests: RFC 6238 vectors + base32 vector + ±1 skew window + malformed-code rejection + otpauth param shape, storage layer (enroll→enable→verify→disable, backup-code single-use, challenge peek-not-consume), endpoints (begin 401/409/200-no-hex-leak, enable 400/200+codes+audit, disable 401-without-code/200+audit, me surfaces mfaEnabled), login enforcement (MFA account → challenge-not-session + NO cookie, non-MFA → direct session, verify completes + returnTo echo + single-use replay-fails, wrong-code 401 challenge-survives, rate-limit 429 + burn, magic-link 302-to-?mfa no-cookie), dispatcher routing, and UI contracts for both pages. 2,489 → 2,515 tests. The enterprise-conversation line: "yes, we support TOTP two-factor with backup codes — enrol from your security page, enforced on every sign-in." |
| 2026-05-21 | Sprint first-run-welcome-v1 — first-run hero on /account/ (closes the dangling ?welcome=1 loop) | Finishes a thread left half-built in password-auth-v1: signup-confirm 302s a freshly-confirmed account to /account/?welcome=1, but nothing on /account/ ever read that param — a brand-new user saw the generic "Welcome back" header instead of an orienting first-run experience. This sprint closes the loop AND broadens it into a real activation surface, which matters directly for the Founding 10 pilot (post-signup activation — do they actually build a plan? — is the metric that turns a signup into a retained user). **Two first-run signals, not one.** The hero shows when EITHER (1) ?welcome=1 is present (explicit "just created an account" signal from signup-confirm), OR (2) the onboarding endpoint reports progress.completed === 0 (a brand-new user arriving by ANY path — e.g. a first-ever magic-link sign-in that never carried ?welcome=1). The second signal is the broader, more robust one; the first lets the hero appear instantly without waiting for the onboarding round-trip. No backend change — reuses /api/account/onboarding, which already returns progress.completed from onboarding-v1. **UX.** New #welcome-hero block at the top of the signed-in state (gold-accent gradient card): kicker "Welcome to OrcaTrade", headline "Your account is ready", a one-line orientation of what a plan delivers (landed cost, duties, trade-defence, compliance, routing in ~a minute), and a single primary CTA to /start/ — which is exactly onboarding step 1 (Save your first import plan), so the hero and the getting-started checklist below it point the same direction. The pre-existing "Welcome back" header is wrapped in #returning-header and hidden for first-runners so the two don't both show. **Mechanics.** consumeWelcomeParam() reads ?welcome=1 and strips it via history.replaceState (so a refresh doesn't re-trigger the hero), preserving any other query params + the hash. applyFirstRun(on) toggles hero vs returning-header and is idempotent via a firstRunShown guard — the early `if (on && firstRunShown) return` prevents double-application when BOTH signals fire on the same load (param present AND zero progress, the common signup-confirm case). The signup-confirm producer is unchanged and still prefers returnTo over ?welcome=1, so a subscribe-resume signup (returnto-resume-v1) still lands on Stripe Checkout rather than the welcome hero — the two flows compose correctly. 10 new tests: markup contract (hero slot hidden-by-default, returning-header present, hero CTA → /start/, welcome-hero CSS defined), JS contract (consumeWelcomeParam reads + replaceState-strips the param, applyFirstRun toggles both elements, first-run fires on ?welcome=1 immediately, first-run also fires on completed===0, idempotent firstRunShown guard), and a producer check that signup-confirm still emits /account/?welcome=1 with returnTo precedence intact. One pre-existing onboarding load-order test (loadOnboarding within 800 chars of showState('signedin')) needed a comment trim — the load-order invariant is preserved, the verbose first-run comment had just pushed the call past the proximity window, so the comment was tightened rather than the test loosened. 2,479 → 2,489 tests. The activation outcome: a new pilot user who confirms their account lands on a page that tells them exactly what to do next and links them straight into it, instead of a bare account home. |
| 2026-05-21 | Sprint auth-i18n-v1 — tri-locale (EN/PL/DE) for the pre-auth funnel (signup + sign-in + reset) | Brings the auth funnel up to the same tri-locale standard as the rest of the product. The wizard, marketing, agents, emails, and 351 guide pages are all EN/PL/DE — but password-auth-v1 + the /account/ sign-in form shipped EN-only, so a Polish visitor coming off /pl/ marketing hit an English sign-up form. Polish is the biggest non-EN segment (orcatrade.pl, Polish-founded), so this was the highest-value remaining localisation gap. **Architecture-respecting approach.** Auth surfaces are a single URL by design (js/site-nav.js localizeHref explicitly never prefixes /account/ — same as /agent/ + /dashboard/). So this does NOT clone /pl/signup/ + /de/signup/ directory variants; it localises client-side instead. New js/auth-i18n.js holds an EN/PL/DE dict for every pre-auth-funnel string, a detectLocale() (precedence: ?lang=pl|de query param → navigator.language prefix → 'en' fallback), an applyAuthI18n() that walks [data-i18n] (textContent) + [data-i18n-ph] (placeholder) elements on DOM-ready, and a window.authT(key) resolver for dynamic copy (button states, error messages) — authT falls back to EN then the key itself, so a missing translation degrades gracefully rather than showing blank. Detected non-EN locale is also reflected onto <html lang> for a11y + downstream scripts (cache-preferences.js reads documentElement.lang). **Scope = pre-auth funnel only, by deliberate decision.** /signup/, /account/ (sign-in + sent states), and /account/reset/ are localised. Post-authentication account management (/account/ signed-in view, /account/billing/, /account/security/, /account/orgs/, etc.) stays English — a committed user inside the tooling is a more defensible English-only compromise than a cold visitor at the entry point, and localising the full internal surface is a much larger sprint with diminishing return. The funnel a PL/DE marketing visitor actually traverses BEFORE becoming a user is now in their language. **Wiring.** All three pages load /js/auth-i18n.js before their app.js; static copy carries data-i18n / data-i18n-ph attributes (signupTitle, signinLeadMagic, forgotPassword, resetNoTokenTitle, phNewPassword, etc.); the three app.js files gained a local `T(key, fallback)` helper (= window.authT with a literal fallback if the module didn't load) and route every dynamic string through it — toggle-button labels (magic-link ↔ password mode), in-flight states ("Sending…"/"Wysyłanie…"/"Senden…"), and all error messages. The magic-link / signup-confirm round-trip lands the user on the EN /account/ post-auth view as before — acceptable since they're authenticated by then. 19 new tests in test/auth-i18n.test.js exercise the module in a node:vm sandbox with stubbed window/document/navigator (same pattern as cookie-consent.test.js): detectLocale precedence (?lang wins over navigator, navigator prefix fallback, de-AT→de, unknown→en, unsupported ?lang=fr ignored), authT resolution (PL + DE strings, EN-then-key fallback for missing keys), dictionary completeness (every EN key has a PL AND DE translation — fails CI if a future string is added EN-only — AND no orphan PL/DE keys absent from EN), applyAuthI18n DOM application (textContent on data-i18n, placeholder on data-i18n-ph, <html lang> reflected for non-EN, left alone for EN), plus markup contracts that each page loads the module + carries the right data-i18n attributes and that all three app.js files resolve through window.authT. 2,460 → 2,479 tests. The completeness test is the load-bearing one: it makes "ship a new auth string without translating it" a CI failure, so the funnel can't silently drift back toward English. |
| 2026-05-20 | Sprint returnto-resume-v1 — anonymous Subscribe → /signup/ → automatic checkout resume after sign-in | Closes a live conversion leak the previous Stripe sprint introduced: an anonymous visitor clicking Subscribe on /pricing/ would 401 and dead-end. The Subscribe button already caught 401 and bounced to /account/?return=/pricing/ — but no auth page actually honoured the ?return param, so the user landed on /account/, signed in, and ended up at /account/ instead of back on /pricing/ mid-checkout. After this sprint the flow is: click Subscribe → /signup/?return=/pricing/?subscribe=growth-monthly → sign in via any flow (magic-link, password, or email+password signup with confirmation email) → land back at /pricing/?subscribe=growth-monthly → JS auto-fires the original checkout intent → land on Stripe Checkout. Zero "where did my session go" friction, zero re-clicks. **Open-redirect defence first.** lib/auth.js gains `isSafeReturnTo(value)` — accepts same-site relative URLs only. Rejects absolute (https://evil.com), protocol-relative (//evil.com), backslash bypasses (some browsers normalise \\ to //), javascript:/data:/mailto:/vbscript:/file: even when prefixed with /, non-strings, empty, and >512 chars. Used at THREE layers: (1) on write before persisting to KV — so an attacker can never land a tainted value in storage even briefly; (2) on read from KV before redirect — defence-in-depth in case storage is ever compromised; (3) on every JSON response from /api/auth/login + /api/auth/password/reset/confirm — so the page JS can `window.location = resp.returnTo` without naively trusting it. **Five paths threaded.** /api/auth/request body gains `returnTo`; the KV magic-link record changes from a plain email string to `{ email, returnTo }` when present (legacy plain-string records still work — handleVerify accepts both shapes). /api/auth/verify reads it and 302s there instead of /account/. /api/auth/signup carries it on both email+password (stored in `signup:<token>` pending record) and email-only (stored in the magic-link record) branches. /api/auth/signup/confirm 302s to it after promoting the password into the real slot. /api/auth/login + /api/auth/password/reset/confirm echo it in the JSON response so the client navigates. **Frontend.** /pricing/ Subscribe handler refactored to a `startCheckout(tier, cycle, ctaEl)` function reused by BOTH the click handler AND the auto-resume block; on 401 it encodes the intent in the URL as `/signup/?return=/pricing/?subscribe=<tier>-<cycle>`. On every page load /pricing/ checks `?subscribe=` — if present it strips the param via history.replaceState (so refresh doesn't loop), flips the body's data-billing attribute to match the requested cycle, and re-fires startCheckout for the parsed tier+cycle. Tier+cycle parsed defensively: only the three real paid tiers (starter/growth/scale) and the two real cycles (monthly/annual) proceed; anything else is silently ignored. The 401 path bounces to /signup/ rather than /account/ because Subscribe-mid-signin is more likely a new-user flow; /signup/ explicitly leads with that option, and existing users can pivot to "sign in instead" from there. /account/, /signup/, /account/reset/ all read `?return=` once on load (URLSearchParams), thread it through their request bodies, and (where applicable) navigate to the server-echoed validated value. 25 new tests in test/returnto-resume.test.js pin: the isSafeReturnTo allowlist (relative paths) + every blocklist branch (absolute, protocol-relative, backslash, javascript:/data:/mailto:, empty/non-string/over-long, no-leading-slash), handleRequest persistence (safe value stored as object, unsafe value silently dropped and KV stays the legacy plain-string shape), handleVerify redirect (302 to returnTo OR /account/ on legacy/missing/tainted records — the tainted-on-read test pins the defence-in-depth re-validate), handleSignup → handleSignupConfirm round-trip on BOTH email+password and email-only branches, the tainted-on-read defence on the signup-confirm path, handleLogin + handlePasswordResetConfirm response-body echoes (safe value present / unsafe value → null), createPendingSignup opts.returnTo storage (safe persisted / unsafe → null), and markup contracts for /pricing/ (uses /signup/?return=… not /account/, has the params.get('subscribe') auto-resume hook, has the startCheckout function), /account/app.js + /signup/app.js + /account/reset/app.js (pageReturnTo read, threaded into request body, server-echo honoured on success), plus the module-export sanity check. 2,435 → 2,460 tests. The investor / sales-conversation outcome: a cold visitor lands on /pricing/, clicks Subscribe Growth Monthly, gets a sign-up form pre-coupled to that intent, confirms their email, and lands on Stripe Checkout — one continuous flow. The friction that was costing every-anonymous-Subscribe-click is gone. |
| 2026-05-20 | Sprint password-auth-v1 — email + password sign-in alongside magic-link, /signup page, and forgot-password reset by email | Closes the friction of "request a sign-in link every time". Three composed phases shipped in one sprint, with password as STRICTLY ADDITIVE — magic-link keeps working for everyone, including new password-using accounts, so we can never lock anyone out of their own account. **Phase 1 — password primitives + sign-in.** lib/auth.js gains scrypt-based password hashing via `node:crypto` (zero-dep, FIPS-grade): N=2^15, r=8, p=1, dkLen=64, ~100ms per hash, per-record param storage so we can lift N later without breaking existing passwords. validatePasswordStrength enforces NIST SP 800-63B (length over complexity): min 12 chars, max 1024 (CPU-DoS guard), reject all-same-char + ≥12-char ASCII sequences, NO arbitrary upper/lower/symbol rules (that's theatre that pushes users toward reuse). New KV namespaces: `password:<email>` (record), `pwreset:<jti>` (single-use reset tokens, 1h TTL), `signup:<token>` (pending signup with hashed password, 1h TTL). New endpoints: `POST /api/auth/login` (rate-limited 10/15min/IP+email — both keys to defeat the two natural brute-force shapes; vague "Invalid email or password" message on both no-record AND mismatch so an attacker can't enumerate accounts; emits auth_signin_password OR auth_signin_failed_password audit row), `POST /api/auth/password/set` (authenticated; requires currentPassword when rotating to stop a stolen session cookie from being a permanent-takeover path; emits auth_password_set OR auth_password_changed), `POST /api/auth/password/clear` (revert to magic-link only; requires currentPassword), `POST /api/auth/password/reset/request` (5/15min/IP rate limit; UNIVERSAL 202 response regardless of whether the email has an account — same shape for known and unknown emails so the wire never leaks account existence; only mints a KV reset record when a password actually exists), `POST /api/auth/password/reset/confirm` (HMAC-verified single-use token from KV; revokes ALL other sessions for the email AFTER reset because the prior password was compromised by definition; drops a fresh session cookie so the user lands signed in). /api/auth/me now surfaces `hasPassword` so /account/security/ can render the right CTA on first paint. **Phase 2 — /signup page.** New /signup/index.html + /signup/app.js with email-only OR email+password modes (toggle). Email+password path stages the hashed password in `signup:<token>` (NOT yet in the real `password:<email>` slot) and sends a confirmation email; only on click does GET `/api/auth/signup/confirm?token=...` promote the password into the real slot + drop a session cookie + 302 to /account/?welcome=1. Without confirmation-by-email, an attacker could pre-empt sign-up for an address they don't own and lock the real owner out OR squat the magic-link auth surface. The pending-record dance closes that. /account/index.html + app.js gain a "Use password instead" toggle on the sign-in form (reveals the password field; submit POSTs to /api/auth/login) plus a "Forgot password?" link that POSTs to /api/auth/password/reset/request and shows the universal "check your inbox" confirmation. **Phase 3 — /account/reset page.** New /account/reset/index.html + app.js read ?token= from the URL, POST { token, newPassword } to /api/auth/password/reset/confirm, then either showState('done') (success — cookie was set server-side, user is signed in) or surface the server error. Tokens are 1h TTL + single-use via KV; HMAC binds the token to the email so a swap-jti attack fails verification. /account/security/ gains a password card next to the sessions list — set/change/remove flows all wired with the inline currentPassword gate. **Audit trail.** events.ALLOWED_TYPES gains 9 new types: auth_password_set/_changed/_cleared, auth_signin_password (distinct from auth_signin so the funnel splits cleanly between magic-link and password), auth_signin_failed_password (brute-force tripwire — auditor greps failures-per-email), auth_password_reset_requested/_confirmed, auth_signup_requested/_confirmed. 64 new tests cover: validatePasswordStrength (5 reason branches), hashPasswordRecord shape + verifyPasswordRecord round-trip + malformed-record defense, setPassword/verifyPassword KV round-trip + case-insensitive email + createdAt preserved across rotations, password reset tokens (round-trip + single-use + forged-sig rejection + malformed-shape rejection), pending signup tokens (round-trip + single-use + carries passwordRecord), ALLOWED_TYPES inclusion, handleLogin (405/400/401 no-record + 401 wrong-pw with identical error message, 200 + Set-Cookie + audit row, 429 rate-limit), handlePasswordSet (401 unauthed / 400 weak / 200 set / 400 missing-current-on-rotate / 401 wrong-current / 200 rotate + audit row), handlePasswordClear (200 no-op / 401 wrong-current / 200 delete + audit), handlePasswordResetRequest (universal 202 + only-real-emails-mint-tokens + 400 invalid email), handlePasswordResetConfirm (400 no-token / 400 weak / 400 invalid-token / 200 happy with cookie + revokeAll + audit / replay-prevented), handleSignup (400 invalid email / 202 email-only / 202 email+password with no real password slot yet / 400 weak), handleSignupConfirm (400 missing / 400 unknown / 302 happy with cookie + password promoted), handleMe surfaces hasPassword (false default / true after setPassword), dispatcher routing for /api/auth/login + /api/auth/password/{set,clear,reset/request,reset/confirm} + /api/auth/signup{,/confirm} + unknown-subaction 404, markup contracts for /signup/ + /account/reset/ + /account/security/ password card + /account/ password-mode toggle + forgot-password link + /signup link from /account/. 2,371 → 2,435 tests. The customer-facing outcome: the Founding 10 pilot signups can now sign up with a password (no more "where's my magic link?" inbox-anxiety), sign in instantly on every visit, and recover via email if they ever forget — without us giving up the magic-link safety net that already worked. |
| 2026-05-19 | Sprint BG-3.3 phase 2 — Stripe Checkout writes org tier directly + "higher tier wins" resolution + at-period-end cancellation | Closes the deferred-from-BG-3.3-phase-1 billing-side-of-org-tier loop. Phase 1 (2026-05-18) shipped admin curl to flip an org's tier; phase 2 makes Stripe Checkout itself the writer so the sales motion ("Acme Co buys Growth at /pricing/") lands at the right granularity (the ORG, not the buyer's personal email). Three composed picks landed together — each independently chosen as "best default" because they bake the right incentives in: (1) **Primary org as upgrade target.** When a multi-org member runs Checkout, the session metadata carries `org_id = first org from listOrgsForEmail` (oldest membership). Predictable, no UX surface needed; the user who runs Checkout is the buyer for THEIR PRIMARY org. Multi-org buyers can still re-Checkout from a different primary later — the metadata travels with each subscription independently. (2) **"Higher tier wins" resolution.** lib/tiers.js gains tierRank() (free=0 → enterprise=4); lib/user-tier.js#resolveTier rewritten to iterate ALL user orgs and pick the highest. Defence: a Growth-org member who joins a free-tier side org doesn't get silently downgraded — the resolver picks the highest rank across memberships. Ties broken by iteration order (oldest membership) preserves phase-1 behaviour for equal-rank ties so no existing user's resolved tier changes today. (3) **At-period-end cancellation via natural Stripe events.** customer.subscription.updated with cancel_at_period_end:true is an INTENTIONAL no-op — the user keeps paid access until the period actually expires, at which point customer.subscription.deleted fires and the downgrade happens. The test for this case is critical: pinning the no-op prevents a future "be helpful" refactor from accidentally downgrading paying customers mid-cycle. lib/stripe.js#createCheckoutSession accepts an `orgId` param and writes it to BOTH locations: `metadata[org_id]` on the session (so checkout.session.completed event sees it) AND `subscription_data[metadata][org_id]` on the subscription (so customer.subscription.deleted at period-end ALSO sees it, even if the buyer's org memberships have changed in the meantime). lib/handlers/billing.js#handleCheckout: looks up `orgs.listOrgsForEmail(user.email)[0]` BEFORE the Stripe call, passes orgId through, gracefully falls back to per-email writes if the org lookup throws (so an account with no org or a transient orgs-module failure still gets a working checkout). lib/handlers/billing.js#inferTierFromEvent now also extracts orgId from the merged metadata. Webhook processWebhookEvent#checkout.session.completed branches: when orgId present → `userTier.setOrgTier(orgId, …)` + writes a `stripe:org-by-customer:<customerId>` reverse index (30-day TTL) for cancellation lookups + emits `org_tier_assigned` audit row with source:'stripe'; when absent → falls back to the existing `userTier.setUserTier(email, …)` path (preserves legacy per-email Stripe buyers). customer.subscription.deleted: reads orgId from the SUBSCRIPTION metadata first (most reliable — survives membership changes), falls back to the reverse index from session-time, finally falls back to per-email downgrade for legacy subscribers. Emits `org_tier_assigned` with source:'stripe-cancelled' so an auditor can grep the full lifecycle. 15 new tests in test/bg-3-3-phase-2.test.js cover: tierRank ordering (free=0 ... enterprise=4, unknown=-1), createCheckoutSession sends org_id in metadata in BOTH places, checkout.session.completed writes tier:org:<id> when orgId present, fallback to setUserTier when no orgId, reverse map written for cancellation, subscription.deleted reads orgId from metadata → downgrades org, reverse-map fallback, per-email legacy fallback, cancel_at_period_end:true → NO-OP (the critical "at period end" pin), audit-row emissions on both upgrade + cancel, handleCheckout integration with primary org lookup. Phase-1 test updated to reflect the new "higher tier wins" semantics (phase-1 expected primary org always to win; now the highest rank wins, with primary as the tiebreaker). 2,356 → 2,371 tests. The investor / sales-conversation completion: "Acme buys Growth via Stripe → every Acme employee on /account/ sees Growth tier, with the BG-3.4 'via your organisation' badge surfacing the provenance. When Acme cancels, every employee falls back to free at the end of the paid period — not a second sooner." |
| 2026-05-19 | Sprint audit-csv-export-v1 — same CSV pattern applied to /dashboard/audit/ | One-shot adoption of the portable pattern flagged in leads-csv-export-v1: extend /api/audit with ?format=csv, add an Export CSV button. Direct procurement value — "send me the audit log of who did what" gets a real CSV answer. Reuses the existing redactRow output (already emailHashed + 80-char message truncation), so the serialiser layer doesn't introduce ANY new redaction logic — that would be a security regression if it ever drifted from the redactRow contract. CSV_COLUMNS chosen for compliance / DPA-review use cases: at, type, emailHash, planId, orgId, ip, payload. payload column captures every non-reserved key as JSON-stringified extras (sorted alphabetically for determinism) — handles the long tail of audit row shapes (auth_signin's sid + source, plan_saved's various extras, org_member_invited's role + inviteeEmail-now-emailHash, etc.) without adding 30 columns. Reserved-set defence: even if a caller forgets to redact and passes a raw `email`, it's marked reserved → never flows into the payload bucket → never leaks. CSV default limit is 5000 (vs 200 for the JSON dashboard path) because the use case is "send me the data" not "render the visible table"; JSON path keeps its 1000-row ceiling unchanged. /dashboard/audit/ gains an Export CSV button next to Reload — same auth path (cookie OR token from input + sessionStorage), Blob+ObjectURL download with the server's filename, disabled while in flight. Shares the type filter — if you've narrowed to plan_share_opened in the UI, that's what exports. 18 new tests: RFC 4180 escaping (already proven in leads-csv but pinned here for the audit projection), CSV_COLUMNS shape, rowsToCsv (header / empty / standard fields / JSON-payload extras / empty payload / raw-email defensive — confirms that even a contract-violating caller cannot leak email through this serialiser), csvFilename pattern, end-to-end (200 with right headers + body starting with header row, raw email NEVER appears in body even when KV rows had one — emailHash column carries 12-hex pseudonyms instead, 401 still gates, 503 when no admin auth configured, type filter narrows, CSV default limit 5000 vs JSON default 200, JSON path unchanged), markup contract (Export CSV button present), JS wiring (fetch /api/audit?format=csv + URL.createObjectURL + exportCsv function), module surface. 2,330 → 2,348 tests. Same pattern still portable to /dashboard/calibration/ in a one-line sprint if needed — would just need a different CSV projection (dimension/key/sampleSize/avgVariancePct/totalEstimateEur/totalActualEur). |
| 2026-05-19 | Sprint leads-csv-export-v1 — procurement-friendly CSV export on /dashboard/leads/ | Every serious procurement / investor conversation eventually asks "can you send me the raw data?" Today the answer is "let me build that." After this sprint it's "here's the CSV." Extends /api/leads with ?format=csv: same auth gate, same query params (since/type/limit), same event source — just a different serialiser at the response boundary. lib/handlers/leads.js gains escapeCsvField (RFC 4180: null→empty, wraps in quotes on comma/newline/quote, doubles embedded quotes), eventsToCsv (projection across 13 columns: at, type, locale, productCategory, originCountry, destinationCountry, route, landedTotalEur, hsCodeProvided, hsCodeLength, dutyMfnSource, emailCaptured, emailHash), csvFilename (ISO-stamped, .csv ext). PII discipline matches the audit dashboard: raw email NEVER reaches the wire — KV-shape rows get the email hashed to a 12-hex prefix at the serialiser boundary, PG-shape rows pass through their pre-hashed emailHash unchanged, deleted-pseudonym addresses are NOT re-hashed (they're already opaque). Response sets Content-Type: text/csv; charset=utf-8 + Content-Disposition: attachment; filename="orcatrade-leads-<iso>.csv" + Cache-Control: no-store. /dashboard/leads/ gains a single "Export CSV" button at the top-right of the dashboard; on click fetches the CSV URL with the current auth (cookie OR sessionStorage token OR URL ?token=), reads the Blob, triggers a hidden-<a>-click download with the server-suggested filename. Disabled while in flight, restored on success/failure. 21 new tests cover: escapeCsvField (5 cases including the null/quote/comma/newline RFC paths), eventsToCsv (header row, empty case, full projection from import_plan_generated, hash-on-raw-email, pre-hashed PG passthrough, deleted-pseudonym defensive, comma-escape via productCategory), csvFilename pattern, end-to-end via /api/leads?format=csv (200 with right headers, 401 still gates, 503 when no admin auth, no-raw-email-in-body grep across multi-event corpus, ?type filter narrows export), /dashboard/leads/ markup + JS contract (button present + JS wires to /api/leads?format=csv + URL.createObjectURL for download), module surface. 2,309 → 2,330 tests. Pattern is portable: /dashboard/audit/ + /dashboard/calibration/ can adopt the same ?format=csv + escapeCsvField + button-with-Blob-download flow in a one-line sprint each when needed. |
| 2026-05-19 | Sprint wizard-step-funnel-v1 — per-step funnel measurement built before traffic arrives | Today we have `import_plan_generated` events on completion but no step-by-step dropoff data — every UX decision about the 6-step wizard is a guess. wizard-step-funnel-v1 closes the gap with the smallest possible always-on measurement: fire a `wizard_step_completed` event on each Next / Back / Submit click, aggregate per (step, action) on /dashboard/leads/. Cheap insurance — when traffic arrives the dataset is sitting there. New POST /api/wizard-event handler (lib/handlers/wizard-event.js): unauth (the wizard is anonymous + signed-in alike), rate-limited 120/min/IP (a full 6-step round trip with some Back navigation fits comfortably), strict step validation (integer 1-6), action validation (one of next / back / submit / entered), locale fallback to 'en' on unknown. Fire-and-forget events.record — a slow KV write doesn't delay the user's next step; failure is silently dropped (telemetry must never break a user flow). No PII at the wire: payload is `{ step, action, locale }` only — no email, no inputs, no IP in the persisted row. events.ALLOWED_TYPES gains `wizard_step_completed` (one-line addition). start/app.js gains fireWizardStep(step, action) — fetch with keepalive:true so the final submit event survives the page navigation that follows. Wired into all three transition points (Next click, Back click, Submit) plus the Enter-key shortcut. lib/events.js#aggregate extended with wizardFunnel: six-entry byStep array (1-6) carrying { step, next, back, submit, total } counters + totalNext / totalBack / totalSubmit roll-ups. Empty input renders an explicit zero-state shape so the dashboard never NaNs. Malformed wizard events (bad step) skipped defensively. /dashboard/leads/ gains a "Wizard step funnel" panel that renders the six bars with per-step "next: N · back: N · submit: N" detail line. Hidden until at least one wizard event fires — a brand-new dashboard with no traffic doesn't show empty placeholder bars. 17 new tests cover: ALLOWED_TYPES contract, method gate (POST only / 204 OPTIONS / 405 GET), step + action validation (missing step, out-of-range, unknown action, bad locale-falls-back-to-en), happy path (records full payload with no PII), each allowed action persists with its label, rate-limit (429 after 120 events from same IP within minute), aggregator (zero-state empty, counts per step+action, ignores non-wizard events, ignores malformed-step events), router registration regex, start/app.js wiring (fireWizardStep helper + next/back/submit call sites + keepalive flag), /dashboard/leads/ markup contract (panel slot + hidden default + DOM IDs), app.js renderer (renderWizardFunnel + reads s.wizardFunnel), module surface (constants exposed). 2,292 → 2,309 tests. The data answers questions like "how many users abandoned at step 4?" without external analytics — built before the traffic that would make it useful arrives, so when paid-ads or Founding 10 conversations start driving visitors, the funnel is already capturing. |
| 2026-05-19 | Sprint first-share-welcome-v1 — third activation milestone, completes the trilogy | first-plan-welcome-v1 + first-actual-welcome-v1 + first-share-welcome-v1 now cover the full user lifecycle in emails: save → calibrate → collaborate. When a user mints their first share on any plan, fire a one-shot email explaining what the recipient sees, that the URL is public-read-only, that view counts are tracked, that revocation truly invalidates bookmarks (post share-render-v1), and that the CFO sees TODAY's numbers — not a snapshot, which is the point. lib/welcome.js gains FIRST_SHARE_KEY_PREFIX (separate namespace from welcome:sent + welcome:first-actual so all three milestones can fire for the same user, three different KV keys), firstShareKey helper, FIRST_SHARE_TEMPLATES with EN/PL/DE bodies interpolating the share URL prominently at the top of the body, buildFirstShareEmail pure helper, sendFirstShareWelcomeIfFirst mirroring the other two helpers with KV-idempotency-first, circuit-wrapped Resend, send-failure does NOT write dedupe (retry possible). New no-share-url short-circuit (returns reason:'no-share-url' rather than fall through to a body with the placeholder) — the caller knows it forgot to pass the URL. lib/handlers/plans.js handleCreateShare: reads listPlans(email) BEFORE createShare and counts plans where `p.share && p.share.code` is set → priorShareCount. createShare is idempotent (re-call returns the existing record); priorShareCount=1 on the second call so the welcome doesn't re-fire. Locale via notificationPrefs.getLocale (set by email-locale-v1's write-through). Share URL constructed as ${SITE_ORIGIN}/share/<code> — same form the dashboard surfaces. Fire-and-forget IIFE wrapped in .catch(() => {}) so nothing in the welcome path can break the share mint. 19 new tests cover: module surface (separate namespace from welcomeKey + firstActualKey), EN body shape (subject mentions "share link", body carries share URL + view counter + Revocation works + TODAY's numbers concepts), PL/DE openers + URL present in both, unknown-locale EN-fallback, missing-shareUrl placeholder rendering, lifecycle (not-configured / no-share-url / no-email / write-on-success / idempotency / send-failure-no-dedupe / all-three-milestones-fire-for-same-user-independently with three separate KV keys), plans-handler integration (first share fires with the right URL, idempotent re-call on same plan does NOT re-fire, second-plan share does NOT re-fire, DE-stored-locale produces German subject/body, share mint survives a thrown Resend send). 2,273 → 2,292 tests. The activation trilogy is now complete: save → email, calibrate → email, collaborate → email. |
| 2026-05-19 | Sprint user-calibration-breakdown-v1 — per-user dimensional variance card on /account/plans/ | Today /account/plans/ shows ONE calibration number ("3.2% under estimate, value-weighted across 5 outcomes"). user-calibration-breakdown-v1 extends that with the same per-dimension breakdown the admin /dashboard/calibration/ surfaces — but scoped to ONE user's plans, rendered on the customer-facing surface. Pays off the framing the first-actual-welcome email set up ("once you have outcomes on 3+ plans, /account/plans/ shows a value-weighted roll-up of YOUR data"). Reuses lib/calibration.js#summarise — no new aggregation logic. New actuals.rowsFromPlans(plans): adapter from saved-plan record shape (`{ inputs, snapshot, actual: { landedCents } }`) to the calibration row shape calibration.summarise expects (flattens landedCents to the top level, filters plans without all three of .actual/.inputs/.snapshot or with zero/negative landedCents). Pure, no I/O, ~15 LOC. New GET /api/account/calibration sub-action in lib/handlers/account.js: getCurrentUserStrict-gated (same as the existing /api/account/* family, revocation-aware), reads listPlans(email) + rowsFromPlans + calibration.summarise → returns the same byCategory/byRoute/byOrigin/byDestination + total shape the admin endpoint does, plus a minSamples field (=WEAK_SAMPLE_THRESHOLD=3) so the UI can gate "render breakdown vs render nudge" without hard-coding the constant. Cross-user isolation guaranteed by listPlans — a test pins that another user's plans never appear. /account/plans/ markup: new #plans-calibration-breakdown card slot below the existing #plans-calibration roll-up, blue-accent left border to distinguish from the gold-accent main calibration card. CSS for .bd-row with three columns (key / pct / sample-count), direction-coloured pct (green under, amber over, gold neutral), weak rows faded to 0.45 opacity. /account/plans/app.js: loadBreakdown() fires fetch('/api/account/calibration') alongside the existing showCalibration() call, renders four bd-group sections (category, route, origin, destination), filters out empty groups so a user with one-route-only plans doesn't see a single-row "By origin" section. Hidden when total.sampleSize < minSamples — a brand-new actual-logger doesn't see noise, the weekly digest's actuals-summary line carries the signal until they have 3+ data points. Non-blocking — list still renders if /api/account/calibration fails. 13 new tests: rowsFromPlans (empty / skips no-actual / skips no-inputs-snapshot-or-bad-cents / output-shape compatible with summarise), handleCalibration (401 unauth / 200 empty / 200 aggregates 3-apparel + 1-machinery into right byCategory + byRoute with correct direction signs + weak-flag on n=1 group / cross-user isolation / minSamples surface), dispatcher (POST returns 404 since this is read-only), /account/plans/ markup contract (slot present + hidden default + breakdown CSS class), app.js contract (fetch URL + loadBreakdown + renderBreakdownCard functions + minSamples threshold read + dir-under/dir-over CSS classes used), module surface assertions. 2,260 → 2,273 tests. The investor-demo line: "show me a user with 5 actuals — apparel CN→PL ran 2% over, machinery KR→PL ran 8% under, average 3% under — that's the customer-level calibration moat, not 'industry average'." |
| 2026-05-19 | Sprint first-actual-welcome-v1 — second activation milestone, fires on first actual logged | Parallel to first-plan-welcome-v1 (shipped 2026-05-19 earlier). When a user logs their first actual outcome on any plan, fire a one-shot celebration email with the variance the system just observed — turns the most emotionally-resonant moment of the calibration loop into a tangible response instead of silence. Strategic angle: the welcome email surfaces "we calibrated against reality" at the moment when the user has just contributed the data point that makes the moat real — every email that fires moves the customer closer to the "this is mine, not an industry average" framing the calibration dashboard exists to make explicit. lib/welcome.js extended (not a new module — same one-shot transactional pattern, separate KV namespace) with: FIRST_ACTUAL_KEY_PREFIX (separate from welcome:sent: so both milestones can fire for the same user), firstActualKey helper, fmtEur + fmtPctSigned + directionWord (EN/PL/DE direction-word table for over/under/onTarget), FIRST_ACTUAL_TEMPLATES with EN/PL/DE bodies that interpolate { estimateEur, actualEur, deltaPctSigned, directionWord, firstName, planUrl, wizardUrl, prefsUrl }. Subject includes the signed pct ("+10%" or "-22.2%") so the inbox-list preview already carries the variance signal. Body explains the meaning of the number ("the delta between perShipmentLandedTotal computed at save time and the real total you just reported"), references the value-weighted roll-up that appears at 3+ outcomes, and points at /account/plans/ + /account/preferences/. buildFirstActualEmail is defensive against missing variance — renders zeros + "on target" rather than crashing on undefined. sendFirstActualWelcomeIfFirst mirrors sendWelcomeIfFirst: KV-idempotency-check-first, circuit-wrapped Resend send, send-failure does NOT write the dedupe key (retry on next attempt). lib/handlers/plans.js handleSetActual now: (1) reads listPlans(email) BEFORE setActual and counts plans where `p.actual` is set — gives priorActualCount; (2) after the existing audit emission, if priorActualCount === 0 AND variance was computable, fires sendFirstActualWelcomeIfFirst inside an async IIFE wrapped in .catch(() => {}) so nothing in the celebration path can break the actual write. The locale comes from notificationPrefs.getLocale (set on first plan save by email-locale-v1's write-through, mutable via /account/preferences/). Re-reporting an actual on the same plan: priorActualCount becomes 1 immediately after the first save → second call skips. Logging an actual on a SECOND plan after the first: priorActualCount ≥ 1 → skips. Both validated by integration tests. 20 new tests cover: module surface, separate-namespace key, EN body interpolation + signed-pct subject + "under budget" word, positive variance renders "+N.M%" + "over budget", on-target direction word, PL + DE locale openers + direction words, unknown-locale EN-fallback, defensive missing-variance, lifecycle (not-configured / no-email / write-on-success / idempotency / send-failure-no-dedupe / both-milestones-fire-for-same-user-independently), and plans-handler integration (first actual fires with correct under-budget variance string, re-report on same plan no-op, second-plan-actual after first is no-op, DE-stored-locale produces "unter Budget", actual save survives a thrown send). 2,240 → 2,260 tests. |
| 2026-05-19 | Sprint share-render-v1 — share revocation now actually invalidates bookmarked URLs | shares-v1 minted a code, mapped it to the plan, and 302-redirected /share/<code> to /start/?p=<base64>. Revoke deleted the code. BUT: anyone who bookmarked the resolved /start/?p=... URL still saw the plan after revoke — the inputs were in the URL, no server check on subsequent loads. UX theatre. share-render-v1 closes the gap. New GET /api/share-check/<code> handler (lib/handlers/share-check.js): wizard calls it on every cold load when ?share=<code> is in the URL. On valid code → 200 with { ok, code, viewCount, createdAt } + view count incremented (fire-and-forget) + plan_share_opened audit emitted (no email). On revoked / unknown → 404 (uniform response, no leakage of "this code existed but is revoked"). 60-req/min/IP rate limit (slightly higher than the 30/min on /share/<code> since the wizard hits it on every reload). Email never reaches the wire — a regression test grep-asserts the owner's email doesn't appear in the response body. /share/<code> handler: redirect target gains &share=<code> (so the wizard knows about the share); view increment + audit emission MOVED OUT — defers entirely to /api/share-check/<code>. The corollary is that every wizard load with ?share= counts as a view (not just the first click), so bookmarked revisits accumulate the counter — which is what an owner actually wants to see. start/app.js: maybeValidateShareCode() Promise-chain fires on cold load. On 404 → showShareRevokedOverlay() replaces the entire wizard shell with a centered card ("This share link is no longer active · Build your own import plan →"). On 200 → continues normally to loadFromShareUrl(). On network error → non-blocking; the URL inputs render anyway. New CSS in start/wizard.css for the overlay (gold-accent kicker, Cormorant headline, single CTA to the wizard home, locale-aware via wizardHome()). 18 new tests cover the readCode helper (query + URL fallback + empty), the method gate (405/204), invalid code shapes (400), the unknown-vs-revoked-uniform-response invariant (both → 404), happy-path response shape, the bookmark contract (three successive calls increment to 3), the audit-row contract (code + planId but NO email), the no-email-at-the-wire invariant (response.body grep), the /share/<code> redirect appending &share=<code>, router registration, and the wizard wiring (urlParams.get('share') → /api/share-check fetch → 404 triggers the showShareRevokedOverlay path → overlay CSS classes present). One pre-existing test in shares.test.js updated to reflect that /share/<code> no longer increments or audits — the new /api/share-check does that job. 2,222 → 2,240 tests. The investor-demo line: "watch — I revoke the share, and the URL my CFO bookmarked instantly becomes a 'this share has been revoked' page. Not just a deleted lookup row." |
| 2026-05-19 | Sprint cron-observability-v1 — admin dashboard for all 7 scheduled jobs | We've shipped 7 cron jobs (founder-digest, plan-revision-emails, regime-change-check, taric-warm, db-migrate, calibration-drift-check, weekly-user-digest) but had no way to answer "did Monday's digest actually fire?" Sprint cron-observability-v1 closes that visibility gap. lib/handlers/cron.js dispatcher now wraps every job call: on success it writes `cron:lastRun:<job>` to KV with { ranAt, completedAt, durationMs, ok, params, summary }; on thrown it writes `cron:lastError:<job>` with { ranAt, completedAt, durationMs, ok:false, error }. summariseJobResult is a pure helper that bounds the cardinality — scalars pass through, arrays collapse to { _len: N }, nested objects to { _keys: N } — so a taric-warm run with a 30-entry details[] doesn't bloat the KV record. ok:false return (e.g. plan-revision-emails when RESEND_API_KEY is unset) records lastRun with ok:false but NO error key — the dashboard distinguishes "ran but no-op" from "threw". 30-day TTL on both keys so a job that hasn't fired in a week is loudly missing on the dashboard. New GET /api/cron-status handler — gated by admin-auth (the same allowlist + token fallback Sprint admin-session-auth shipped, so a signed-in allowlisted founder gets straight through with no token paste). Sibling route to /api/cron — registered as `'cron-status'` in api/[...path].js to avoid colliding with the POST dispatcher's cron-token gate. Returns { ok, asOf, jobs: [{ name, lastRun, lastError }] } sorted by job name. New /dashboard/cron/ page: cookie-first silent probe (same UX pattern as the other admin dashboards), token form revealed only on 401. Each row shows name + status pill (green=ok / amber=noop / red=error / grey=never-run) + last-fire timestamp with relative ("3h ago" / "2d ago") + a single-line summary built from the summary block ("sent: 4 · skipped: 12 · 14000ms") + an error-detail block when the last fire was an error. Mobile breakpoint at 700px stacks the columns. Cross-links to /dashboard/leads, /dashboard/orgs, /dashboard/audit, /dashboard/calibration, /dashboard/ai across the top; /dashboard/leads's subtitle gains a "cron jobs →" cross-link; /account/ admin card gains a "Cron jobs" quick-link below "AI cost telemetry". 16 new tests: summariseJobResult shape (scalars / arrays-to-_len / nested-to-_keys / null + undefined / non-object passthrough), dispatcher write-through (success path persists lastRun with summary + durationMs, ok:false from a job records lastRun with ok:false but NO error key, thrown job persists lastError + returns 500), handleStatus auth (503 unconfigured / 401 anon / 405 POST / 200 session-cookie-on-allowlist / 200 legacy-token), router registration regex against api/[...path].js, /dashboard/cron/ markup contract (noindex + DOM hooks + sibling xrefs), app.js contract (cookie-first probe + token fallback + /api/cron-status fetch shape + sessionStorage persistence), and cross-link assertions on /account/ + /dashboard/leads/. 2,206 → 2,222 tests. |
| 2026-05-19 | Sprint BG-3.2 phase 2 — visible sessions list + per-device revoke (Track 3.2 closeout) | Phase 1 (2026-05-17) shipped "Sign out everywhere" — phase 2 shows users WHICH devices have access and lets them revoke one without touching the others. Closes Track 3.2 entirely. lib/auth.js: cookie payload extends with sid (16 hex chars, generated per sign-in via generateSessionId). buildSessionCookie(email, opts.sid?) honours an explicit sid for tests + handleVerify's "same sid in cookie AND KV" requirement; getCurrentUser surfaces sid (or null for pre-sprint cookies — backwards-compat carries every legacy 30-day cookie through to natural expiry without forcing a re-sign-in). New session metadata layer: session:<sid> records { email, iat, exp, ua, ip, createdAt, lastSeenAt } + session:byEmail:<email> denormalised index for the listing endpoint (hard-capped at 50 sids to bound the index). recordSession is fire-and-forget from handleVerify (a KV write failure can't break sign-in). sanitiseUa caps the UA at 250 chars + returns null on empty (prevents fingerprinting payloads from getting persisted). listSessionsForEmail returns own sessions newest-first, filtering expired + revoked + cross-email defensive cases. revokeSession ownership-checked + idempotent; isSessionRevoked returns truthy for the dead set; getCurrentUserStrict now denies ANY of: missing/forged cookie, iat < per-email min-iat (phase 1), or sid in the revoked set (phase 2). lib/handlers/auth.js: handleVerify generates a sid up front, threads it into BOTH buildSessionCookie + recordSession so the cookie and the KV record agree; audit row (auth_signin) gains sid for traceability. New GET /api/auth/sessions returns the user's own sessions with isCurrent flag computed against the requesting cookie's sid, currentSid surfaced separately so the UI can render a "legacy session" banner when null. IP is redacted at the wire — the server keeps it, the client never sees it (defensive privacy). New POST /api/auth/sessions/<sid>/revoke: ownership-checked (404 NOT 403 on mismatch so the existence of unfamiliar sids doesn't leak), audit-emits auth_session_revoked (new ALLOWED_TYPE, distinct from auth_revoke_all so an auditor can grep per-device vs all-devices flows), AND clears the session cookie when the user revoked their CURRENT session (otherwise the next request 401s with a stale cookie still in the browser). Dispatcher reroutes /api/auth/sessions (list) and /api/auth/sessions/<sid>/revoke (per-session). New /account/security/ page: noindex, breadcrumbs to /account/, leads with the relationship to "Sign out everywhere", legacy-banner hidden by default and revealed when currentSid is null, per-row card with UA hint ("Chrome on macOS"), full UA below, "This device" pill on the current row + green tint, per-row Revoke button (with a different confirm message for the current device). Revoking the current session bounces to /account/. Quick-link added to /account/. 31 new tests cover: generateSessionId (hex + uniqueness), buildSessionCookie carrying + honouring sid, getCurrentUser surfacing sid (or null for legacy cookies), sanitiseUa (trim, 250-cap, empty-null), recordSession + listSessionsForEmail (newest-first, expiry filter, case-insensitive, empty-email defensive), revokeSession (ownership, idempotent, unknown-sid), getCurrentUserStrict honouring revoked set + legacy cookies, handleListSessions (401/405/200 with isCurrent + currentSid + IP-not-leaked + legacy-cookie returns null+empty), handleRevokeSession (401/400/405/404/200 + audit emission + cookie-clear-on-self-revoke), handleVerify writes the cookie's exact sid to KV (microtask drain + listSessionsForEmail roundtrip), dispatcher routing for both new endpoints + 404 on garbage sub-sub-action, ALLOWED_TYPES inclusion, and /account/security/ markup + JS contracts. 2,175 → 2,206 tests. |
| 2026-05-19 | Sprint email-locale-v1 — every transactional email now honours per-user locale | The wizard, marketing pages, and welcome email are all tri-locale (EN/PL/DE) — but until today plan-revision emails were EN-hardcoded, weekly-digest emails passed `'en'` to the formatter, and the alt-origin "By the way" line lived in EN only. A Polish user who built plans on /pl/start/ got English Monday digests + revision emails — broken UX for the biggest non-EN traffic segment we have. lib/notification-prefs.js gains ALLOWED_LOCALES + DEFAULT_LOCALE constants + a `locale` string field on the stored prefs record (default 'en', backwards-compat for everyone created before today). setPrefs(`{ locale: 'pl' }`) round-trips with case normalisation + unknown-locale fallback to 'en' (matches the discipline of every other email formatter in the codebase — accept any string, fall back rather than throw). New setLocaleIfMissing(email, locale) write-only helper that fires from the welcome trigger on first plan save: writes the wizard's locale ONLY when no explicit user choice exists, so an admin who flipped to DE on /account/preferences/ doesn't get overwritten by a stray PL save later. No-op when wizard-locale=EN AND no record exists — avoids crystallising the default into a KV record with no information value. getLocale(email) convenience returns the resolved locale (with EN fallback) in one call. lib/handlers/account.js handleGet returns `allowedLocales` so the UI doesn't have to hard-code the list; handleSet's audit-row comparison extended to the string-valued locale field (notification_prefs_updated rows now carry `changes.locale` when it flipped). lib/handlers/cron.js gained REVISION_TEMPLATES — three locale blocks each with subject template, body template, and per-locale driver-word table (duty rates / stawki celne / Zollsätze etc.). buildRevisionEmail(locale, ctx) composes the right one; the runPlanRevisionEmails inner loop replaces ~25 lines of EN string-building with a single call. Same pattern in runWeeklyUserDigest — the hard-coded `'en'` becomes `await notificationPrefs.getLocale(userEmail)`. lib/origin-suggest.js formatLine grew an opts.locale parameter with EN/PL/DE COPY map: "By the way: routing this from VN under EVFTA…" / "Przy okazji: trasowanie z VN w ramach EVFTA…" / "Übrigens: ein Routing von VN unter EVFTA…". /account/preferences/ surfaces a third card with a `<select data-pref-key="locale">` carrying en/pl/de options; app.js gets a parallel saveLocale() to savePref() (string-valued, same optimistic-with-revert UX, same POST shape). lib/handlers/plans.js writes the locale through on first plan save (priorPlanCount === 0 branch) — fire-and-forget alongside the welcome email, both wrapped in `.catch(() => {})` so the locale persistence layer can't break the save. 23 new tests cover: prefs locale defaults + round-trips + case-normalisation + setLocaleIfMissing semantics (write-when-missing, do-not-overwrite-explicit, no-op-on-EN-default), GET/POST /api/account/preferences (locale surfaces + audit row carries change), origin-suggest formatLine in EN/PL/DE + unknown-locale fallback + null-suggestion-empty across all locales, plan-revision cron integration (PL user gets Polish subject + body, DE user gets German), weekly-digest cron integration (PL user gets Polish subject), plans handler write-through (first save persists, explicit existing locale preserved against stray wizard-locale), /account/preferences/ UI contract (locale select element present + JS saveLocale wiring). 2,152 → 2,175 tests. |
| 2026-05-19 | Sprint first-plan-welcome-v1 — one-shot welcome email on first plan saved | Closed the activation loop. Before today a new user's path was: magic-link → /account/ → build a plan → save it → silence until something drifted ≥5% (the revision cron, days or weeks later). The first-plan moment is the highest-intent slice we'll ever get from them and we were leaving it on the floor. New lib/welcome.js — pure templates + idempotent send. EN/PL/DE templates (the wizard already passes its locale via Sprint 33; we honour it with EN-fallback) each carry the three follow-on CTAs that compound user value: (1) log a real outcome to start the per-user calibration, (2) save more plans across origins/categories so the alt-origin matrix runs against the portfolio, (3) share a plan with a teammate or supplier via the public read-only link from shares-v1. sendWelcomeIfFirst is fire-and-forget: idempotency-key check FIRST (KV `welcome:sent:<email>`, 2-year TTL) so a second save in the same second is a clean no-op, Resend circuit-wrapped (same breaker name as auth + revision + digest) so a Resend outage gives a structured circuit-open fallback rather than throwing, send-failure does NOT write the dedupe key so a future run can retry, case-insensitive email normalisation matches every other persistence path. lib/handlers/plans.js handleSave reads pre-save listPlans().length so it can detect a 0→1 transition; the welcome call goes inside a Promise rejection-safe `.catch(() => {})` so nothing in the welcome path can break the underlying save. start/app.js threads LOCALE into the POST /api/plans body — the wizard already knows it from the page bootstrap (window.LOCALE) so this is a one-line change. Transactional, not marketing — no opt-out toggle; one email per email-address ever. 17 new tests: normaliseLocale (3 known + 4 unknown-fallback), buildWelcomeEmail (EN body shape + 3 CTAs + first-name personalisation + first-name-space drop when missing + PL/DE localised subjects + unknown-locale EN-fallback), sendWelcomeIfFirst lifecycle (not-configured / empty-email / successful-send writes dedupe / idempotency / case-insensitive / send-failure does NOT write dedupe so retry is possible), three integration tests against the plans handler (first save fires welcome with locale carried through + idempotency key landed, second save does NOT re-trigger across two real POSTs to the dispatcher, plan still saves OK when email.send throws), wizard contract (start/app.js POSTs locale alongside inputs). One micro-bug caught + fixed during testing: my initial welcome.js passed an OBJECT to circuit.run's `fallback` opt — circuit.run requires a FUNCTION, which would have thrown on every welcome path in production. Refactored to `({ state }) => ({ ok: false, reason: ... })` and the integration tests now verify the welcome fires end-to-end. 2,135 → 2,152 tests. |
| 2026-05-19 | Sprint richer-revision-v1 — plan-revision emails recommend a cheaper origin (+ GHA cron hygiene) | Plan-revision emails became actionable instead of merely informational. Before today the email said "your CN→PL plan moved +€500" and stopped there; the user knew something shifted but not what to do about it. richer-revision-v1 appends a single sentence when the user isn't already on the cheapest origin AND the alternative would save material money: "By the way: routing this from TR under A.TR instead of CN would now land at €128,565/shipment — €124,067 less (49%). Open the plan to see the alternatives matrix." Strategic alignment: turns the notification into a recommendation, which is exactly the "import operations team available 24/7" pitch from the North Star. New lib/origin-suggest.js — pure helper, ~80 LOC. suggestAlternativeOrigin(plan) reads composePlan's already-shipped originSensitivity (matrix, cheapestOrigin, userOrigin, savingEur/Pct, shipmentsPerYear), returns { origin, userOrigin, savingEur, savingPct (0.1pp rounded), preferential, transportMode, perShipmentLandedTotal, annualSavingEur (when shipmentsPerYear known) } or null. The null branches cover: no originSensitivity, user already on cheapest, saving below the dual-floor threshold (5% AND €500/shipment — both must clear to avoid both crying-wolf on TARIC noise AND surfacing operationally trivial switches), matrix lacks the cheapest entry, zero/negative landed total. formatLine() composes the EN sentence with the preferential phrase folded in only when present + an annual /year suffix only when shipmentsPerYear was set AND the annual ≠ the per-shipment figure. EN-only for v1 — the saved-plan record doesn't carry user locale yet; cross-locale work is a separate sprint. lib/handlers/cron.js gained computeRevisionContext (parallel to computeSnapshot) that returns both the BG-9 snapshot AND the origin suggestion from one composePlan call — keeps the weekly-digest path cheap (snapshot-only) while letting the per-plan revision path access the rich data. The email body grew a conditional block: when ctx.suggestion is non-null, a blank line + the formatLine() sentence sit between the "Driver" line and the "Open your saved plans" CTA. When null, the body shape is unchanged from before — stable plans stay quiet. GHA workflow hygiene fix: `.github/workflows/cron.yml` had gaps for THREE shipped jobs — calibration-drift-check (BG-1.7 nightly, never scheduled), weekly-user-digest (yesterday's sprint, never scheduled), db-migrate (BG-2.1 on-demand). Added: weekly-user-digest at Monday 09:00 UTC (one hour after the founder digest to avoid stepping on it), calibration-drift-check at 03:00 UTC daily (one hour before TARIC warm so alerts land before the calculator-using users do). Manual dispatch dropdown extended with weekly-user-digest, calibration-drift-check, db-migrate so any of the seven jobs can be fired by hand from the GHA UI. 20 new tests: suggestAlternativeOrigin's six null branches + four happy-path properties (preferential carries through, annual multiplier, decimal rounding, preferential may be null), formatLine's four shape conditions (null → empty, EUR+pct render, no-preferential phrase drop, /year suffix gating), two integration tests against runPlanRevisionEmails (line appears when CN e-bike → TR alternative is material, BD-EBA branch with a one-way diagnostic — the live data tables surprised us by surfacing a TR alternative even there, validating the helper works), GHA workflow contract tests (weekly-user-digest schedule + dispatch, calibration-drift-check schedule + dispatch). 2,115 → 2,135 tests. |
| 2026-05-18 | Sprint weekly-digest-v1 — Calendar-triggered weekly user digest email | Closes the engagement-loop gap. The plan-revision-emails cron is event-triggered (fires per-plan only on ≥5% drift); for a user with five stable plans, weeks pass without a touchpoint. weekly-digest-v1 ships a calendar-triggered companion that always fires (subject to opt-out) for any user with ≥1 saved plan. New lib/digest.js: pure aggregator + EN/PL/DE formatters — buildDigestPayload(records, opts) returns { planCount, planCountSignificant (≥5% drift via plan-diff), planCountWithActuals, topMover (largest |deltaPct| across the portfolio, may be null), calibration (actuals.summariseActuals roll-up) }. formatDigestText handles three locales with EN-fallback for unknowns; subject lines pluralise per locale; topMover line is omitted when planCountSignificant === 0 (avoid noise). Unsubscribe + prefs URLs included in the body via the BG-J unsubscribe-token machinery (no new auth). New cron job runWeeklyUserDigest: scans every user-plans index, opt-out check BEFORE composePlan work (saves budget), per-user idempotency via digest:lastSent:<email> with 6-day TTL (DIGEST_MIN_INTERVAL_DAYS) so a same-day re-run is a no-op but the weekly cadence re-sends, recomputes current snapshot per plan via the same path the plan-revision job uses (so the two emails never disagree about drift), bounded by maxUsers=5000 + a per-user 50-plan cap inherited from BG-J era. Email send failure increments failedSend but doesn't break the loop or write the dedupe key — next run picks up. notification-prefs.PREF_KEYS gains weeklyDigestEmails (default true, opt-out semantics matching planRevisionEmails); /account/preferences/ surfaces a second toggle card with cadence + scope labels. 27 new tests cover: aggregator (empty, missing currentSnapshot, single significant mover surfaces in topMover, multi-plan largest-|delta| wins, sub-5% drift not significant, value-weighted actuals roll-up where €100k plan dominates €100 plan), formatters (EN core lines + €1,500 / +12.5% rendering, PL+DE locale switch, unknown-locale EN-fallback, top-mover-line conditional on planCountSignificant, actuals-none vs actuals-summary branches, subject pluralisation), normaliseLocale (case-insensitive, en-fallback), prefs surface (PREF_KEYS contains weeklyDigestEmails, default true, setPrefs persists, independence from planRevisionEmails), cron lifecycle (RESEND_API_KEY-unset returns ok:false, empty-plans-list → skippedNoPlans, opt-out flip → skippedOptOut, eligible-user counts in dry-run, idempotency within 6-day window + re-fire after 8-day window, JOBS-map registration), /account/preferences/ markup contract (toggle slot + data-pref-key + headline). JOBS map cross-file test updated to include weekly-user-digest. 2,088 → 2,115 tests. The wiring story end-to-end: weekly GHA cron → POST /api/cron?job=weekly-user-digest → scan users → opt-out check → digest payload → Resend → dedupe key → next week. |
| 2026-05-18 | Sprint admin-session-auth — unified admin gate (session cookie OR token) | Until this sprint /account/ (magic-link cookie) and /dashboard/* (ORCATRADE_LEADS_TOKEN pasted into sessionStorage) lived on parallel auth systems — every admin had to do the token dance per tab and there was no nav from /account/ into the dashboards. New lib/admin-auth.js exposes verifyAdmin(req) accepting EITHER: (1) a valid session cookie whose email is on ORCATRADE_ADMIN_EMAILS (comma-separated allowlist, case-insensitive, trim-normalised); (2) the legacy X-Admin-Token header OR ?token=… query param matching ORCATRADE_LEADS_TOKEN. Both env vars independent — operations can use either, both, or neither (the latter degrades to 503 "admin not configured" distinguishable from 401 wrong-creds). Session path goes through getCurrentUserStrict so "Sign out everywhere" kicks admins out of dashboards too. Header-token strict-first-wins (header present-but-wrong does NOT fall through to query — closes a ?token=secret-by-attacker-past-fixed-header-proxy hole). All four admin handlers migrated: leads, audit, calibration, orgs (admin sub-routes). /api/auth/me now returns `isAdmin` so /account/ renders an "Admin dashboards" card (gold-accent, hidden by default, revealed only when data.isAdmin === true) with links to all 5 admin pages (leads / calibration / orgs / audit / AI). The five dashboard pages refactored to a "cookie-first probe" pattern: on cold load fetch the admin API silently (no token, credentials:same-origin sends the session cookie) — if 200 render straight away; if 401 fall back to the existing token input + sessionStorage flow without showing an error. Net effect: an admin signed into /account/ now clicks "Admin dashboards" and lands on a fully-loaded dashboard with zero password-paste interaction. The token path stays live for curl / CI / sales-tools that can't carry a session cookie. 24 new tests cover: env parsing + isAdminEmail (case-insensitive trim, unknown-input handling), isConfigured (both env paths), verifyAdmin all branches (503 unconfigured / 401 anon / 401 wrong-cookie-email / 200 session-cookie / 200 header-token / 200 query-token / 401 wrong-header-NOT-fall-through-to-query / 200 cookie-tried-before-token-even-with-wrong-token-header), handleMe surfaces isAdmin (true/false/case-insensitive-via-second-allowlist-entry), audit handler 503 reflects "no admin auth configured" + 200 with session cookie when on allowlist + 401 with session cookie when off, /account/ markup contract (admin-card slot hidden by default + links to all 5 dashboards), /account/app.js reveals admin-card only inside auth-me .then() reading data.isAdmin, dashboard cookie-first probe (calibration + orgs) + flexible URL shape regex (token query param now optional). 3 pre-existing tests adjusted to allow the new optional-token URL shape + the expanded auth-me .then() body. 2,062 → 2,088 tests. |
| 2026-05-18 | Sprint BG-9 — Calculator regression harness (Track 1.2) | Locks in numerical truth as the calculators evolve. New lib/intelligence/regression/corpus.js with nine canonical scenarios — the 8 customer-visible worked examples shipped under /examples/* (apparel CN→PL, BD-EBA, VN-EVFTA, CN e-bike 87% AD+CVD, CN aluminium AD+CBAM, TR steel A.TR+AD, IN cosmetics GSP, KR machinery EUKFTA) plus one FX-hedge edge case (VN apparel quoted in USD with 120d payment terms + 90d inventory days) that exercises the FX + working-capital paths none of the /examples pages hit. New lib/intelligence/regression/snapshot.js: pure extractSnapshot(plan) plucks the deterministic, customer-visible numeric surface (totals, customs.standard money fields, trade-defence measures sorted by type+rate desc, preferential code + ratePct, compliance ids sorted, routing primaryMode, tco.main, workingCapital, fx, originSensitivity) and drops every drifting field (asOf, fx.asOf, free-text narratives, sourcing arrays). EUR fields half-even rounded to integers; pct fields to 0.1pp; sortedStringify writes JSON with alphabetised keys + trailing newline for clean PR diffs. Frozen JSON lives in lib/intelligence/regression/__snapshots__/<slug>.json — one file per scenario. New scripts/regression-snapshot.js CLI: --scenario <slug> regenerates one; no args regenerates all; --diff reports drift without writing; unknown slug exits 1 with the available-slug list. ORCATRADE_DISABLE_LIVE_TARIC=1 forced so snapshots stay network-deterministic. 23 new tests in test/calculator-regression.test.js cover: corpus surface (unique slugs + entry shape), snapshot file lifecycle (no orphans + no stale files), extractor unit (ok-false passthrough, snapshotVersion stamp, EUR/pct rounding, trade-defence deterministic sort, compliance.ids derivation, fx-null on ok=false), sortedStringify contract (alphabetical + trailing-newline + nested), snapshotPath safety (rejects ../ + spaces + empty), CLI parseArgs surface, per-scenario regression (composePlan → extractSnapshot → strict-equal vs frozen; failure message tells the operator the exact CLI to regenerate), and a sentinel that scans /examples/* directories — fails if any page slug has no corpus entry, so a future Sprint Y addition can't ship without regression coverage. The Series-A diligence answer "every euro we show is reproducible" now has a CI artefact behind it; non-negotiable rule #6 in [docs/backend-grade-plan.md](docs/backend-grade-plan.md) is enforced for the headline plan numbers. 2,039 → 2,062 tests. |
| 2026-05-18 | Sprint onboarding-v1 — Getting-started checklist on /account/ | First-time-user nudge for the moat-building loop. New lib/onboarding.js computes a four-flag progress (planSaved / actualLogged / orgCreated / shareCreated) over EXISTING KV storage — no new tables. Each flag is read directly from the index keys (user:<email>:plans, org:byEmail:<email>) plus a bounded MAX_PROBE=10 walk of the user's plan records to check for .actual + .share fields. STEPS list is the single source of truth — adding a new step is a 3-line change. New GET /api/account/onboarding sub-action returns { progress, steps, nextStep } (gated by the existing /api/account/* auth flow). /account/ renders a gold-bordered "Getting started" card above the quick-links when at least one step is incomplete, with check-marks for done steps, "→" + bold + an inline CTA for the NEXT step the user should tackle, and line-through styling for completed ones. The card auto-hides when allDone (the user has gone through every step) so it doesn't clutter the page forever. Load order pinned by a test: loadOnboarding() runs INSIDE the /api/auth/me .then() handler so signed-out visitors never trigger a /api/account/onboarding 401 (which would otherwise show up as noise in Sentry). 19 new tests cover: STEPS shape contract, getProgress for every flag (empty email, brand-new, planSaved-only, actualLogged-after-setActual, orgCreated-after-createOrg, shareCreated-after-createShare, full-allDone), nextStep ordering (null-when-allDone, first-incomplete-in-order, null/undefined defaults), handler (401 without session, 200 payload shape, reflects state changes from other handlers), HTML markup (slot present + hidden default), JS contract (fetch URL, render function present, allDone branch, post-auth load order). The investor demo improvement: "every new user opens /account/ and sees a checklist of the moat-building actions. They know what to do next; they can see at a glance what they've done." 2,020 → 2,039 tests. |
| 2026-05-18 | Sprint prefs-v1 — Email notification preferences + one-click unsubscribe | Closes a real compliance gap. The plan-revision-emails cron (BG-J era) has been emailing anyone with a saved plan when current pricing drifts ≥5% from the snapshot — defensible as "service-related" (the user actively saved THIS plan, the email is about THAT plan), but missing an explicit opt-out surface. Three pieces ship together: (1) lib/notification-prefs.js — getPrefs/setPrefs/isEnabled over a new `prefs:<email>` KV namespace with opt-out semantics (every shipping pref defaults to TRUE; backwards-compatible with the pre-prefs behaviour so nobody who created an account before today suddenly stops receiving emails). Unknown pref keys are silently dropped on write so a future schema addition can't be smuggled through. (2) HMAC-signed unsubscribe tokens — `base64url(email).hmac-sha256-hex(email)` using ORCATRADE_AUTH_SECRET. Constant-time verify. Per-email-forever (a 6-month-old email's unsubscribe link still works). The plan-revision email body now embeds the token-signed unsubscribe URL + a manage-preferences link. (3) Two new endpoints: GET/POST /api/account/preferences (user-session-gated, follows the same dispatcher pattern as /api/account/activity from BG-5.6) and GET /api/unsubscribe?token=… (public — the signed token IS the auth — renders a small HTML confirmation page, NOT JSON, because most users hit it from a mail client). New /account/preferences/ page with iOS-style toggles, one-toggle-per-pref, optimistic-ish UX (switch flips on click, server confirms, reverts on failure). Cron-loop opt-out check lives AFTER the significance gate so `skippedOptOut` only counts users who WOULD have received an email — important so the counter is a real "opt-out rate" signal rather than inflated by users with no significant drift. The audit story: `notification_prefs_updated` audit row carries ONLY the changed key (signal-dense — no-op POSTs emit zero rows; test pins this), `plan_revision_emails_unsubscribed` is the specific one-click-path audit row so an auditor can grep that single type to answer "show me everyone who unsubscribed last quarter". 30 new tests cover: defaults (true for every pref), CRUD (write + read + unknown-key drop + empty-email guard), isEnabled (true/false branches + unknown-key returns false), HMAC tokens (round-trip + tamper rejection + malformed shapes + empty parts + case/trim normalisation), /api/account/preferences (401 unauth + 200 defaults shape + POST writes audit-emits + no-op-no-audit guard), /api/unsubscribe (405 non-GET + 400 + tampered + 200 flip-pref-emit-audit + idempotent), cron source-wiring (isEnabled invocation + skippedOptOut counter + unsubscribe URL placeholder) + result-schema (skippedOptOut field present even at N=0), ALLOWED_TYPES inclusion, UI markup contract (every DOM hook, POST endpoint, switch-revert-on-failure logic, /account/ quick-link presence), dispatcher registration. The compliance line: "Every automated email has a one-click unsubscribe in the footer + a preferences page; the audit log shows the full lifecycle (pref change → unsubscribe → re-opt-in) per user." 1,990 → 2,020 tests. |
| 2026-05-18 | Sprint shares-v1 — Public shareable saved plans | First customer-facing feature in a long stretch of admin/infra sprints. The schema's `share_code` column (BG-2.1) and `plan_share_opened` event type have been forward-looking placeholders since the corpus migration — this sprint wires both up. Use case: "send this plan to my CFO / supplier / shipping partner" without making them sign in. New lib/saved-plans share helpers: generateShareCode (10 hex chars, room for ~10^12 codes — collision-free at any realistic N), createShare (idempotent re-call returns existing code), getByShareCode (with OWNER EMAIL STRIPPED at the lookup boundary — privacy enforced in storage layer, not handler), incrementShareViews (writes viewCount + lastViewedAt), revokeShare (deletes both plan.share field AND `plan:share:<code>` reverse index). Reverse index = single KV lookup for /share/<code>, no scanning. New POST + DELETE /api/plans/<id>/share owner-gated sub-actions in the existing plans handler. New public GET /api/share/<code> handler (no auth, 30/min/IP rate-limited via consumeRateLimit — same primitive as other public endpoints). On valid code: increment view counter fire-and-forget, audit-emit `plan_share_opened` with code + planId + IP (NO email — public-traffic audit row mustn't reintroduce identity), 302 redirect to /start/?p=<base64-encoded-inputs>&from=share. The wizard already handles ?p= for the existing permalink pattern; ?from=share is a future-friendly marker. Routing: vercel.json rewrites /share/:code → /api/share/:code so the share URL the user sees is the clean form. New `plan_share_revoked` ALLOWED_TYPE emitted from the DELETE path so the audit log shows the full share lifecycle (mint → open* → revoke). /account/plans/ UI gains a per-card "→ Share this plan" toggle; once a share exists, the pane shows a readonly URL input + Copy button (navigator.clipboard with input.select() fallback for non-HTTPS local) + Revoke button (with confirm + a brutal-honest "people who already followed the link may still see it from their browser history" caveat) + view count meta line. Delegated listener pattern reused from the actuals UI — survives DOM swaps after create/revoke. Privacy / security points landed via tests: (1) getByShareCode strips email at the storage boundary (raw email never reaches public callers, can't accidentally leak through a future handler refactor); (2) plan_share_opened audit row has NO email field even though it's authenticated by URL — same defensive PII discipline as BG-5.5 + BG-5.6; (3) ownership-check on POST/DELETE returns 404 NOT 403 to avoid leaking plan existence; (4) stale index (code present, plan deleted) returns null rather than serving a half-dead record; (5) rate-limit foils plan-code-guessing brute-force. 27 new tests cover: ALLOWED_TYPES, every share helper (mint + idempotent + ownership-checked + stripped-email + stale-index defense + view increment + revoke), POST + DELETE plans-sub-resource (401 no-session + 404 unknown + 404 wrong-owner + happy + audit emission), public share handler (405 non-GET + 400 malformed + 404 unknown + 302 happy with audit + counter increment + email-not-leaked guard + encodeShareInputs round-trip + falsy-field stripping), routing registration in api/[...path].js, vercel.json rewrite presence, UI markup (share-pane CSS + share-toggle + readonly input + every action handler + Copy clipboard + Revoke confirm). The investor demo upgrade: "save a plan, click Share, hand the URL to a supplier — they see the same numbers without signing in. We see who opened it and when." 1,963 → 1,990 tests. |
| 2026-05-18 | Sprint BG-3.7 — Expandable per-org detail on /dashboard/orgs/ | Closes the BG-3.6 list view's obvious next-tap loop. Clicking a row in /dashboard/orgs/ now expands an inline detail pane below it showing the org's full member list (email + role + joinedAt) and current tier with the override timestamp. Same data the user-facing /account/orgs/?id=<id> page surfaces, but admin-token-gated so ops can inspect orgs without a session cookie. New GET /api/orgs/admin/<orgId> sub-action — same admin gate as /admin (BG-3.6) and /<id>/tier (BG-3.3 phase 1), all three now grouped behind one isAdminRoute branch in the dispatcher. Returns { org, members (full records with role + joinedAt), tier (null when no override) }. UI design points: (1) Row toggles inline — click expands, click again collapses; no separate page. Detail is fetched lazily on first expand so the list-page payload stays small even with hundreds of orgs. (2) Copy-ID button's click handler stopPropagation's so it doesn't ALSO trigger the row-expand — subtle but important because nested-interactive-elements bugs are the kind of thing that erodes UX trust. Test pins this. (3) Role pills colour-coded — owner gold, admin blue, member grey — matching the /account/orgs/ user-facing UI for visual consistency across admin + user surfaces. (4) Empty/error/loading states each render distinctly inside the expanded pane. 9 new tests on top of BG-3.6's 18 cover: detail endpoint (401 no-token + 404 unknown org + 200 happy path with role + joinedAt assertions + tier-null-when-unset + no-user-session-needed), UI markup (expandable-row CSS hooks + role-pill colours + toggleDetail wiring + fetch URL shape + stopPropagation on Copy-ID), exported handler surface. The full ops flow is now: list orgs → click into one → see members → curl tier endpoint. 1,954 → 1,963 tests. |
| 2026-05-18 | Sprint BG-3.6 — /dashboard/orgs/ admin view (every org on the platform) | Closes an obvious ops gap that BG-3.3 phase 1 left open. Sales can flip an org's tier via curl POST /api/orgs/<id>/tier, but until this sprint there was NO way to LIST orgs to find that <id> without grepping logs. Now the operator opens /dashboard/orgs/, sees every org with its name + owner + member count + current tier (org-tier override if set, else "—"), copies the Org ID with one click, and curls the tier endpoint. The same admin token gates both: ORCATRADE_LEADS_TOKEN via X-Admin-Token header OR ?token=… query. New lib/orgs.listAllOrgs({ limit }) scans every `org:<id>` KV key, skips the `org:members:*` + `org:byEmail:*` index namespaces, sorts newest-first by createdAt (records with missing createdAt sink), respects a [1, 10000] limit clamp. New GET /api/orgs/admin?token=… handler enriches each record with memberCount (from listMembers) + tier (from getOrgTier, null when not set) — kept separate from the storage-layer function so a future change in enriched shape doesn't ripple. Dispatcher reroutes the admin paths (/admin AND /<id>/tier) to bypass the user-session gate; the rest of the orgs handler is untouched. /dashboard/orgs/ page: noindex, three headline stat tiles (Orgs / With tier override / Total memberships), table with name + id + owner + member count + tier pill (colour-coded by tier — free grey / starter blue / growth gold / scale violet / enterprise red / no override dashed-grey) + createdAt + per-row Copy-ID button using navigator.clipboard with a graceful fallback when the API isn't available (non-HTTPS local testing). Inline note at the bottom shows the exact curl command to set a tier, with a pointer to /dashboard/audit/ for the org_tier_assigned audit row. Cross-links to /dashboard/leads/, /dashboard/audit/, /dashboard/calibration/, /dashboard/ai/. sessionStorage token persistence (same pattern as the other admin dashboards). 18 new tests cover: listAllOrgs (empty, skip-index-keys, newest-first sort with deterministic timestamps, limit), admin endpoint (401 no-token, 401 wrong-token, 503 env-unset, 200 enriched payload with memberCount + tier, empty list returns 0, limit defaults + clamp [1, 10000], token-via-query parity with /tier, no-user-session-needed), HTML markup contract (noindex, every DOM hook, fetch URL shape, sessionStorage persistence, curl example documented). The completion line: "We have the whole sales ops loop now — list orgs, copy the ID, flip the tier, see it in the audit log." 1,936 → 1,954 tests. |
| 2026-05-18 | Sprint BG-3.4 — Surface org-tier provenance on /account/billing/ | Closes the user-visible loop opened by BG-3.3 phase 1. Without this sprint, a Team-plan org member would see "Tier: Growth" on /account/billing/ but no explanation of why — confusing if they paid Stripe for Starter previously, or never paid anything at all. Now the page surfaces a gold-bordered "via your organisation: Acme Imports" badge that links straight to /account/orgs/?id=<orgId> when the resolved tier comes from an org override. /api/billing/me response gains 3 fields: `origin` ('org' | 'email' | 'default' — from BG-3.3 phase 1's resolveTier), `orgId`, and `orgName` (looked up lazily via orgs.getOrg only when the override actually applies). Pre-existing fields (ok, email, tierId, billingCycle, since, source, hasStripeCustomer) are unchanged so no caller breaks. /account/billing/ markup gets the badge slot (hidden by default) + an Origin row in the meta-grid; app.js reveals the badge only when origin === 'org', falls back to orgId when orgName is missing (e.g. a stale org reference). 7 new tests cover: handleMe response for default / email / org origins, graceful handling of a missing org record (orgName null but no crash), backwards-compat field-preservation guard, HTML badge slot declaration with hidden attribute + origin row, JS conditional rendering + link wiring. The investor / sales-conversation completion: "When we flip Acme Co to Growth in BG-3.3 phase 1, every Acme employee opening /account/billing/ now sees 'Growth via your organisation: Acme Imports' — the data was already correct, this sprint makes it visible." 1,929 → 1,936 tests. |
| 2026-05-18 | Sprint BG-3.3 phase 1 — Org-aware tier lookup (opt-in, backwards-compatible) | First slice of the deferred-from-BG-3.1 tier migration. Full Stripe-on-org migration has serious blast radius (webhook + every gating handler + customer support flow); phase 1 lands the contained, no-billing-side-effects foundation: an admin can override an org's tier, and every member of that org transparently picks it up via the existing userTier.resolveTier() call site. Migration is opt-in by design — orgs without an override fall through to the existing per-email tier behaviour, so nothing silently downgrades for grandfathered Stripe-paid users. New `tier:org:<orgId>` KV namespace via lib/user-tier.js additions: ORG_TIER_KEY_PREFIX, orgTierKey, getOrgTier, setOrgTier (validates against tiers catalog, throws on bad inputs), clearOrgTier (idempotent). resolveTier(email) updated with a 3-tier precedence: (1) primary org's tier if set — primary = first in orgs.listOrgsForEmail (oldest membership), deterministic; (2) per-email tier (Stripe-paid + manual); (3) free default. Returns a new `origin: 'org' | 'email' | 'default'` field for audit/debug surfaces; existing { record, tier } shape preserved so gating.js + billing.js + handlers/tiers.js callers work unchanged. Lazy-requires lib/orgs to avoid the (future) circular import. POST /api/orgs/<id>/tier + DELETE /api/orgs/<id>/tier — admin-only, gated by ORCATRADE_LEADS_TOKEN (NOT the user session cookie — this is sales-team tooling, not a self-service flow). Accepts the token via X-Admin-Token header or ?token=… query param. Emits org_tier_assigned (carries { orgId, tierId, source }) + org_tier_cleared audit events — never raw email. Dispatcher rerouted so /tier hits the admin gate BEFORE the user-session check, leaving every other sub-action's behaviour intact. 24 new tests cover org-tier CRUD (key namespace + write/read/clear + invalid-input throws), resolveTier precedence (empty email, no-org+no-email, no-org+email, in-org-no-tier-fallback, in-org-with-tier-overrides, primary-org-deterministic, backwards-compat shape), ALLOWED_TYPES inclusion, admin endpoint (401 no-token, 401 wrong-token, 503 env-unset, 400 missing/invalid tierId, 404 unknown org, happy POST + audit, header OR query token, DELETE happy + audit, no-user-session-needed). The investor / sales-conversation enabling line: "We can flip Acme Co to Growth tier in one curl, with an audit row, while we hammer out the Stripe contract — not blocked on the billing integration." Phase 2 (Stripe webhook writes here too; per-email path deprecates) is now a clean follow-on. 1,905 → 1,929 tests. |
| 2026-05-18 | Sprint BG-1.7 — Calibration drift alerts (ops paged before customers feel it) | Operational maturity layered on top of BG-1.6's analytics. When any group (route / category / origin / destination) crosses both an actionable drift threshold AND a sample-size threshold, the platform now pages ops via Sentry rather than waiting for someone to open the dashboard. Pure addition: lib/calibration.js gains findAlerts(summary, opts), which walks every dimension and returns the groups crossing ≥5% drift on ≥5 samples (defaults tighter than the dashboard's "significant" ≥3%/≥3 — alert thresholds are "do we have enough signal to make someone look at this?", not pricing-team thresholds). Sorted by |drift| desc so consumers always see the worst row first. New cron job: calibration-drift-check. Reads actuals.listFromPg() → summarises → findAlerts → emits one structured warn per alert. Via BG-4.2's log.js → Sentry drain, each warn becomes a Sentry event tagged with handler + dimension + groupKey, so Sentry dedupes on fingerprint (running the cron twice in a day doesn't double-page). Run summary persists to KV at `calibration:lastAlerts` (14-day TTL) so the dashboard can show "last run, N alerts active" without re-running the aggregator. The /api/calibration handler also surfaces the same alerts inline in its response — single source of truth between the Sentry stream and the dashboard view (they cannot drift apart, by construction). /dashboard/calibration/ gains a red-bordered alerts pane at the top showing one bullet per active alert: dimension · key, direction-coloured percentage, sample size, est→actual EUR delta. JOBS map cross-file regression test updated. 10 new tests cover: findAlerts (empty summary / too few samples / insufficient drift / multi-dim sorted by |drift| / custom opts / malformed-row defenses), runCalibrationDriftCheck (PG-unconfigured no-op writes the empty snapshot for "last run, no alerts" UX, JOBS registration), /api/calibration response (alerts array always present even at N=0, never undefined), HTML markup (alerts-pane DOM slot), JS (renderAlerts function + direction-coloured rendering). The Sprint BG-1.7 narrative: BG-1.6 made the calibration story visible to ops; BG-1.7 makes ops not need to look. 1,895 → 1,905 tests. |
| 2026-05-18 | Sprint BG-1.6 — Cross-user calibration dashboard | Track 1 reality-check loop now has a closed admin loop. New /dashboard/calibration/ + token-gated GET /api/calibration. Reads via actuals.listFromPg() (BG-1.4 phase 1.5) which JOINs saved_plans + actuals via DISTINCT ON (saved_plan_id) ORDER BY reported_at DESC — one row per plan, the latest reported value. New lib/calibration.js: pure aggregators. rowVariance computes the per-row delta (estimate vs landed); groupBy(rows, keyFn) returns finalised group summaries with value-weighted avgVariancePct + sampleSize + over/under/onTarget counts + weak/significant flags (weak = <3 samples; significant = ≥3% drift backed by ≥3 samples). Sort order: significant non-weak groups float to the top by |drift|, weak groups sink, sample size breaks ties — so the operator sees actionable drift first. summarise() rolls everything into { total, byCategory, byOrigin, byDestination, byRoute }. New lib/handlers/calibration.js: ORCATRADE_LEADS_TOKEN gate (same as /api/audit + /api/leads), constant-time compare, limit param defaults to 1000 + clamped [1, 10000]. Returns { ok, mode, limit, rowsScanned, asOf, total, byCategory, … }. /dashboard/calibration/ page: noindex, token input persisted to sessionStorage (auto-loads on revisit), six headline stat tiles (sampleSize / avgVariancePct / totalEstimateEur / totalActualEur / cumulativeDelta / under-on-over split — colour-coded by direction), four ranked tables (route / category / origin / destination) with per-row weak/significant pill badges + dir-coloured rows. Cross-links from the new page to /dashboard/leads/ + /dashboard/audit/ + /dashboard/ai/ (no reverse cross-links yet — those handlers stay untouched this sprint). 24 new tests cover: rowVariance (positive delta, null on missing inputs, div-by-zero guard, non-positive actual guard), groupBy (empty input, key grouping, value-weighted average, significant-non-weak floats above weak, missing-key skip), summarise (empty, value-weighted total + direction, route key shape + missing-parts drop, under-budget direction), handler (405 non-GET, 503 token unset, 401 missing/wrong token, 200 + empty summary when PG unconfigured, limit defaults + clamp [1,10000]), dispatcher registration, HTML markup contract (existence + noindex + every DOM hook), JS contract (fetch /api/calibration with encodeURIComponent token, 401 + 503 + ok branches, sessionStorage persistence). Now the investor demo line is concrete: "show me the calibration dashboard — apparel CN→PL is over by 3% on average across our actuals; aluminium VN→DE under by 8%. That's the moat made visible." 1,871 → 1,895 tests. |
| 2026-05-18 | Sprint BG-1.4 phase 1.5 — Actuals dual-write to Postgres | Completes the Track 1 corpus. BG-2.4 put saved_plans in PG; this sprint follows actuals into the same store, finally activating the `actuals.saved_plan_id` → `saved_plans.id` FK that's been declared in the BG-2.1 schema since the Neon migration. Same fire-and-forget pattern as events (BG-2.2) + saved_plans (BG-2.4): KV is the synchronous, user-facing primary; PG is the durable corpus for cross-user analytics (BG-1.6 calibration dashboard now becomes a SQL JOIN away). New surface in lib/actuals.js: buildPgInsertParams(planExternalId, actualRecord, ownerEmail) (pure, exported for tests — hashes the email, sets duty/freight columns to null since v1 doesn't capture the breakdown yet, normalises empty notes to null for a cleaner text column), recordPg (the INSERT resolves the FK via subquery `(SELECT id FROM saved_plans WHERE external_id = $1)` — single round-trip; if the saved_plans PG row doesn't exist because its own dual-write earlier failed, the NOT NULL FK rejects the insert cleanly, which is the right outcome — KV stays the truth and we'll backfill later), clearPg (DELETE all rows for the plan; matches the "user said remove this data" GDPR semantics — there's no acceptable middle ground), listFromPg (cross-user read returning ONE row per plan via DISTINCT ON (saved_plan_id) ORDER BY reported_at DESC — preserves revision history as separate rows on the storage side but exposes only the latest to callers, simplifying analytics). Storage choice rationale: append-only INSERT on setActual rather than UPSERT. The actuals table has no unique constraint on saved_plan_id; UPSERT requires one. Append-only preserves the revision history for free ("did the user keep tweaking the number or was the first answer the final one?") and the DISTINCT ON in listFromPg makes the "current value" view trivial. Wired fire-and-forget into setActual + clearActual. 16 new tests cover: buildPgInsertParams (email hashed + pseudonym passthrough + duty/freight null + notes-normalisation + every-missing-field throw), recordPg (not-configured + DB error structured failures + malformed-input network-short-circuit), clearPg (defensive paths), listFromPg (empty paths), setActual integration (KV-only behaviour preserved, fire-and-forget proven via Promise.race against a hung second-fetch — the first PG fetch is the saved_plans INSERT, which we let resolve fast so the test can specifically pin the actuals INSERT, PG failure doesn't corrupt KV), clearActual (KV-only mode), module surface. Now unblocked: BG-1.6 — cross-user calibration dashboard reading from `SELECT … FROM actuals a JOIN saved_plans p ON p.id = a.saved_plan_id` with no in-app aggregation. 1,855 → 1,871 tests. |
| 2026-05-18 | Sprint BG-2.4 — Saved-plans dual-write to Postgres (unblocks actuals FK + cross-user analytics) | Same pattern as BG-2.2 events: KV stays the authoritative primary (every dashboard + /account/plans/ UI reads from it), PG is the durable corpus that survives KV's 1-year TTL and can be queried across all users. Strategic unblock: the BG-2.1 schema declares `actuals.saved_plan_id REFERENCES saved_plans(id) ON DELETE CASCADE` — that FK only works once saved_plans actually exist in PG. This sprint lays that ground. New lib/saved-plans.js surface: `buildPgInsertParams(record)` (pure helper for tests + future cron use — hashes the email via lib/hash.js, strips any email field that snuck into inputs/snapshot, clamps label at 100 chars, returns the tuple for INSERT INTO saved_plans), `recordPg(record)` (async write with ON CONFLICT (external_id) DO UPDATE for future "edit existing plan" semantics + archived_at=NULL on re-save so a soft-deleted plan re-appearing is treated as a restore), `softDeletePg(externalId)` (UPDATE archived_at=now(); soft-delete keeps the audit trail + lets the future actuals FK survive), `listFromPg({ limit, includeArchived })` (cross-user read path for future calibration analytics — returns rows in a KV-compatible shape so existing aggregators can consume either source). Wired fire-and-forget into savePlan (after KV write) and deletePlan (after KV delete) — a PG outage cannot break a save/delete that already succeeded in KV. KV-only mode (DATABASE_URL unset) is a no-op for both paths. Privacy discipline matches BG-2.2: raw email NEVER lands in PG; pseudonymised "deleted-…@anonymised.local" addresses pass through verbatim (post-Article-17 identity). 18 new tests cover: buildPgInsertParams (email hashed + stripped, jsonb serialisation, null-snapshot path, 100-char label clamp + falsy→null, pseudonym passthrough, defensive email-stripping from inputs/snapshot, missing-id-or-email throw), recordPg (not-configured returns structured failure, DB error returns structured failure without throwing, malformed record fails before any network call), softDeletePg (not-configured + bad-id refusal), listFromPg (empty + DB-error defensive paths), savePlan integration (KV-only mode preserves existing behaviour, fire-and-forget proven via Promise.race against a deferred fetch — savePlan must resolve while PG fetch is still pending, otherwise the test times out, PG failure doesn't corrupt KV writes), module surface. The original timing-based fire-and-forget test (elapsed < 90ms) was too brittle under full-suite event-loop pressure (200ms cold-start cost); refactored to a Promise.race against a never-resolving fetch — deterministic, doesn't depend on wall-clock timing. Next sprint candidates: BG-1.4 phase 1.5 (actuals dual-write to PG, now unblocked), BG-1.6 (cross-user calibration dashboard reading from PG). 1,837 → 1,855 tests. |
| 2026-05-18 | Sprint BG-1.5 — Per-user calibration card on /account/plans/ | The actuals loop from BG-1.4 captured signal but the UI surfaced it only one-plan-at-a-time. This sprint adds the cumulative story above the plan list: "Across the plans you've logged outcomes for, real landed costs have come in 3.2% under OrcaTrade's estimate on a value-weighted basis." New `summariseActuals(plans)` pure helper in lib/actuals.js (130 LOC), exported alongside the existing v1 helpers. Aggregates by VALUE-WEIGHTED average — a single €1M plan shouldn't be drowned out by ten €500 ones, and vice versa — using estimate cents as the weight. Returns { planCount, withActuals, totalEstimateEur, totalActualEur, totalDeltaEur, avgVariancePct, byDirection: { over, under, onTarget } }. Rules: plans without an actual aren't counted in withActuals; plans with a zero/negative estimate are excluded entirely (would inflate "outcomes logged" without affecting the math — confusing UX). Three render states in /account/plans/app.js: (1) empty (planCount === 0) — card hidden, existing empty-state panel handles copy; (2) pre-actuals (withActuals === 0) — motivational card showing the loop EXISTS before the user engages with it ("After your next shipment lands, click + Log actual outcome…"); (3) live — direction-coloured card (green border + headline if estimates run ≥0.5% conservative on average, amber if optimistic, gold-neutral on-target) with four stat tiles (plans saved / outcomes logged / total estimated / total actual + delta) + a footer split ("3 under-budget · 1 on target · 1 over-budget"). Client-side aggregation over the existing /api/plans payload — no new endpoint, no extra fetch. The math LOC duplicated client-side is small enough to inline (avoiding shipping the whole lib to the browser); a UI-contract test pins the client copy against the canonical lib via the same function name + shape. 8 new tests cover summariseActuals (empty list, plans-without-actuals counted but excluded from variance, value-weighted average pulls big plans more, on-target stays on-target, malformed entries skipped, mixed direction byDirection split) + UI contract (calibration card DOM slot, dir-under + dir-over CSS hooks, three render states distinguishable in source, plans-calibration slot wired). The investor-demo headline: "show me a user with 5 saved plans — here's the loop, here's the cumulative variance, that's our moat compounding." 1,819 → 1,837 tests. |
| 2026-05-18 | Sprint BG-1.4 — Actuals capture v1 (Track 1 reality-check loop) | First write to the `actuals` table that's been sitting unused in the BG-2.1 schema. Every saved plan can now carry a single "actual outcome" — the total landed cost the user actually paid — and the platform computes the variance against the snapshot taken at save time. Why this matters: the LLM-never-produces-numbers rule means our calculator's accuracy IS the moat. Without actuals we'd never know when the calculator drifts. v1 captures the smallest useful loop — one number (landed EUR), one optional notes field, one actual per plan (re-reporting overwrites). v2 will add duty/freight breakdown once we have enough actuals to know which dimensions are worth asking about. New lib/actuals.js: sanitiseLandedEur (positive, ≤€1B sanity cap), sanitiseNotes (trim + 500-char cap), buildActualRecord (integer-cents internally, matches lib/intelligence/money.js BG-1.1 convention), computeVariance (compares actual to snapshot.perShipmentLandedTotal, returns direction/deltaEur/deltaPct/significant — ≥3% threshold), setActual + clearActual (KV persistence on the plan record, ownership-checked via savedPlans.getPlan). New API: POST /api/plans/<id>/actual (audit-emits `actual_reported` with planId + landedCents + deltaPct), DELETE /api/plans/<id>/actual (emits `actual_cleared`). events.ALLOWED_TYPES gains both types. lib/handlers/plans.js dispatcher extended to handle the /<id>/actual sub-resource; handleList + handleGet now enrich every plan with actualVariance when present. /account/plans/ UI: inline "Log actual outcome" toggle on each plan card expands to a small form (EUR input + notes textarea + Save/Cancel); once saved, the form collapses to a colour-coded variance badge (green for under, amber for over, neutral on-target) with estimate/actual numbers and Edit/Clear controls. Form submission is delegated via a single click listener on the list — survives DOM swaps after save/clear without re-binding. 29 new tests cover: ALLOWED_TYPES inclusion, sanitisers (positive/zero/negative/NaN/Infinity/oversize, trim+cap), buildActualRecord (cents conversion + half-even rounding + bad-input throws), computeVariance (over/under/on-target + 1% not-significant + 3% boundary significant + null on missing inputs + div-by-zero guard), setActual + clearActual (writes through, doesn't disturb other fields, re-reporting overwrites, ownership-check, idempotent clear), handler (401 unauth, 404 unknown plan, 400 bad input, happy-path response shape + audit emission with deltaPct, ownership 404 not 403, DELETE happy path + audit, GET list enriches with variance only when actual present), HTML + JS markup contract (CSS hooks, endpoint URLs, JSON body shape, render functions present). The Track 1 calibration story is now live: every customer who hits "Log actual outcome" teaches OrcaTrade where its estimate drifted from reality. 1,789 → 1,819 tests. |
| 2026-05-18 | Sprint BG-5.6 — user-facing /account/activity/ (GDPR Article 15 right of access UX) | Track 5 user-facing layer on top of BG-5.5's audit trail. New GET /api/account/activity sub-action (getCurrentUserStrict gated): scans up to 5000 events from the unified log, filters to the eight security event types where the signed-in user is the actor or target, redacts every OTHER email on each surviving row to "(another user)", caps at 50 rows, returns { ok, user, events, limit, note }. Three identity fields covered: email (actor), inviteeEmail, removedEmail, toEmail — the test pins all four against a "removed by another admin" scenario to verify the admin's email is never leaked into the removed user's view. Product events (import_plan_generated, plan_saved, ai_call, founding_applied) deliberately excluded — this page answers "what happened to my account", not "what did I do in the product"; SECURITY_EVENT_TYPES set test pins the inclusion/exclusion list. New /account/activity/ page: noindex, friendly per-event rendering with colour-coded pills (signin / logout / revoke / export / org), UTC timestamps in Geist Mono, EVENT_META table covers every BG-5.5 type, renderDetail distinguishes "you invited X" vs "you were invited by X" using the signed-in email, escapeHtml neutralises angle brackets + quotes, DOMContentLoaded handler browser-guarded for Node-test loading. Cross-link added to /account/ quick-links + footer-note cross-link to /account/privacy/ for the complete GDPR export. 27 new tests cover: filterUserActivity (empty list, security-types-only, actor + target field matches, case-insensitive email, empty-email zero, malformed entries), redactActivityRow (own email kept, foreign emails redacted, case-insensitive own-email match, transfer fields, non-identity fields preserved), SECURITY_EVENT_TYPES surface (exact 8-type match, product events excluded), handleActivity (401 unauth, empty timeline, only-mine filter, foreign-email redaction reaches the wire, 50-row hard cap), dispatcher 404 advertises the new endpoint, HTML contract (existence + noindex + every DOM hook + breadcrumb + privacy-cross-link), app.js contract (auth bootstrap + activity fetch + DOMContentLoaded guard + EVENT_META coverage of all 8 types + renderDetail inviter-vs-invitee + escapeHtml XSS-safety + quick-link from /account/). Users get the Article 15 right-of-access UX without writing to /api/account/export every time. 1,762 → 1,789 tests. |
| 2026-05-18 | Sprint BG-5.5 — audit log for security-sensitive operations | Track 5 (compliance + enterprise readiness) gains its operational audit trail. Every state-changing security operation now records a typed audit event to the existing events log: `auth_signin` (magic-link redemption, with source + IP), `auth_logout` (when a session cookie was present), `auth_revoke_all` ("Sign out everywhere" success), `org_created`, `org_member_invited` (new members only — re-invites are idempotent no-ops, not security events), `org_member_removed` (actor + target), `org_ownership_transferred` (outgoing + incoming owner, the highest-impact org operation), `account_exported` (GDPR Article 20), `account_deleted` (with pseudonym identity — the audit row carries the deleted-…@anonymised.local form, never the raw email). lib/events.js ALLOWED_TYPES gains 8 new types. lib/handlers/auth.js + lib/handlers/orgs.js + lib/handlers/account.js wired to record after each successful operation, fire-and-forget so an events-log failure can't break the user flow. Failure paths (malformed token, validation error, unauth) deliberately do NOT emit — the audit dashboard shows successful operations, not noise. Negative-case test pinning this. /dashboard/audit/ inherits all 8 new types automatically (it reads every event). 15 new tests cover: ALLOWED_TYPES surface, every handler's happy path emits the right type + payload, every handler's failure path emits nothing, invite idempotency (re-invite same email → exactly one event), delete-event identity is the pseudonym not the raw email + raw email not present anywhere on the row. account.test.js's delete test narrowed to type='founding_applied' (BG-5.5 prepends a same-prefix pseudonym row). The auditor / investor question "who did what?" now has a complete answer for every security-sensitive operation in the platform. 1,746 → 1,762 tests. |
| 2026-05-17 | Sprint BG-3.2 phase 1 — Session revocation list ("Sign out everywhere") | Track 3 second piece. Stateless sessions stay stateless: per-email `auth:rev-min-iat:<email>` KV key (31-day TTL = SESSION_TTL + 1) stores a "minimum iat" timestamp; any cookie whose iat is earlier is rejected on the strict path. lib/auth.js gains revKvKey + revokeAllSessionsForEmail + getMinIat + getCurrentUserStrict (async; null on missing/expired/forged/min-iat-violated). Deliberately NOT adding a per-session ID — that would change the cookie contract + require server-side state per session. Min-iat gives the same security guarantee ("kill every active session in one click") with minimum-invasive surface area. New POST /api/auth/revoke-all sub-action: writes the timestamp, clears the local session cookie, 200. lib/handlers/account.js + lib/handlers/orgs.js switched from getCurrentUser → getCurrentUserStrict so /api/account/* + /api/orgs/* honour revocation on the next request, on every device. Lower-stakes handlers (e.g. /api/start anonymous plan generation) keep the sync getCurrentUser. /account/ gains a "Sign out everywhere" button with confirm() guard + inline result message; success drops the page back to the sign-in form. 16 new tests cover revKvKey namespacing + email normalisation, getMinIat happy + zero-default, revoke case-insensitivity + empty-email guard, strict-check happy / blocked-by-revocation / fresh-cookie-after-revocation roundtrip, handleRevokeAll method-guard + auth-gate + happy path, dispatcher routing + 404-list contains revoke-all, UI markup contract for the button + fetch wiring + confirm() prompt, strict-gate cross-file regression guard. 1,730 → 1,746 tests. |
| 2026-05-17 | Sprint BG-3.1 closeout — /account/orgs/ UI | The org API from BG-3.1 shipped without a user-facing surface. Now /account/orgs/ is a two-mode SPA: list+create at /account/orgs/, detail (members + invite + remove) at /account/orgs/?id=<orgId>. Bootstraps from /api/auth/me (sign-in gate). Invite dropdown intentionally hides "owner" role — only transferOwnership mints owners. Remove buttons gated on admin+ AND hidden for the owner row + the signed-in user's own row. Confirms before remove. Cross-link added to /account/ quick-links. 16 markup + JS surface tests pin every contract. The Team-plan sales conversation now has a usable demo flow. 1,714 → 1,730 tests. |
| 2026-05-17 | Sprint BG-2.3 — flip dashboards to Postgres reads | events.listUnified() routes reads: PG when DATABASE_URL is set + has data, KV otherwise (fallback covers the dual-write transition where pg.events is still filling up, also covers DATABASE_URL-unset dev). lib/handlers/audit.js + lib/handlers/leads.js switched. Covers /dashboard/audit/ + /dashboard/leads/ + /dashboard/ai/ (via /api/audit?type=ai_call). aggregator's foundingRecent surfaces BOTH email (KV) and emailHash (PG) for any-identifier UX. 9 new tests on routing (KV-only, PG-empty-fallback, PG-error-fallback, type-filter), aggregator-on-PG-shape, mixed-KV+PG composition, handler contract regression guards. 5000-event KV cap is now decorative once PG has been receiving traffic long enough. 1,705 → 1,714 tests. |
| 2026-05-17 | Sprint BG-2.2 — dual-write events to Postgres | First consumer of the BG-2.1 Neon database. lib/events.js#record() now writes to BOTH KV (primary, hot path; dashboards still read from it) AND Postgres (secondary, fire-and-forget, unbounded corpus). New lib/hash.js as the single source of truth for emailHash (16-hex SHA-256, deterministic + case-insensitive + trim-stable). buildPgInsertParams pure helper strips raw emails from payload before INSERT — email_hash is the ONLY identity column in pg.events, raw emails never land in Postgres. Pseudonymised "deleted-…@anonymised.local" addresses pass through unchanged (post-Article-17 identity). New listFromPg() optional read path returns rows in the same shape KV consumers expect — future sprint flips dashboards from KV to PG with a one-line swap. KV-only mode (no DATABASE_URL) is a no-op for the PG path. 18 new tests cover hash determinism + case/trim normalisation + null handling, buildPgInsertParams (email hashed + stripped, no-email = null hash, pseudonym passthrough, defensive null/undefined), recordPg not-configured + db-error paths, record() fire-and-forget doesn't block on slow PG (<80ms verified), listFromPg empty paths, ALLOWED_TYPES preserved. 1,686 → 1,705 tests. |
| 2026-05-17 | Sprint BG-4.2 — Sentry drain (zero-dep) | Track 4's external-error-store layer goes live. lib/sentry.js builds + POSTs Sentry envelopes via raw fetch — no @sentry/node, ~zero extra bundle weight. Same pattern as lib/stripe.js. DSN parser handles EU/US/legacy/self-hosted shapes; auth header includes sentry_client lineage. lib/log.js's emit() now forwards warn + error levels to Sentry fire-and-forget when SENTRY_DSN is set; info/debug never forward (would blow quota). PII redaction already happens in emit() before either destination receives the payload. Whitelisted tag fields (handler/action/agent/model/promptVersion/requestId/tier/orgId) promote to Sentry tags for search; everything else lands in extras (with the 200-char tag-value cap enforced). 4-second timeout on the network call. /api/health gains `sentry` as the 7th subsystem (validates DSN shape only — no real network call); /status/ page knows about it. 34 new tests cover DSN parsing (valid + invalid shapes), envelope wire format (3-line per spec), log forwarding semantics (warn + error forward, info + debug stay local, no DSN → no network), PII redaction reaching the envelope body, health probe states. Once SENTRY_DSN is set on Vercel the drain activates automatically. 1,652 → 1,686 tests. |
| 2026-05-17 | Sprint BG-2.1 — Neon Postgres source-of-truth | Track 2 unblocked. Neon project `orcatrade-prod` provisioned in Frankfurt; DATABASE_URL + DATABASE_URL_UNPOOLED set on Vercel. New dependency: `@neondatabase/serverless` (OrcaTrade's 2nd runtime dep, decision documented in lib/db/client.js — the no-deps rule was about function-count + build pipeline, not literal zero). lib/db/client.js (query / queryOne / transaction / probe with slow-query log), lib/db/schema.sql (9 v1 tables — schema_versions, users, organisations, memberships, saved_plans, audit_log, events, actuals, subscriptions, prompt_runs — all idempotent, jsonb for payloads, integer-cents for money, timestamptz for time, email_hash never raw email). scripts/db-migrate.js (alphabetical file runner, schema_versions tracking with SHA-256 content hash, drift detection, --dry-run, idempotent re-runs). Wired into lib/handlers/cron.js as `db-migrate` job so GHA can apply schema changes via existing CRON_TOKEN auth. /api/health gains postgres as its 6th subsystem (degraded when unconfigured, down when unreachable); aggregator flags postgres-down as a paging condition like kv-down. /status/ page knows about it. 25 new tests + 3 existing tests updated for the new subsystem. Migration runner is ready; the schema goes live when `npm run db:migrate` runs locally with DATABASE_URL_UNPOOLED, or when GHA triggers `POST /api/cron job=db-migrate` post-redeploy. Track 2.2 (dual-write events to PG, replacing the 5000-row KV cap) is the next sprint. 1,625 → 1,650 tests. |
| 2026-05-17 | Sprint BG-3.1 foundation — Org & seat data layer + handler | First piece of Track 3 (tenancy + auth maturity). New lib/orgs.js with createOrg / getOrg / listMembers / listOrgsForEmail / addMember / removeMember / transferOwnership / hasRole. Role hierarchy owner > admin > member; owner auto-member on create; idempotent addMember; hard guards against second-owner-via-addMember and against removing the owner. Case-insensitive email normalisation. Storage: `org:<id>`, `org:members:<id>`, `org:byEmail:<email>` denormalised index. New lib/handlers/orgs.js with six URL-segment-dispatched sub-actions: GET /api/orgs, POST /api/orgs, GET /api/orgs/<id>, POST /api/orgs/<id>/invite (admin+), POST /api/orgs/<id>/remove (admin+, owner-protected), POST /api/orgs/<id>/transfer (owner-only, recipient must already be a member, old owner demoted to admin). All sub-actions 401 without a valid session. 28 tests cover the full lib API + handler dispatch + role gates + invite flow + transfer happy path + every rejection branch. Tier migration (tier:<email> → tier:<orgId>) deferred to follow-up — billing/gating/plans need to flip in lockstep — but the data layer here is forward-compatible. 1,597 → 1,625 tests. |
| 2026-05-17 | Sprint BG-6.5 — AI cost dashboard | Track 6 visibility closeout. events.ALLOWED_TYPES gains ai_call; lib/ai/cost-telemetry.js's recordAnthropicCall now writes to BOTH the structured stdout log (primary) AND the durable KV event log via fire-and-forget events.record() (secondary — KV failure can't break the request). New /dashboard/ai/ page reads /api/audit?type=ai_call (inherits the audit token-gate; ai_call rows carry no PII by construction). Client-side aggregator surfaces total spend, last-7-days spend, call count, mean cost, total tokens, spend-by-agent bar chart, spend-by-prompt-version bar chart, top-10-most-expensive-calls table. Cross-links wired between all three dashboards (leads ↔ audit ↔ AI). Aggregator + fmtCents exported via module.exports for pure-Node test exercise; DOMContentLoaded guarded behind `typeof document !== 'undefined'`. 9 tests cover ai_call ALLOWED_TYPES inclusion, end-to-end events.record write via recordAnthropicCall (drained microtasks + full payload assertion), no-PII invariant, markup hooks + /api/audit fetch target, pricing-source citation, aggregator math (empty/multi-event/last-7-days isolation), fmtCents (<100 → cents, ≥100 → euros). 1,588 → 1,597 tests. |
| 2026-05-17 | Sprint BG-6.4 — Anthropic cost telemetry | Closes Track 6's self-contained scope. New lib/ai/cost-telemetry.js with priceFor(model) reading from a frozen MODEL_PRICING_CENTS_PER_MILLION_TOKENS table (Sonnet 4.x / Opus 4.x / Haiku 4.5), computeCost(model, usage) pure + deterministic + half-even rounded with cache-read discount, withCostTelemetry({ agent, promptVersion, promptHash, model, requestId }, fn) wrapping any Anthropic fetch with latency measurement + structured log line per call ({ event:"ai_call", agent, promptVersion, promptHash, model, requestId, inputTokens, outputTokens, cacheReadTokens, costCents, latencyMs, stopReason }). Error path re-throws after logging a warn line — failed calls still count toward rate-limit budgets. lib/handlers/orchestrator.js migrated as proof-of-value (6-line wrap around the existing callAnthropic); the other 4 agents follow the same pattern. 17 tests cover priceFor (Sonnet/Opus/Haiku ratios + unknown-model fallback), computeCost (1M-token baselines, mixed call with cache discount, large-shipment scenario, cache cheaper than input invariant, never-negative defensive guard), summariseTokens (happy + null/empty), withCostTelemetry end-to-end (structured log shape captured via console-capture + error path re-throws + logs warn), recordAnthropicCall never-throws guarantee. Future cost dashboard (BG-6.5) reads from these log lines via the Axiom drain (BG-4.2). 1,570 → 1,587 tests. |
| 2026-05-17 | Sprint BG-6.2 phase 2 — live agent-eval runner unified with new tree + grounding | scripts/agent-eval.js refactored from compliance-only to per-agent. Auto-detects cases from lib/ai/evals/<agent>/cases.v1.json (new tree, 5 agents) with legacy fallback to test/agent-eval-cases.json (compliance, 15 cases). New CLI: --agent <name>, --list-cases (no API key needed), --require-grounding, --bail + positional case-id. Two case shapes normalise to a common structure; run pipeline applies BOTH legacy expectations (mustCallTools / mustCite / shouldEscalate / stopReason) AND new-shape scorer (mustContain/Not + optional calc-grounding from BG-6.3). Every internal function exported so the offline test suite exercises the whole pipeline against stubbed SSE handlers without hitting Anthropic. 18 new tests cover parseArgs (all flag forms), AGENT_HANDLERS coverage of 5 agents, loadCases auto-detection per agent, legacy + new-shape case normalisation, runCase end-to-end with a stubbed handler (clean / 2 missing patterns / tool-never-called legacy failure / hybrid composes ≥3 failures / --require-grounding catches a hallucinated 14.2% against calculator 12%), summariseEvents shape. Smoke-tested in offline mode: `--list-cases --agent orchestrator` prints all 6 cases without an API key. 1,552 → 1,570 tests. |
| 2026-05-17 | Sprint BG-5.3 — admin audit log dashboard | Track 5 closeout. New lib/handlers/audit.js GET /api/audit returns the raw event-by-event feed for an admin reviewing who-did-what (sibling to /api/leads's aggregates). Same ORCATRADE_LEADS_TOKEN gate + constant-time compare. PII discipline: redactRow() replaces every email with a deterministic 12-hex SHA-256 prefix so the same user always pseudonymises to the same identity across events; free-text messages truncated to 80 chars; pseudonymised "deleted-<hash>" emails pass through. New /dashboard/audit/ page with token persistence (sessionStorage), filterable table (type-coded colour pills, payload preview), summary tiles. /dashboard/leads/ subtitle gains an "audit log →" cross-link. 15 tests cover all four auth lifecycle paths, the redactRow contract (hash determinism + case/trim normalisation + pseudonymised-passthrough + message truncation), end-to-end PII redaction in the response, type filter, limit clamping (0 → 1, 99999 → 1000, missing → 200, NaN → 200), page-contract assertions (markup hooks, /api/audit fetch target, sessionStorage persistence). 1,537 → 1,552 tests. |
| 2026-05-17 | Sprint BG-6.3 — calc-grounding enforcement (AI moat keystone) | The core CLAUDE.md guarantee — "the LLM never produces a number that drives a business decision" — moves from prompt discipline to enforced code. lib/ai/evals/scorer.js gains: extractNumbers(text) parsing every money / percent / weight token from agent output (handles US/UK "1,234.56" AND EU "1.234,56" separators via a position-and-count heuristic), checkGrounding(response, allowedNumbers, opts) asserting every extracted token matches a calculator output within per-kind tolerance (money ±1% or ±€1, percent ±0.5pp, weight ±2%), ALWAYS_GROUNDED_NUMBERS auto-allowing citation language (small counts, 100, calendar years, 365), kind-strict matching (a money allow-list entry cannot satisfy a percent token). score() takes opts.groundedNumbers or caseSpec.requireGrounding — every ungrounded number becomes a kind:"ungrounded" failure with offending value + raw text. 17 new tests cover extractNumbers (all kinds + both separator styles + trailing-EUR), parseEuropeanNumber (6 ambiguous inputs including the "1,234 = thousands vs 1,5 = decimal" disambiguation), checkGrounding (all-grounded / hallucination caught / citation auto-allowed / tolerance branches / kind-strict guard), score integration (opt-in via opts + via requireGrounding flag), golden-path synthetic, wrong-calculator-output synthetic. When a future Claude release hallucinates "duty is 14.2%" against a calculator return of 12.0%, this layer catches it before the response reaches the user. 1,520 → 1,537 tests. |

When you complete a sprint, append here.
